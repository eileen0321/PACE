# PACE — FULL TEST 명세 (전 기능 · 예외 케이스 전수)

> **이 문서의 위치**
> - `QA_MATRIX.md` — 기능×플랫폼 지원 현황 + **출시 전 sanity**(K1~K10). 짧고, 매 출시마다 돌린다.
> - **`QA_FULL_TEST.md`(이 문서)** — 전 기능 상세 + 예외 케이스. 길고, 주기적으로/큰 변경 후에 돌린다.
>
> **표기**
> `✅` 실기기 검증 완료(날짜 표기) · `❌` 실패/버그 · `⬜` 미검증 · `🔍` 코드 검토 중 의심 발견
> `🧪` **코드 검증 완료 — 실기기 아님**(근거 파일:줄 표기). 아래 "🧪는 무엇을 보증하나" 참고
> `🤖` Android · `🍎` iOS · `🌐` 공통 · `☁️` 서버
>
> **🧪는 무엇을 보증하나** — 2026-08-13 확장 때 도입했다. "그 로직이 소스에 실제로 존재하고 분기가
> 맞다"까지만 보증한다. 기기에서 그 코드에 **도달하는지**는 보증하지 않는다 — 도달 실패는 이 앱이
> 반복해서 겪은 사고 유형이다(BAL 유예 초과로 `Linking.openURL`이 조용히 막힌 건,
> `resumeThirdPartyApp` 때문에 쇼츠 진입 코드에 영영 도달 못 한 건 둘 다 코드는 멀쩡했다).
> **🧪는 ✅로 승격되기 전 단계지 대체재가 아니다.**
>
> **원칙** — 항목은 지어내지 않는다. 전부 실제 코드/화면/스토어에서 뽑았다.
> 예외 케이스는 "이미 겪은 사고"를 우선으로 넣었다(같은 사고 반복 방지가 이 문서의 목적).

---

## A. 앱 시작 · 권한 · 온보딩 (A1~A28)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| A1 | 최초 설치 후 첫 실행 | 온보딩 6페이지 정상 표시 | ✅ 08-12 |
| A2 | 온보딩 "다음"으로 끝까지 진행 | 홈 도달 | ✅ 08-12 |
| A3 | 온보딩 중 앱 강제 종료 → 재실행 | 온보딩 처음부터 or 이어서(정의 필요) | ⬜ |
| A4 | 🤖 접근성 권한 미허용 상태로 세션 시작 | 안내 표시, 앱 크래시 없음 | ⬜ |
| A5 | 🤖 오버레이 권한 미허용 | 알약 없이도 세션은 동작, 안내 표시 | ⬜ |
| A6 | 🤖 사용정보 접근 미허용 | 포그라운드 판정 폴백, 크래시 없음 | ⬜ |
| A7 | 🤖 알림 권한 거부 | 알림만 안 오고 나머지 정상 | ⬜ |
| A8 | 🤖 카메라 권한 거부 | 손짓만 비활성, 나머지 정상 | ⬜ |
| A9 | 🤖 세션 도중 접근성 권한 회수 | `checkAccessibilityRevoked()` 동작, 사용자 안내 | ⬜ |
| A10 | 🤖 세션 도중 오버레이 권한 회수 | `checkOverlayPermissionRevoked()` 동작 | ⬜ |
| A11 | 기내모드/네트워크 없음으로 첫 실행 | 크래시 없음, 오프라인 안내 | ⬜ |
| A12 | 앱 최초 실행 시 크래시 | FATAL 0건 | ✅ 08-12 |
| A13 | 저사양/저메모리에서 프로세스 강제 종료 후 복귀 | 세션 상태 복구(prefs) | ⬜ |
| A14 | 🤖 기기 재부팅 후 세션 잔여 상태 | 정의된 동작(복구 or 종료) | ⬜ |
| A15 | 다크/라이트 테마 | 가독성 유지 | ⬜ |
| A16 | 시스템 폰트 크기 최대 | 레이아웃 깨짐 없음 | ⬜ |
| A17 | 화면 회전 | 레이아웃 유지 | ⬜ |
| A18 | 작은 화면(예: 720p) | 카드/알약 잘림 없음 | ⬜ |
| A19 | 노치/펀치홀 기기 | 상단 여백 정상 | ⬜ |
| A20 | 제스처 내비 vs 3버튼 내비 | 하단 여백 정상 | ⬜ |
| A21 | 앱 재설치 | 게스트 id 유지(기기 id 기반) | 🧪 `deviceId.ts` — 🤖 `ANDROID_ID` / 🍎 Keychain (V7) |
| A22 | 앱 데이터 삭제 후 실행 | 온보딩부터, 서버 허용량은 유지 | ⬜ |
| A23 | 두 기기에서 같은 계정 로그인 | 데이터 정합 | ⬜ |
| A24 | 딥링크로 앱 진입 | 의도한 화면 도달 | ⬜ |
| A25 | 백그라운드에서 장시간 후 복귀 | 상태 갱신 | ⬜ |
| A26 | 🤖 배터리 최적화 대상일 때 | 포그라운드 서비스 유지 안내 | ⬜ |
| A27 | 🤖 "OS가 Pace를 꺼버리는 걸 막기" 배너 탭 | 배터리 설정으로 이동 | ⬜ |
| A28 | 권한 전부 허용 후 재실행 | 권한 재요청 없음 | ⬜ |

## B. 인증 · 계정 (B1~B24)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| B1 | 게스트로 시작 | 로그인 없이 전 기능 사용 | ✅ 08-12 |
| B2 | 구글 로그인 | 성공, 프로필 표시 | ⬜ |
| B3 | 🍎 애플 로그인 | 성공 | ⬜ |
| B4 | 로그인 취소 | 게스트 유지, 크래시 없음 | ⬜ |
| B5 | 네트워크 끊긴 상태 로그인 시도 | 실패 안내, 재시도 가능 | ⬜ |
| B6 | 로그인 후 게스트 기록 승계 | 기존 시청 기록 유지 | ⬜ |
| B7 | 로그아웃 | 게스트로 복귀, 로컬 데이터 처리 정의대로 | ⬜ |
| B8 | 계정 삭제 요청 | 확인 다이얼로그 → 서버 삭제 | ⬜ |
| B9 | 계정 삭제 중 네트워크 실패 | 오류 안내, 상태 롤백 | ⬜ |
| B10 | 계정 삭제 후 재로그인 | 새 계정으로 취급 | ⬜ |
| B11 | 토큰 만료 후 API 호출 | 자동 갱신 또는 재로그인 유도 | ⬜ |
| B12 | 서버 401 응답 | 무한 재시도 루프 없음 | ⬜ |
| B13 | 서버 5xx 응답 | 사용자 기능 차단 없음(fail-open) | ⬜ |
| B14 | 재설치 후 같은 기기 id | 🤖 SSAID / 🍎 Keychain 유지 | ⬜ |
| B15 | 기기 id를 못 얻는 경우 | 폴백 동작, 크래시 없음 | ⬜ |
| B16 | 동시 로그인 요청 중복 탭 | 중복 계정 생성 없음 | ⬜ |
| B17 | 프로필 이미지 없음 | 이니셜 표시(GU) | ✅ 08-12 |
| B18 | 이메일 없는 소셜 계정 | 표시 폴백 | ⬜ |
| B19 | 로그인 상태로 앱 재시작 | 세션 유지 | ⬜ |
| B20 | 서버 시간과 기기 시간 불일치 | 일자 경계 처리 오류 없음 | ⬜ |
| B21 | 로그아웃 직후 세션 진행 중이었다면 | 세션 정상 종료·기록 | ⬜ |
| B22 | 계정 삭제 시 진행 중 세션 | 안전 종료 | ⬜ |
| B23 | 구독 중 계정 삭제 | 구독 안내 표시 | ⬜ |
| B24 | 게스트→로그인 시 서버 허용량 병합 | 단조 병합(max/OR) | ⬜ |

## C. 홈 화면 (C1~C22)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| C1 | 오늘 사용량 표시 | 실제 값과 일치 | ✅ 08-12 (52분) |
| C2 | 진행률(%) | 사용량/한도 비율 | ✅ 08-12 (87%) |
| C3 | 탭 진입 직후 사용량 0 깜빡임 | 0 깜빡임 없이 로딩 표시 | 🔍 08-12 1회 관측, 재시도 시 **재현 안 됨**(56m 즉시 정확) |
| C4 | 세션 진행 중 카드 배지 | "Active" 표시 | ✅ 08-12 |
| C5 | 유튜브 카드 탭 | 세션 시작 + 유튜브 실행 | ✅ 08-12 |
| C6 | 🤖 틱톡 카드 탭 | 세션 시작 + 틱톡 실행 | ✅ 08-12 |
| C7 | 🍎 틱톡 카드 미노출 | 목록을 못 얻어 숨김 | ⬜ |
| C8 | 대상 앱 미설치 상태에서 카드 탭 | 스토어 유도 or 웹 폴백 | 🔍 **지금 기기에서 상시 재현**(발견 7) — 웹 폴백은 되나 **빈 세션이 열린다** |
| C9 | 하루 한도 소진 후 카드 탭 | 세션 시작은 되고 연장 유도 | ⬜ |
| C10 | 카드 연타 | 세션 중복 생성 없음 | ⬜ |
| C11 | 빠른 설정(포커스/한도/휴식) 표시 | 현재 설정값 반영 | ✅ 08-12 |
| C12 | 출석 팝업 | 하루 1회만 | ⬜ |
| C13 | 출석 팝업 "확인" | 크레딧 +5 | ✅ 08-12 |
| C14 | 배너 광고 표시 | 하단 고정, 콘텐츠 가림 없음 | ✅ 08-12 |
| C15 | 배너 광고 로드 실패 | 빈 공간 없이 레이아웃 유지 | ⬜ |
| C16 | 인사이트 알림 문구 | 데이터 있을 때만 | ⬜ |
| C17 | 휴식 시간(REST) 표시 | Flip 모드 계측값 | ⬜ |
| C18 | 스크롤 시 광고/탭바 겹침 | 없음 | ⬜ |
| C19 | 홈에서 뒤로가기 | 앱 종료 확인 or 종료 | ⬜ |
| C20 | 카드 커버 이미지 로드 실패 | 그라데이션만, 깨짐 없음 | ⬜ |
| C21 | 🔍 틱톡 카드 커버 이미지 | 원본 이미지 표시 | ⬜ **미해결 이슈** |
| C22 | 세션 종료 후 홈 복귀 | 사용량 즉시 반영 | ✅ 08-12 |

## D. 세션 수명주기 (D1~D34)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| D1 | 세션 시작 | `onStartCommand … remaining=N` | ✅ 08-12 |
| D2 | 1분마다 차감 | `tick remaining` 감소 | ✅ 08-12 (60→59) |
| D3 | 대상 앱 밖으로 나감 | 차감 중지 | ✅ 08-12 |
| D4 | 대상 앱 복귀 | 차감 재개 | ⬜ |
| D5 | 화면 꺼짐 | 차감 중지 | ⬜ |
| D6 | 재생 일시정지 | 차감 중지 | ⬜ |
| D7 | PIP로 전환 | 정의된 동작 | ⬜ |
| D8 | 스플릿스크린 | 창 경계 기준 정상 | ⬜ |
| D9 | 한도 소진(0분) | 연장 모달 | ✅ 08-12 |
| D10 | 연장 모달 "나중에" | 세션 종료, 홈 복귀 | ⬜ |
| D11 | 앱 프로세스 강제 종료 후 재실행 | 세션 상태 복구 | ⬜ |
| D12 | 세션 중 기기 재부팅 | 정의된 동작 | ⬜ |
| D13 | 세션 종료 시 DB 기록 | `duration_seconds` 정확 | ✅ 08-12 |
| D14 | 종료 사유 기록 | completed/limit/sleep/manual | 🧪 6종 — `+ unknown`(사유 미상)·`app_restarted`(고아 정리)까지 구분 |
| D15 | 고아 세션(안 닫힌 행) 정리 | `closeOrphanedSession` | ✅ 08-12 (강제종료 후 55분으로 정확히 닫힘) |
| D16 | 열린 세션이 2개 이상 | 집계가 하나만 봄 | 🔍 근거 부족 — 고아 정리가 콜드스타트에 도므로 실사용에서 재현 안 됨 |
| D17 | 4시간 상한 | 비정상 장기 세션도 합계 폭주 없음 | 🧪 집계 3함수 전부 `14400` 일치 |
| D18 | 자정 넘어가는 세션 | 날짜 귀속 정확 | 🧪 `started_at` 기준 귀속 + `startedAtOverride` |
| D19 | 기기 시간 변경(수동) | 음수 시간 없음 | 🧪 전 계산 `Math.max(0, …)` |
| D20 | 타임존 변경 | 오늘 집계 정확 | ⬜ |
| D21 | 세션 중 다른 플랫폼 카드 탭 | 이전 세션 종료 후 새 세션 | ⬜ |
| D22 | 세션 중 앱 삭제/재설치 | 서버 허용량 유지 | ⬜ |
| D23 | 휴식 알림(20분 간격) | `nextBreakIn` 도달 시 | ⬜ |
| D24 | 남은시간 알림 | 설정 켠 경우만 | ⬜ |
| D25 | 포커스 세션 10분 만료 | FOCUS OFF 전환 | ✅ 08-12 |
| D26 | 포커스 세션 연장(광고) | +5분, FOCUS 재개 | ✅ 08-12 |
| D27 | 포커스 세션 연장(크레딧) | 크레딧 5 차감, +5분 | ⬜ |
| D28 | 프리미엄의 포커스 시간 설정 | 설정값 반영 | ⬜ |
| D29 | 무료 사용자 포커스 시간 | 항상 10분 고정 | ⬜ |
| D30 | 세션 중 네트워크 끊김 | 로컬 계속, 나중에 sync | ⬜ |
| D31 | 세션 기록 sync 실패 | 백로그로 남고 재시도 | ⬜ |
| D32 | 같은 세션 중복 sync | 서버에서 같은 행에 합류(PK 재사용) | ⬜ |
| D33 | 세션 시작 직후 즉시 종료(3초 미만) | 기록 안 함 | ❌ **🍎만 성립**(`feed/index.tsx:268`). 🤖 `overlay/index.tsx:397`은 가드 없음 — 발견 1 |
| D34 | 세션 중 수면 확정 | `sleep_detected`로 종료 | ⬜ |

## E. 알약 오버레이 (E1~E26)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| E1 | 대상 앱에서 알약 표시 | 남은시간 + FOCUS 배지 | ✅ 08-12 |
| E2 | 홈/런처에서 알약 숨김 | 숨김 | ✅ 08-12 |
| E3 | 🤖 틱톡에서 알약 표시 | 표시(UsageStats 폴백) | ✅ 08-12 |
| E4 | Pace 자기 화면에서 알약 | 숨김 | ⬜ |
| E5 | 광고 표시 중 알약 | 광고를 가리지 않음 | ⬜ |
| E6 | 알약 드래그 이동 | 위치 변경·저장 | ⬜ |
| E7 | 알약 탭 → P 메뉴 | Open App/Shorts/Favorite/Search | ✅ 08-12 |
| E8 | FOCUS 배지 탭 | 포커스 토글 | ⬜ |
| E9 | 남은시간 0일 때 알약 | 연장 유도 | ⬜ |
| E10 | 알약 텍스트 로케일 | ko/en 반영 | ⬜ |
| E11 | 프로세스 재시작 후 알약 복구 | 복구 | ⬜ |
| E12 | 다른 앱의 전체화면 위 | 알약이 위에 뜸 | ⬜ |
| E13 | 알림 그림자/상태바와 겹침 | 없음 | ⬜ |
| E14 | 알약이 시스템 UI를 막음 | 막지 않음 | ⬜ |
| E15 | 알약 표시 중 화면 회전 | 위치 보정 | ⬜ |
| E16 | 세션 종료 시 알약 제거 | 제거 | ⬜ |
| E17 | 강제 종료 후 알약 잔존 | 잔존 없음 | ⬜ |
| E18 | P 메뉴 바깥 탭 | 메뉴 닫힘 | ⬜ |
| E19 | P 메뉴에서 Open App | 대상 앱 실행 | ⬜ |
| E20 | P 메뉴 Favorite | 즐겨찾기 목록 | ⬜ |
| E21 | 휴식 안내 오버레이 | 표시 후 자동 닫힘 | ⬜ |
| E22 | "잠시 쉬어갈까요" 팝업 | 조건 맞을 때만 | ⬜ |
| E23 | 수면 확인 팝업 | 30초 무응답 시 종료 | ⬜ |
| E24 | 수면 팝업에 반응 | 세션 유지 + 시계 리셋 | ⬜ |
| E25 | 블랙아웃 오버레이 | 수면 확정 시에만 | ⬜ |
| E26 | 🔍 블랙아웃이 보고 있는데 뜸 | 뜨면 안 됨 | ✅ 08-12 (게이트 추가) |

## F. 자동넘김 (F1~F28)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| F1 | 🤖 유튜브 영상 끝 → 다음 | 자동 전환 | ⬜ |
| F2 | 🤖 유튜브 전환 시 로딩 | 없음 | ⬜ |
| F3 | 🤖 틱톡 영상 끝 → 다음 | 자동 전환 | ✅ 08-12 (7회) |
| F4 | 🤖 틱톡 진행바 없는 영상 | 20초 폴백으로 전환 | ✅ 08-12 (6회) |
| F5 | 🤖 제스처 접수/완료 | `accepted=true` + `onCompleted` | ✅ 08-12 |
| F6 | 🤖 제스처가 댓글창을 엶 | 열면 안 됨 | ✅ 08-12 (시작점 조정) |
| F7 | FOCUS OFF일 때 | 자동넘김 안 함 | ✅ 08-12 |
| F8 | 사용자가 직접 스와이프 직후 | 중복 넘김 없음 | ⬜ |
| F9 | 광고 재생 중 | 자동넘김 안 함 | ✅ 08-12 |
| F10 | 대상 앱 밖 | 자동넘김 안 함 | ❌→✅ **08-13 재검증에서 뚫려 있던 걸 발견·수정**(발견 20) |
| F11 | 검색 결과 재생 중 | 자동 진행 안 함 | ✅ 08-12 |
| F12 | HOT 이어서재생 | 목록 순서대로 | ✅ 08-12 |
| F13 | HOT 큐 소진 | 멈춤(무한 반복 없음) | ⬜ |
| F14 | 🍎 자동넘김 토글 노출 | 숨김/비활성 | ⬜ |
| F15 | 🤖 접근성 꺼짐 상태 | 자동넘김만 정지, 앱 정상 | ⬜ |
| F16 | 매우 짧은 영상(3초) | 과도한 연속 넘김 없음 | ⬜ |
| F17 | 매우 긴 영상(10분) | ~~중간에 안 끊김~~ → **90초에서 끊고 넘긴다** | ⚠️ **기대값이 뒤집혔다 — 아래 F29 참고** |
| F18 | 진행률 추정 이상치 | 3초~10분 밖은 버림 | ⬜ |
| F19 | 세로/가로 영상 혼재 | 정상 | ⬜ |
| F20 | 넘긴 편수 카운트 | 통계에 반영 | ⬜ |
| F21 | 스와이프 실패(제스처 취소) | 재시도 or 로그 | ⬜ |
| F22 | 연속 넘김 최소 간격 | `MANUAL_SWIPE_MIN_GAP_MS` 준수 | ⬜ |
| F23 | 🔍 SeekBar 여러 개 | 보이는 것만 선택 | ✅ 08-12 |
| F24 | 🔍 버퍼 바를 진행바로 오인 | 오인 안 함 | ✅ 08-12 |
| F25 | 틱톡 UI 변경 시 | 조용히 정지, 크래시 없음 | ⬜ |
| F26 | 손짓으로 넘김 | 동작 | ⬜ |
| F27 | BT 리모컨 볼륨키로 넘김 | 동작, 실제 볼륨 안 바뀜 | ⬜ |
| F28 | 볼륨키 길게 누름 | 중복 넘김 없음(디바운스) | ⬜ |
| F29 | **한 영상 90초 상한(over-stay)** | 진행바가 있어도 90초 넘으면 넘김 | 🧪 `MAX_SINGLE_VIDEO_MS = 90_000` |
| F30 | 광고 복귀 후 첫 영상 | 광고 보는 동안 흐른 시간을 시청으로 안 셈 | 🧪 복귀 시 시계 3종 리셋 |
| F31 | 🤖 실기기 자동넘김 실측 | 발사 → 제스처 → 실제 영상 전환 | ✅ **08-13 실측** — 아래 로그 |
| F32 | 진행바 없는 영상(광고 등) | 20초 폴백으로 넘김 | ✅ 08-13 실측(`no-progressbar elapsed=20036ms`) |

> **F17의 기대값이 뒤집힌 경위(2026-08-13).** 원래 "긴 영상은 중간에 안 끊김"이 기대였는데,
> 실기기에서 **117초짜리 틱톡 영상이 세션 내내 유지되는** 것이 관측됐다(아래 F31 로그). 자동넘김이
> `remain <= NEAR_END_LEAD_MS(2.5초)`에서만 발사되고 **길이 상한이 없었기** 때문이다.
> 사장님 `FOCUS 2m` 세션에서는 영상 하나로 세션이 끝난다 — "쇼트폼 총 시청시간 줄이기"라는
> 제품 목적과 정반대다. 원인은 **"숏폼 = 15~60초"라는 설계 전제가 깨진 것**(틱톡은 이제 최대
> 10분짜리를 추천 피드에 섞는다). → `MAX_SINGLE_VIDEO_MS = 90_000` 상한 도입.
> **F17을 "안 끊김"으로 되돌리면 이 문제가 그대로 재발한다.**

### F31 실측 로그 (2026-08-13, R3CN80S5GWW / 틱톡 `com.ss.android.ugc.trill`)

```
01:31:59  RANGE_INFO SeekBar(max=10000.0, frac=0.004)   ← 영상 시작
01:32:30  RANGE_INFO SeekBar(max=10000.0, frac=0.284)
01:33:03  RANGE_INFO SeekBar(max=10000.0, frac=0.564)   ← 리셋 없이 단조 증가 = 루프 아님
01:33:52  AUTO_NEXT reason=frac-predict frac=0.9825 est=118380ms remain=2071ms
01:33:52  dispatchGesture accepted=true
01:33:52  gesture onCompleted
01:33:56  VIDEO_ADVANCE reason=fingerprint count=9        ← 총 117초
01:34:12  AUTO_NEXT reason=no-progressbar elapsed=20036ms ← 광고엔 20초 폴백
```
스크린샷 2장으로 영상이 실제로 바뀐 것도 확인(Miss Piggsy → 틱톡 광고).
**"계속 같은 영상이 나온다"는 체감의 정체는 루프가 아니라 117초짜리 영상이었다** — 로그만 보면
자동넘김은 정상 동작 중이었고, 화면 캡처와 대조해서야 원인이 갈렸다(A2/A3 확인 방법의 그 교훈).

## G. HOT 쇼츠 (G1~G24)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| G1 | HOT 목록 표시 | 항목 로드 | ✅ 08-12 |
| G2 | 탭 구성 | All/Music/Gaming/Comedy/Entertainment | ✅ 08-12 |
| G3 | 항목 중복 | 없음 | ✅ 08-12 |
| G4 | 🔍 한 채널 쏠림 | 상한 적용 | ⬜ |
| G5 | 🔍 **KR인데 스페인어 콘텐츠** | 지역 스코핑 준수 | ❌ **08-12 관측** |
| G6 | 🔍 Music 탭이 all과 다른 목록 | 달라야 함 | ⬜ **과거 실패 이력** |
| G7 | 항목 탭 → 해당 영상 재생 | 정확히 그 영상 | ✅ 08-12 |
| G8 | 이어서재생 | 다음 항목으로 | ✅ 08-12 |
| G9 | 목록 로드 실패 | 빈 화면 대신 안내 | ⬜ |
| G10 | 네트워크 없음 | 캐시 or 안내 | ⬜ |
| G11 | ☁️ 스크래핑 차단 시 | 직전 정상 목록 서빙(stale-if-error) | ⬜ |
| G12 | ☁️ 쿼터 소진 시 | 검색/HOT 동반 정지 없음 | 🔍 **오늘 발생** |
| G13 | 목록 새로고침 | 시간대별 로테이션 | ⬜ |
| G14 | 같은 시간대 사용자 간 목록 | 완전 동일 → 개선 여지 | ⬜ |
| G15 | 썸네일 로드 실패 | 깨짐 없음 | ⬜ |
| G16 | 제목이 따옴표로 시작 | 파싱 깨짐 없음 | ⬜ |
| G17 | 제목 매우 김 | 말줄임 | ⬜ |
| G18 | 목록 스크롤 | 부드러움 | ⬜ |
| G19 | 목록 닫기(X) | 닫힘 | ⬜ |
| G20 | 가로 영상 혼입 | 쇼츠만 | ⬜ |
| G21 | 채널명 표시 | 있으면 표시 | ✅ 08-12 |
| G22 | 🔍 검색 결과의 channelTitle 빈 값 | 유튜브가 안 줌(oEmbed 필요) | 🔍 **알려진 제약** |
| G23 | 페이지네이션(더 보기) | 하드스톱 없음 | ⬜ |
| G24 | 목록 항목 수 | 충분(20+) | ✅ 08-12 |

## H. 검색 (H1~H22)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| H1 | 검색 패널 열기 | 입력창+버튼+프리셋 | ✅ 08-12 |
| H2 | 키워드 직접 입력 | 결과 표시 | ✅ 08-12 ("cat") |
| H3 | 검색 버튼 탭 | 검색 실행 | ✅ 08-12 |
| H4 | IME 검색 키 | 검색 실행 | ⬜ |
| H5 | 프리셋 칩 탭 | 해당 주제 결과 | ⬜ |
| H6 | 결과 항목 탭 | **고른 것만** 재생 | ✅ 08-12 |
| H7 | 결과 재생 후 자동 진행 | **없어야 함** | ✅ 08-12 |
| H8 | 빈 검색어로 검색 | 무시 or 안내 | ⬜ |
| H9 | 공백만 입력 | 무시 | ⬜ |
| H10 | 매우 긴 검색어 | 크래시 없음 | ⬜ |
| H11 | 특수문자/이모지 | 정상 인코딩 | ⬜ |
| H12 | 한글 검색어 | 정상 | ⬜ |
| H13 | 로케일 접미사 | ko→"쇼츠" / ja→"ショート" / else "shorts" | ⬜ |
| H14 | 결과 없음 | "결과가 없어요" | ✅ 08-12 |
| H15 | ☁️ 쿼터 소진 시 | 결과 없음이 캐시되지 않음 | ⬜ |
| H16 | ☁️ 차단 시 | 직전 정상 결과 서빙 | ⬜ |
| H17 | 검색 중 네트워크 끊김 | 안내, 크래시 없음 | ⬜ |
| H18 | 검색 패널에서 키보드 | 입력창 가림 없음 | ⬜ |
| H19 | 검색 패널 닫기 | 닫힘 | ⬜ |
| H20 | 연속 검색 | 이전 결과 잔상 없음 | ⬜ |
| H21 | 국가별 검색 결과 | gl/hl 반영 | ⬜ |
| H22 | 검색 비용 | 정상 경로는 쿼터 0 | 🔍 **오늘 위반** |

## I. 수면감지 (I1~I20)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| I1 | 무입력 10분 미만 | 수면 판정 안 함 | ⬜ |
| I2 | 무입력 10분 → SUSPECT | 단계 진입 | ✅ 08-12 |
| I3 | SUSPECT 5분 → PROMPTED | 팝업 | ✅ 08-12 |
| I4 | 팝업 30초 무응답 | 세션 종료 | ✅ 08-12 |
| I5 | 팝업에 반응 | AWAKE 복귀 | ⬜ |
| I6 | 밤 시간대(22~9시) 밖 | 확정 안 함 | ✅ 08-12 |
| I7 | 보조 신호 없음(밝음·세움·미충전) | 확정 안 함 | ⬜ |
| I8 | **손으로 넘기며 보는 중** | 수면 판정 안 함 | ✅ 08-12 (게이트) |
| I9 | 🔍 관측 불가 앱에서 | 확정 **보류** | ✅ 08-12 |
| I10 | 접근성 꺼짐 | 확정 보류 | ⬜ |
| I11 | FOCUS OFF에서 | 수면감지 정상 동작 | ⬜ |
| I12 | 프롬프트가 감시앱 창을 가림 | 교착 없이 30초 타임아웃 | ⬜ |
| I13 | 프로세스 재시작 | timedOut 플래그 유지 | ⬜ |
| I14 | 🍎 인앱 블랙아웃 | 보는 중엔 안 덮음 | ⬜ |
| I15 | BT 이어폰 분리 | 단축 임계 적용 | ⬜ |
| I16 | 충전 중 | 보조 신호로 인정 | ⬜ |
| I17 | 폰을 평평히 둠 | 보조 신호 | ⬜ |
| I18 | 조도 센서 없는 기기 | 기능 죽지 않음 | ⬜ |
| I19 | 수면 종료 후 기록 | `sleep_detected` + 잠든 시각 | ⬜ |
| I20 | 홈 인사이트 "…에 잠드셨습니다" | 표시 | ⬜ |

## J. 광고 (J1~J26)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| J1 | 배너 광고 로드 | 표시 | ✅ 08-12 |
| J2 | 보상형 광고 로드 | 표시 | ✅ 08-12 |
| J3 | 광고 끝까지 재생 | 중간 종료 없음 | ✅ 08-12 |
| J4 | 🔍 광고가 1~2초 만에 사라짐 | 재발 없음 | ✅ 08-12 |
| J5 | 보상 지급 | `reward earned` | ✅ 08-12 |
| J6 | 보상 후 +5분 | FOCUS 재개 | ✅ 08-12 |
| J7 | 하루 3회 한도 표시 | `오늘 0/3` | ✅ 08-12 |
| J8 | 4회째 시도 | 차단 | 🧪 `FocusSessionExtendModal:60,64` + 서버 max 병합(재설치 우회 차단) |
| J9 | 광고 도중 앱 이탈 | 보상 미지급, 상태 정상 | ⬜ |
| J10 | 광고 로드 실패 | 안내, 크레딧 대안 제시 | ⬜ |
| J11 | 네트워크 없음 | 광고 버튼 비활성/안내 | ⬜ |
| J12 | 🤖 비공개 테스트 트랙 | **테스트 광고** | ✅ 08-12 |
| J13 | 🤖 프로덕션 트랙 | **실광고** | ⬜ **설정 필요** |
| J14 | 🍎 프로덕션 | 실광고 | ⬜ |
| J15 | 개발 빌드 | 항상 테스트 광고 | ⬜ |
| J16 | 광고 동의(UMP) 흐름 | EEA에서 표시 | ⬜ |
| J17 | 광고 개인정보 옵션 | 설정에서 재호출 가능 | ⬜ |
| J18 | 광고 중 화면 회전 | 크래시 없음 | ⬜ |
| J19 | 광고 중 전화 수신 | 복귀 후 정상 | ⬜ |
| J20 | 광고 중 세션 타이머 | 정지(차감 안 함) | ⬜ |
| J21 | 광고 중 자동넘김 | 정지 | ✅ 08-12 |
| J22 | 광고 닫은 뒤 복귀 | 원래 앱으로 | ✅ 08-12 |
| J23 | 배너 높이만큼 레이아웃 | 콘텐츠 안 가림 | ⬜ |
| J24 | 프리미엄 사용자 | 광고 없음 | ⬜ |
| J25 | 광고 3회 소진 후 크레딧 | 크레딧으로 연장 가능 | ⬜ |
| J26 | 서버 허용량과 로컬 불일치 | 단조 병합 | ⬜ |

## K. 구독 · 결제 (K1~K18)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| K1 | 페이월 표시 | 상품·가격 | ⬜ |
| K2 | 구매 성공 | 프리미엄 즉시 반영 | ⬜ |
| K3 | 구매 취소 | 상태 변화 없음 | ⬜ |
| K4 | 구매 복원 | 프리미엄 복구 | ⬜ |
| K5 | 결제 실패 | 안내 | ⬜ |
| K6 | 구독 만료 | 무료로 복귀 | ⬜ |
| K7 | 환불 | 무료로 복귀 | ⬜ |
| K8 | 개발 모드 | **과금 경로 전부 차단** | ⬜ |
| K9 | 로그인 없이 구독 시도 | 로그인 유도 | ⬜ |
| K10 | 웹훅 수신 | 서버 상태 갱신 | ⬜ |
| K11 | 프리미엄 전용 설정 노출 | 무료에겐 잠금 | ⬜ |
| K12 | 프리미엄 포커스 시간 조절 | 반영 | ⬜ |
| K13 | 프리미엄 수면 임계 조절 | 반영 | ⬜ |
| K14 | 무료 보관 한도(3개) | 초과 시 안내 | ⬜ |
| K15 | 구독 관리 링크 | 스토어로 이동 | ⬜ |
| K16 | 가격 로케일 | 통화 표기 정확 | ⬜ |
| K17 | 구독 상태 네이티브 전달 | `setIsPremium` | ⬜ |
| K18 | 구독 중 재설치 | 복원 가능 | ⬜ |

## L. 크레딧 · 출석 · 휴식(Flip) (L1~L18)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| L1 | 출석 크레딧 +5 | 하루 1회 | ✅ 08-12 (재실행해도 재지급 없음) |
| L2 | 연속 출석 | 카운트 증가 | ⬜ |
| L3 | 하루 건너뜀 | 연속 리셋 | ⬜ |
| L4 | 크레딧으로 5분 연장 | 5 차감 | ⬜ |
| L5 | 크레딧 부족 | 버튼 비활성 | ⬜ |
| L6 | 휴식 10분당 크레딧 5 | 적립 | ⬜ |
| L7 | 폰 뒤집기 감지 | 휴식 시작 | ⬜ |
| L8 | 뒤집기 해제 | 휴식 종료 | ⬜ |
| L9 | 휴식 중 알림 | 방해 없음 | ⬜ |
| L10 | 휴식 시간 표시 | 홈/분석 | ⬜ |
| L11 | 크레딧 총합 표시 | 적립+보너스 | ✅ 08-12 |
| L12 | 크레딧 자정 리셋 | 오늘 적립분만 | ⬜ |
| L13 | 출석 보너스 이월 | 유지 | ⬜ |
| L14 | 센서 없는 기기 | Flip 비활성, 크래시 없음 | ⬜ |
| L15 | 주머니 속 오탐 | 휴식 오적립 없음 | ⬜ |
| L16 | 크레딧 서버 동기화 | 일치 | ⬜ |
| L17 | 오프라인 크레딧 사용 | 나중에 동기화 | ⬜ |
| L18 | 크레딧 음수 방지 | 0 미만 없음 | ⬜ |

## M. 통계 · 분석 (M1~M24)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| M1 | 오늘 사용시간 | 정확 | ✅ 08-12 |
| M2 | 이번 주 합계 | 월요일 기준 | ✅ 08-12 |
| M3 | 일일 평균 | 경과일수로 나눔 | ✅ 08-12 |
| M4 | 지난주 대비 | 같은 요일 구간 비교 | ⬜ |
| M5 | 지난주 기록 없음 | "아직 없어요" | ✅ 08-12 |
| M6 | 집중도 점수 | 한도 이내 날 비율 | ✅ 08-12 |
| M7 | 연속 안전 시청 | 계산 정확 | ✅ 08-12 |
| M8 | 최장 연속 시청 | 오늘 최대 세션 | ✅ 08-12 |
| M9 | 요일별 그래프 | 목표선 표시 | ✅ 08-12 |
| M10 | 가장 건강한 날 | 계산 정확 | ✅ 08-12 |
| M11 | **앱별 사용시간(신규)** | 유튜브/틱톡 분리 표시 | ✅ **08-13 실기기** — 갈아탄 뒤 "플랫폼 분석"에 두 앱 다 표시(발견 12 수정 후 최초 성공) |
| M12 | 앱이 하나뿐일 때 | 섹션 숨김 | ✅ 08-12 |
| M13 | 안 본 앱 | 목록에 없음 | ⬜ |
| M14 | 진행 중 세션 포함 | 합계와 일치 | ⬜ |
| M15 | 열린 세션 2개 이상 | 집계 누락 | 🔍 재현 안 됨(D16 참고) |
| M16 | 30초 세션 | 0분으로 사라지지 않음(초 보존) | ⬜ |
| M17 | 유튜브 앱 켠 시간 vs Pace 기록 | 나란히 표시 | ✅ 08-12 |
| M18 | 🔍 "유튜브 앱 켠 시간" 문구 | 틱톡 추가 후 이름 불일치 | 🔍 **정리 필요** |
| M19 | 🍎 사용정보 미지원 | 섹션 미표시 | ⬜ |
| M20 | 데이터 없는 신규 사용자 | 빈 상태 문구 | ⬜ |
| M21 | 통계 새로고침 | 탭 진입마다 | ✅ 08-12 |
| M22 | 서버 통계와 로컬 | 정합 | ⬜ |
| M23 | 자정 경계 | 어제/오늘 분리 | ⬜ |
| M24 | 주간 그래프 막대 탭 | 상세 표시 | ⬜ |

## N. 설정 (N1~N26)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| N1 | 일일 한도 변경 | 즉시 반영 | ⬜ |
| N2 | 휴식 간격 변경 | 반영 | ⬜ |
| N3 | 수면 타이머 변경 | 반영 | ⬜ |
| N4 | 포커스 세션 시간 | 프리미엄만 | ⬜ |
| N5 | 자동넘김 토글 | 반영 | ⬜ |
| N6 | 즐겨찾기 자동 이어서 | 반영 | ⬜ |
| N7 | 하드블록 모드 | 🤖만 | ⬜ |
| N8 | 남은시간 알림 | 반영 | ⬜ |
| N9 | 언어 변경 | 즉시 반영 | ⬜ |
| N10 | 알림 설정 | 반영 | ⬜ |
| N11 | 광고 개인정보 옵션 | 재호출 | ⬜ |
| N12 | 개인정보 처리방침 | 열림 | ⬜ |
| N13 | 도움말 센터 | 열림 | ⬜ |
| N14 | 앱 평가 | 스토어 리뷰 | ⬜ |
| N15 | 평가 불가 환경 | 안내 | ⬜ |
| N16 | 구독 관리 | 스토어 | ⬜ |
| N17 | 로그아웃 | 동작 | ⬜ |
| N18 | 계정 삭제 | 확인 후 삭제 | ⬜ |
| N19 | 설정값 서버 동기화 | 반영 | ⬜ |
| N20 | 오프라인 설정 변경 | 로컬 저장 후 sync | ⬜ |
| N21 | 설정 초기화 | 기본값 | ⬜ |
| N22 | 설정 화면 스크롤 | 광고 겹침 없음 | ⬜ |
| N23 | 접근성 서비스 바로가기 | 시스템 설정 이동 | ⬜ |
| N24 | 신고/차단 정책 | 표시(스토어 심사 요건) | ⬜ |
| N25 | 버전 정보 표시 | 정확 | ⬜ |
| N26 | 설정 변경 후 세션 중 반영 | 정의된 시점에 반영 | ⬜ |

## O. 알림 (O1~O12)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| O1 | 포그라운드 서비스 알림 | 상시 표시 | ⬜ |
| O2 | 휴식 알림 | 간격마다 | ⬜ |
| O3 | 남은시간 알림 | 임계에서 | ⬜ |
| O4 | 인사이트 알림 | 데이터 있을 때 | ⬜ |
| O5 | 알림 탭 | 앱 열림 | ⬜ |
| O6 | 알림 권한 없음 | 기능 정상 | ⬜ |
| O7 | 방해금지 모드 | OS 정책 준수 | ⬜ |
| O8 | 알림 채널 설정 | 사용자 조절 가능 | ⬜ |
| O9 | 알림 중복 | 없음 | ⬜ |
| O10 | 세션 종료 후 알림 | 정리됨 | ⬜ |
| O11 | 🍎 Live Activity | 표시/갱신 | ⬜ |
| O12 | 🍎 Live Activity 종료 | 세션 종료 시 제거 | ⬜ |

## P. 동기화 · 오프라인 · 서버 (P1~P18)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| P1 | 세션 기록 업로드 | 성공 | ⬜ |
| P2 | 오프라인 세션 | 로컬 저장 | ⬜ |
| P3 | 온라인 복귀 시 백로그 | 일괄 업로드 | ⬜ |
| P4 | 업로드 중복 | 같은 행에 합류 | ⬜ |
| P5 | 서버 500 | 재시도, 데이터 손실 없음 | ⬜ |
| P6 | 서버 타임아웃 | 앱 멈춤 없음 | ⬜ |
| P7 | 허용량 조회 401 | 로컬로 동작 | ⬜ |
| P8 | 허용량 단조 병합 | 0 전송이 3을 지우지 않음 | ⬜ |
| P9 | 허용량 7일 보존 | 스케줄러 동작 | ⬜ |
| P10 | 설정 동기화 | 양방향 | ⬜ |
| P11 | 통계 조회 | 정확 | ⬜ |
| P12 | ☁️ HOT 갱신 스케줄 | 동작 | ⬜ |
| P13 | ☁️ 쿼터 관리 | 소진 방지 | 🔍 **오늘 소진** |
| P14 | ☁️ CDN 캐시 | 국가별 분기 유지 | ⬜ |
| P15 | ☁️ stale-if-error | 직전 정상 응답 서빙 | ⬜ |
| P16 | ☁️ 빈 결과 캐시 | 캐시되지 않음 | ⬜ |
| P17 | DB 마이그레이션 | Flyway 정상 | ⬜ |
| P18 | 백엔드 배포 | `railway up` (GitHub 자동배포 아님) | ⬜ |

## Q. 국제화 · 접근성 · 정책 (Q1~Q14)

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| Q1 | 한국어 전체 문구 | 누락 없음 | ⬜ |
| Q2 | 영어 전체 문구 | 누락 없음 | ⬜ |
| Q3 | 미지원 로케일 | 영어 폴백 | ⬜ |
| Q4 | 숫자·날짜 형식 | 로케일 반영 | ⬜ |
| Q5 | 긴 번역문 | 레이아웃 안 깨짐 | ⬜ |
| Q6 | 스크린리더 | 주요 버튼 라벨 | ⬜ |
| Q7 | 색 대비 | 최소 기준 충족 | ⬜ |
| Q8 | 신고 기능 | 동작(스토어 요건) | ⬜ |
| Q9 | 차단 기능 | 동작 | ⬜ |
| Q10 | 커뮤니티 정책 문서 | 접근 가능 | ⬜ |
| Q11 | 개인정보 수집 고지 | 정확 | ⬜ |
| Q12 | 🤖 접근성 서비스 사용 고지 | Play 정책 준수 | ⬜ |
| Q13 | 데이터 삭제 경로 | 제공 | ⬜ |
| Q14 | 연령 등급 관련 콘텐츠 | 정책 부합 | ⬜ |

---

# 확장분 (2026-08-13) — 코드베이스에 있는데 A~Q가 한 줄도 안 다루던 영역

> **왜 확장했나.** A~Q는 "사용자가 보는 화면"을 따라 만들어져서, 화면이 없는 층이 통째로 빠져
> 있었다. 실제로 `src/` 126개 파일과 네이티브 모듈 6개를 전수 대조하니 **OTA·강제 업데이트 게이트,
> 보관함, 입력 수단(손짓/스냅/볼륨키/플립), iOS Pace Feed, 데이터 무결성, 릴리즈 게이트**가
> 검사 대상에 아예 없었다. 그중 `services/updates/index.ts`는 **주석에만 "🔴 OTA가 영구히 죽는
> 경로가 있었다"는 사고 기록이 세 건** 남아 있는데도 검사 항목이 0개였다 — 사고가 난 곳에
> 검사가 없는 상태가 이 문서가 막으려던 바로 그것이다.

## R. 앱 업데이트 — OTA · 강제 업데이트 게이트 (R1~R22)

근거: `src/services/updates/index.ts`, `src/services/appVersionGate.ts`, `api/app-config.ts`

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| R1 | 콜드스타트 + 포그라운드 복귀마다 체크 | `_layout.tsx`가 fire-and-forget 호출 | 🧪 `updates/index.ts` 호출 계약 |
| R2 | dev 클라이언트 | `skipped-dev`, 에러 아님 | 🧪 `__DEV__`/`isEnabled`/`ERR_NOT_AVAILABLE_IN_DEV_CLIENT` 3중 |
| R3 | 네트워크 없음 | `check-failed`, 앱 정상 사용 | 🧪 |
| R4 | 연속 실패 백오프 | 1→2→4→8분에서 멈춤 | 🧪 `MIN_CHECK_INTERVAL_MS`×2^n, `MAX_=8분` |
| R5 | 세션 중 업데이트 도착 | 다운로드는 하고 **리로드만** 보류 | 🧪 `isUserMidSession()` |
| R6 | 🍎 피드 재생 중 | 같이 보류(`usePlayerStore.isPlaying`) | 🧪 |
| R7 | 백그라운드에서 도착 | `skipped-background` — 몰래 리로드 금지 | 🧪 `AppState==='active'`만 통과 |
| R8 | 보류분의 다음 반영 | **재다운로드 없이** 바로 리로드 | 🧪 `hasPendingDownloadedUpdate` 분기 |
| R9 | 🔴 `reloadAsync` 예외 | `checkInFlight`가 풀려 OTA가 영구히 죽지 않음 | 🧪 pending 경로 catch (2026-08-06 수정분) |
| R10 | 🔴 롤백 지시 수신 | `isRollBackToEmbedded` → 내장 번들 복귀 | 🧪 check/fetch 양쪽 다 처리 |
| R11 | 같은 번들 재수신 | `isNew=false`면 리로드 안 함(세션 보존) | 🧪 |
| R12 | 스로틀 | 60초 내 재호출은 네트워크 왕복 없음 | 🧪 |
| R13 | 진단 정보 | 채널/런타임버전/updateId 노출 | 🧪 `getUpdateDiagnostics` |
| R14 | 네이티브 로그 조회 | 실패해도 빈 배열, 부팅 지연 없음 | 🧪 `getUpdateNativeLog` |
| R15 | 게이트 킬스위치 | 서버 `enabled!==true` → **통과** | 🧪 `appVersionGate.ts` |
| R16 | 게이트 payload 오염 | 문자열/실수/음수 `minBuildNumber` → 통과 | 🧪 `Number.isInteger`+`>=0` |
| R17 | `storeUrl` 없음 | 통과(빠져나갈 길 없는 차단 금지) | 🧪 |
| R18 | 게이트 네트워크 실패·타임아웃 | 4초 후 통과 | 🧪 `FETCH_TIMEOUT_MS=4000` + AbortController |
| R19 | 최신 빌드 | `currentBuild >= minBuild` → 통과 | 🧪 |
| R20 | 낡은 빌드 | 차단 + 스토어 이동 | ⬜ **실기기 필요**(차단은 유일하게 되돌릴 수 없는 UX) |
| R21 | 게이트가 부팅을 지연 | 4초 상한 안에서만 | ⬜ 체감 측정 필요 |
| R22 | 게이트 엔드포인트 주소 | Railway 아님 — **Vercel**(`YOUTUBE_PROXY_URL`) | 🧪 2026-08-08에 실제로 밟은 함정 |

> ⚠️ **R20을 대충 넘기면 안 되는 이유.** 이 앱에서 잘못 켜지면 전 사용자를 쫓아내는 기능은 이거
> 하나뿐이다. 나머지는 전부 fail-open이라 코드로 확인이 끝나지만(R15~R19 🧪), **차단이 실제로
> 일어나는 경로만은 사람이 눈으로 봐야 한다.** 확인할 것: 차단 화면에서 스토어 버튼이 실제로
> 열리는가, 뒤로가기/앱 재실행으로 우회되지 않는가, 업데이트 후 차단이 풀리는가.

## S. 보관함 — Favorite / Capture (S1~S16)

근거: `src/database/repositories/savedVideosRepository.ts`, `src/components/overlays/SavedVideoListOverlay.tsx`

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| S1 | P 메뉴에서 Favorite 저장 | 행 생성 | ⬜ |
| S2 | 썸네일 자동 구성 | `i.ytimg.com/vi/{id}/hqdefault.jpg` | 🧪 `youtubeThumbnailUrl` |
| S3 | videoId 추출 실패 | 썸네일 null, **저장은 성공** | 🧪 |
| S4 | 같은 영상 중복 저장 | 막힘 | 🧪 `isVideoSaved` — ⚠️ 호출부 연결은 미확인 |
| S5 | videoId null인 건의 중복 | **일부러 막지 않음**("일단 저장되게"가 우선) | 🧪 |
| S6 | 예전 `capture` 건 | `favorite` 조회에 병합돼 보임 | 🧪 조회 시점 병합(마이그레이션 없음) |
| S7 | 목록 정렬 | `added_at DESC` | 🧪 |
| S8 | 제목 빈 행의 사후 보정 | oEmbed로 제목·채널 채움 | 🧪 `updateSavedVideoMeta` |
| S9 | 삭제 | 목록에서 사라짐 | ⬜ |
| S10 | **무료 보관 한도(3개)** | K14가 약속하는 동작 | ❌ **구현 없음 — 아래 발견 3 참고** |
| S11 | 항목 탭 → 재생 | 그 영상만 | ⬜ |
| S12 | 썸네일 로드 실패 | 깨짐 없음 | ⬜ |
| S13 | 저장 후 오버레이 복귀 | 보던 자리 유지 | ⬜ |
| S14 | 사용자 격리 | 다른 `user_id` 건이 안 보임 | 🧪 모든 쿼리에 `user_id` 조건 |
| S15 | 로그아웃/계정 전환 | 목록이 새 계정 것으로 | ⬜ |
| S16 | 서버 동기화 | **현재 로컬 전용** — 재설치 시 유실 | 🧪 `saved_videos`에 `synced` 컬럼 없음 |

> S16은 버그가 아니라 **미정의**다. 세션/설정/허용량은 전부 서버에 남기는데 보관함만 안 남긴다 —
> 사용자가 "재설치했더니 즐겨찾기가 없어졌다"고 할 때 답이 정해져 있어야 한다.

## T. 입력 수단 — 손짓 · 스냅 · 플립 · 볼륨키 · BT 리모컨 (T1~T18)

근거: 네이티브 모듈 5개(`pace-gesture`/`pace-flip`/`pace-volumekey`/`pace-sleep`/`pace-live-activity`),
`PaceHandWaveDetector.kt`, `PaceSnapDetector.kt`, `bluetoothService.android.ts`, `hooks/useVolumeNext*.ts`

> A~Q는 이 다섯 모듈을 F26~F28(3줄)로만 다뤘다. **감지기 하나당 오탐/미탐 두 방향이 다 있는데**
> 검사가 "동작함" 한 줄뿐이었다 — 수면감지 오탐(K8)이 터진 것과 정확히 같은 구조다.

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| T1 | 손짓으로 넘김 | 다음 영상 | ⬜ |
| T2 | 카메라 권한 거부 | 손짓만 죽고 나머지 정상 | ⬜ |
| T3 | **손짓 오탐** | 지나가는 사람·조명 변화·얼굴 움직임에 안 넘어감 | ⬜ ⚠️ |
| T4 | 스냅(손가락 튕김) 감지 | 다음 영상 | ⬜ |
| T5 | **스냅 오탐** | 영상 자체 소리·주변 대화에 안 넘어감 | ⬜ ⚠️ |
| T6 | 손짓+스냅 동시 | 한 번만 넘어감 | ⬜ |
| T7 | 볼륨키 넘김 | 넘어가되 **실제 볼륨이 안 바뀜** | ⬜ |
| T8 | 볼륨키 길게 누름 | 디바운스, 연속 넘김 없음 | ⬜ |
| T9 | 세션 **밖**에서 볼륨키 | 정상적인 볼륨 조절로 동작 | ⬜ ⚠️ 가로채기가 안 풀리면 기기가 고장 난 것처럼 보인다 |
| T10 | BT 리모컨 온보딩 시트 | 페어링 안내 | ⬜ |
| T11 | BT 리모컨 키 매핑 | 다음/이전 | ⬜ |
| T12 | BT 연결 끊김 | 조용히 비활성, 크래시 없음 | ⬜ |
| T13 | BT 프리미엄 게이팅 | `setIsPremium`이 네이티브까지 전달 | 🧪 `_layout.tsx:68` — 변경마다 push |
| T14 | 폰 뒤집기 → 휴식 | 시작/종료 | ⬜ |
| T15 | 센서 없는 기기 | Flip 비활성, 크래시 없음 | ⬜ |
| T16 | **주머니 오탐** | 휴식 크레딧 오적립 없음 | ⬜ ⚠️ |
| T17 | Flip 크레딧 자정 리셋 | 적립·소비분이 **같은 날짜 규칙**으로 리셋 | 🧪 `useFlipStore` `settle()` — `sameDay`가 둘 다 지배 |
| T18 | 각 네이티브 모듈 미링크 | 앱이 안 죽음 | 🧪 전 모듈 방어적 `require`+catch |

## U. 🍎 iOS Pace Feed (U1~U16)

근거: `src/app/feed/index.tsx`, `playlistRepository.ts`, `services/api/pexels.ts`, `constants/curatedVideos.ts`

> A~Q에 **이 화면 전용 항목이 0개**다. iOS 사용자가 실제로 시간을 쓰는 곳인데(안드의 "대상 앱"에
> 해당) `pace_videos`/`playlist_sessions` 두 테이블과 자체 플레이어가 통째로 검사 밖이었다.

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| U1 | 피드 진입 | 영상 재생 | ⬜ |
| U2 | 원격 로드 실패 | 로컬 캐시(`pace_videos`)로 표시 | 🧪 write-through 캐시 존재 |
| U3 | 캐시도 없음 | 큐레이션 폴백 | ⬜ |
| U4 | 세그먼트 기록 | `startSession`/`endSession` 짝 | 🧪 |
| U5 | **3초 미만 세그먼트** | 기록 안 함 | 🧪 `feed/index.tsx:268` — **iOS만 있음, 발견 1 참고** |
| U6 | 자정 넘는 세그먼트 | 시작 시각 기준 귀속 | 🧪 `startedAtOverride`(2026-07-27 수정분) |
| U7 | 백그라운드 전환 | flush | ⬜ |
| U8 | 수면감지 | flush + `sleep_detected`, `started_at ≤ ended_at` | 🧪 역전 방지 수정 확인 |
| U9 | 시청 편수 | `videos_watched` 누적 | ⬜ |
| U10 | Pexels 실패/쿼터 | 안내, 크래시 없음 | ⬜ |
| U11 | 테이블 분리 | `playlist_sessions`와 `viewing_sessions`가 안 섞임 | 🧪 |
| U12 | ~~`platform_app` 하드코딩~~ | 실제 `platform`을 기록 | ✅ **해결됨**(`e72050a`) — `feed/index.tsx:282`가 `startSession(uid, platform, …)` |
| U13 | 오프라인 재방문 | 즉시 표시 | ⬜ |
| U14 | 피드 시청이 분석 탭에 잡히는 범위 | 정의대로 | ⬜ **정의 필요** |
| U15 | `playlist_sessions` 업로드 | 백로그 처리 | ⬜ `getUnsyncedPlaylistSessions`는 있으나 소비처 미확인 |
| U16 | Live Activity 연동 | 세션과 수명 일치 | ⬜ `pace-live-activity`에 JS 바인딩 파일 없음(Swift만) |

> **U12는 이 문서를 쓰는 동안 고쳐졌다.** Windows 세션이 "🍎 틱톡이 들어오면 여기부터 고쳐야
> 한다"고 짚어 둔 지점을 Mac 세션이 iOS TikTok을 구현하면서 그대로 고쳤다(`e72050a` 커밋 메시지가
> "Windows 세션이 미리 지적한 지점"이라고 명시). **표를 먼저 갱신하고 기능을 붙인다**는
> `QA_MATRIX.md`의 규칙이 실제로 작동한 첫 사례다 — 남겨 둔다.

### U-2. 🍎 iOS TikTok — 데스크톱 UA 위장의 부작용 (U17~U28, 2026-08-13 신설)

> ⚠️ **케이스 중복 금지.** iOS TikTok의 기본 동작(자동넘김·상단바 유지·홈 카드·렌더러 크래시
> 복구·P메뉴 숨김·통계 정확성)은 **`QA_MATRIX.md` 3-3의 C1~C9가 주인**이다. Mac 세션이
> 시뮬레이터로 이미 검증했고 실기기 필요 항목까지 표시해 뒀다 — **여기서 다시 적지 않는다.**
>
> 이 표는 3-3이 **다루지 않는 것**만 담는다: 데스크톱 UA로 위장했기 때문에 생기는 2차 효과다.
> `TikTokShortsPlayer.ios.tsx:322`가 UA를 **맥 크롬**으로 고정하는데, 이건 "자동넘김이 되게
> 하려고" 고른 값이지 "이 화면에 맞아서" 고른 값이 아니다. 그 간극에서 나오는 것들.

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| U17 | 데스크톱 레이아웃 잔재 | 사이드바·헤더가 안 보이고 영상이 화면을 꽉 참 | ⬜ ⚠️ `QA_MATRIX` 1부가 "알려진 부작용"으로 적어 둔 미해결분 |
| U18 | 오디오/비디오 동기화 | 전환 시 소리가 화면보다 먼저 나오지 않음 | ⬜ ⚠️ 같은 기록 |
| U19 | **데이터 사용량** | 데스크톱 UA로 고화질을 받아 요금이 튀지 않음 | ⬜ ⚠️ **실사용자 요금 — 아무도 안 본 축** |
| U20 | **iOS 저전력 모드** | 자동재생 제한으로 `ended` 기반 자동넘김이 죽지 않음 | ⬜ ⚠️ Y5와 같은 뿌리 |
| U21 | 화면 끄기 / 백그라운드 | 재생 정지 + 세그먼트 flush | ⬜ |
| U22 | 로그인벽·캡차 출현 | 교착 없이 빠져나갈 수 있음 | ⬜ PoC엔 없었으나 UA가 바뀌면 달라질 수 있음 |
| U23 | WebView 쿠키 지속성 | 세션이 남는지/지워지는지 **정의돼 있음** | ⬜ **정의 필요** |
| U24 | 전체화면 강제 차단 | `requestFullscreen`/`webkitEnterFullscreen` no-op 유지 | 🧪 `INJECTED_JS_BEFORE_LOAD` |
| U25 | `ended` 폴백 | `video.loop` 되돌림 대비 재생위치 폴링이 돎 | 🧪 폴링 백업 존재 |
| U26 | 강제 전환 루프 부재 | 8초 강제 재시도가 되살아나지 않음 | 🧪 제거 확인(그 루프가 "중간에 넘어감" 버그였다) |
| U27 | 세로 영상 안전영역 | 노치·홈인디케이터에 안 가림 | ⬜ |
| U28 | **약관 리스크** | 데스크톱 UA 위장이 심사·법무상 문제없는지 판단 | ⬜ ⚠️ **판단 필요** — 코드가 아니라 결정 사항 |

> **U19·U20·U28이 이 표를 만든 이유다.** 나머지는 고치면 되는 버그지만 이 셋은 성격이 다르다 —
> U19는 사용자가 **돈**을 더 내게 되고(우리는 모른다), U20은 배터리 상태에 따라 기능이 조용히
> 죽고(사용자는 "가끔 안 넘어간다"로만 겪는다), U28은 코드로 해결되지 않는다. 셋 다 "자동넘김이
> 된다"는 성공에 가려 아무도 안 본 축이다.

## V. 데이터 무결성 · 초기화 · 이관 (V1~V18)

근거: `src/database/schema.ts`, `sessionsRepository.ts`, `services/auth/deviceId.ts`

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| V1 | 스키마 재실행 | `CREATE IF NOT EXISTS` — 멱등 | 🧪 |
| V2 | 컬럼 추가 마이그레이션 | 기존 설치가 새 컬럼을 받음 | 🧪 `PRAGMA user_version` + `ensureColumn`(2026-07-27 수정분) |
| V2b | **마이그레이션 사다리 유지** | 컬럼 추가 시 `CURRENT_DB_VERSION`도 같이 오름 | ⬜ ⚠️ **발견 5** — 지금 `=1`(칸 2개)뿐이고 강제 수단이 없다 |
| V2c | 신규 테이블 추가 | `CREATE IF NOT EXISTS`로 기존 설치에도 생성 | 🧪 `saved_videos`/`pace_videos`/`playlist_sessions` |
| V2d | 마이그레이션 실패 | 삼키고 DB는 정상 반환(앱이 안 죽음) | 🧪 |
| V2e | 동시 `getDb()` | in-flight Promise 캐싱으로 커넥션 1개 | 🧪 실기기 버그 수정분 |
| V3 | 설정 초기화 | 실제로 기록이 지워짐 | 🧪 `clearUserHistory`(2026-07-20 수정분) |
| V4 | 계정 삭제 | 서버 + 로컬 | ⬜ |
| V5 | 로그아웃 후 로컬 데이터 | 정의대로 | ⬜ |
| V6 | 게스트 → 로그인 승계 | 기록 유지 | ⬜ |
| V7 | 재설치 후 같은 게스트 id | 🤖 `ANDROID_ID` / 🍎 Keychain | 🧪 `deviceId.ts` |
| V8 | Keychain 접근 실패 | AsyncStorage로 폴백, 앱 시작 안 막힘 | 🧪 |
| V9 | **기존 사용자 id 불변** | 업데이트만으로는 아무도 id가 안 바뀜 | 🧪 기존 값 우선 + Keychain 백필 |
| V10 | `ANDROID_ID`를 광고에 안 씀 | 정책 위반 회피 | 🧪 광고 경로에 참조 없음 |
| V11 | 음수 duration | 없음 | 🧪 전 계산에 `Math.max(0, …)` |
| V12 | `started_at ≤ ended_at` | 항상 성립 | 🧪 `startedAtOverride`/`markSleepDetected` |
| V13 | 4시간 상한 | 3개 집계 함수 전부 동일 | 🧪 `14400` 일치 확인 |
| V14 | 고아 세션 정리 | 콜드스타트에 청소 | ✅ 08-12 |
| V15 | 열린 세션 2개 이상 | 앱별 집계가 하나만 봄 | 🔍 D16/M15 — 규칙 확정 필요 |
| V16 | 중복 sync | 같은 행에 합류(PK 재사용) | ⬜ |
| V17 | DB 파일 손상 | 앱이 살아남 | ⬜ |
| V18 | 사용자 데이터 내보내기 | 제공 여부 정의 | ⬜ |

## W. 장애 복구 · 네이티브 미링크 (W1~W12)

> 이 앱은 네이티브 모듈 의존이 유난히 많고(6개 자체 모듈 + 광고 + 결제 + updates + secure-store),
> **미링크로 부팅이 죽은 사고가 실제로 있었다**(2026-07-22 `ExpoUpdates` → ErrorBoundary undefined →
> 앱 전체 크래시). 그 뒤로 전 모듈에 방어적 require를 깔았는데, 그게 유지되는지 보는 검사가 없었다.

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| W1 | JS 크래시 | ErrorBoundary가 잡음 | 🧪 `getDerivedStateFromError`+`componentDidCatch` |
| W2 | ErrorBoundary 화면 | 사용자가 빠져나갈 경로 있음 | ⬜ |
| W3 | `expo-updates` 미링크 | 조용히 스킵 | 🧪 lazy require |
| W4 | 광고 SDK 미링크 | 광고만 스킵 | 🧪 `adsConfig`/`AdBanner`/`rewardedAd` 3곳 |
| W5 | `expo-secure-store` 미링크 | 게스트 id 폴백 | 🧪 |
| W6 | RevenueCat 키 없음 | 앱 정상, 구독만 비활성 | ⬜ 빈 문자열 기본값 확인 |
| W7 | 세션 중 접근성 권한 회수 | 감지 + 안내, 수면감지 **확정 보류** | ⬜ |
| W8 | 세션 중 오버레이 권한 회수 | 감지 + 안내 | ⬜ |
| W9 | 네이티브 예외가 부팅을 막음 | 막지 않음 | ⬜ |
| W10 | 강제 종료 후 복귀 | 세션 상태 복구 | ⬜ |
| W11 | 저메모리 종료 | 복구 | ⬜ |
| W12 | 백그라운드 폴링이 ANR을 유발 | 유발 안 함 | ⬜ |

## X. 릴리즈 게이트 — **이 순서대로 돌려서 전부 PASS면 올린다** (X1~X58)

> 5부(자동화 현황)가 "게이트가 없어서 vc10/vc11이 검사 없이 나갔다"고 적어 놓고 **게이트 항목
> 자체는 없었다.** 여기를 그 게이트로 만든다.
>
> ### 게이트 전제 — 무엇으로 도는가 (이걸 어기면 나머지가 전부 무의미)
>
> **반드시 제출할 그 바이너리로 돈다.** dev 빌드/Expo Go/`expo run` 결과물로 돌면 이 앱에서는
> 통과가 통과가 아니다 — 이유가 이 프로젝트에 실제로 쌓여 있다:
>
> | 릴리즈에서만 갈리는 것 | 근거 |
> |---|---|
> | 광고가 실광고인가 테스트 광고인가 | `USE_REAL_ADS = !__DEV__ && …` — dev는 **영원히 테스트 광고** |
> | OTA가 도는가 | `checkAndForceUpdate`가 `__DEV__`면 즉시 `skipped-dev` |
> | dev POC 화면이 보이는가 | `shorts-poc:63`/`tiktok-poc:360`의 `if (!__DEV__)` 스텁 |
> | 강제 업데이트 게이트가 판정하는가 | dev는 빌드번호를 못 읽어 `no-version`으로 통과 |
> | 프로가드/난독화가 네이티브 브리지를 깨는가 | 릴리즈 빌드에서만 드러남 |
>
> 즉 **A~W에서 아무리 ✅가 쌓여도 dev 빌드로 얻은 것이면 X를 대신하지 못한다.**
>
> 명령: `eas build --profile production --platform android` (AAB) / `… --platform ios`
> 검증 설치: `adb -s <serial> install -r <릴리즈 apk/aab에서 뽑은 apks>` — 스토어 서명본과
> 같은 경로여야 한다.

---

### X-A. 산출물 정합성 — 빌드 **전에** 정적으로 (약 5분, 전부 자동 확인 가능)

| # | 항목 | PASS 기준 | 확인 방법 | 현재 |
|---|---|---|---|---|
| X1 | versionCode 미사용 | Play 콘솔에 없는 번호 | `app.json` + `build.gradle` | ✅ **해결(`f7ae3df`) — 12**. 두 파일 다 올림 |
| X2 | iOS build number | ASC에 없는 번호 | `app.json` ios.buildNumber(현재 7) | ⬜ |
| X3 | version 문자열 일치 | `build.gradle` ↔ `app.json` | `versionName` vs `version` | 🧪 **해결** — 둘 다 `1.0.3`(발견 13) |
| X3b | 🤖 진짜 값은 `build.gradle` | `app.json`이 아니라 여기를 본다 | 파일 주석이 명시 | 🧪 `build.gradle:95` |
| X4 | 🤖 실광고 스위치 | 트랙에 맞는 값이 **명시**돼 있음 | `adsConfig.ts` `USE_REAL_ADS` | 🧪 **해결** — eas.json production에 `EXPO_PUBLIC_ANDROID_REAL_ADS: "false"` 명시 |
| X5 | 🤖 테스트 트랙이면 테스트 광고 | 현재 `submit.android.track=alpha`(비공개 테스트) → false가 맞다 | 동일 | 🧪 **해결** — 없어도 기본이 false지만 "빠뜨린 것"과 "정한 것"을 구분하려 적었다 |
| X5b | 프로덕션 트랙 승격 시 | 그날 `"true"`로 바꾼다 | eas.json | ⬜ **잊으면 수익 0** |
| X6 | 🍎 실광고 | 프로덕션은 자동 실광고 | `Platform.OS !== 'android'` | 🧪 |
| X7 | 테스트기기 강제 해제 | `EXPO_PUBLIC_AD_TEST_DEVICES`가 production에 **없음** | eas.json | 🧪 없음 확인 |
| X8 | 죽은 env 없음 | 정의했는데 아무도 안 읽는 변수 0개 | `EXPO_PUBLIC_*` 교차 대조 | 🧪 **해결** — eas.json 3개 프로필에서 `EXPO_PUBLIC_USE_REAL_ADS` 제거 |
| X9 | dev POC 스텁 | 두 파일 다 `if (!__DEV__)` 반환 | `shorts-poc:63`/`tiktok-poc:360` | 🧪 ⚠️ 발견 4 |
| X10 | 심사자 화이트리스트 | 제출 계정이 등록됨 | `reviewers.ts` `s7.reviewer@gmail.com` | 🧪 |
| X11 | 진단 로그 | 릴리즈에 민감정보/과다 로그 없음 | `LAUNCH_CHECKLIST.md` P1 대상 파일 | ⬜ |
| X12 | 타입체크 | `tsc --noEmit` 0 error | — | ⬜ |
| X13 | JS 테스트 | `npm test` 통과 | — | ❌ **스크립트 자체가 없음** |
| X14 | 미커밋 변경 없음 | `git status` 깨끗 | 지금 4개 파일 수정 중 | ❌ **현재 미커밋** |

> **X8이 왜 여기 있나** — `eas.json` production이 `EXPO_PUBLIC_USE_REAL_ADS: "true"`를 넘기는데
> **코드 어디도 이 이름을 읽지 않는다**(주석 4곳에 이름만 남아 있다). 실제 스위치는
> `EXPO_PUBLIC_ANDROID_REAL_ADS`인데 eas.json에 **없다**. 그래서 지금 상태로
> `--profile production` 안드로이드 빌드를 올리면 **실사용자에게 테스트 광고가 나가고 수익이 0**이
> 된다 — 그런데 eas.json만 보면 "실광고 켜둠"으로 읽힌다. 정확히 `de02499`가 고쳤던 사고가
> 설정 파일 쪽에 그대로 남아 있는 셈이다.

---

### X-B. 설치 직후 — **신규 사용자 콜드 상태** (기기 필요)

> `adb shell pm clear com.strides7.pace` 로 만든 상태에서 시작한다(재설치와 동등하게 만들려면
> **삭제 후 재설치**가 정확하다 — `pm clear`는 `ANDROID_ID`/Keychain을 안 건드리므로 V7 검증엔 부적합).

| # | 항목 | PASS 기준 | 현재 |
|---|---|---|---|
| X15 | 첫 실행 크래시 | FATAL 0건 (`adb logcat -b crash`) | ⬜ |
| X16 | 온보딩 완주 | 6페이지 → 홈 도달 | ⬜ |
| X17 | 권한 **전부 거부**로 완주 | 앱이 살아서 홈까지 감, 안내 표시 | ⬜ ⚠️ |
| X18 | 권한 부분 허용 조합 | 접근성만/오버레이만/알림만 — 각각 크래시 없음 | ⬜ |
| X19 | 게스트로 전 기능 도달 | 로그인 강요 없음 | ✅ 08-12(B1) |
| X20 | 강제 업데이트 게이트 통과 | 최신 빌드는 안 막힘 | 🧪 R19 |
| X21 | 게이트 부팅 지연 | 체감 지연 없음(4초 상한) | ⬜ |
| X22 | OTA 체크 | 릴리즈에서 실제로 돎(`skipped-dev` 아님) | ⬜ ⚠️ dev로는 확인 불가 |
| X23 | 배너 광고 종류 | 트랙에 맞는 광고(X4/X5와 일치) | ⬜ ⚠️ |
| X24 | 첫 세션 시작~종료 | DB 행 1개, `duration` 정확 | ✅ 08-12(D13) |

---

### X-C. 핵심 경로 — **돈·시간·집행** (여기서 하나라도 FAIL이면 올리지 않는다)

| # | 항목 | PASS 기준 | 현재 |
|---|---|---|---|
| X25 | 일일 한도 차감 | 1분마다 감소, 대상 앱 밖에선 정지 | ✅ 08-12 |
| X26 | 한도 소진 → 연장 모달 | 표시 | ✅ 08-12 |
| X27 | 광고 연장 = **정확히 5분** | 10분 아님 | ✅ 08-12 |
| X28 | 광고 하루 3회 한도 | 4번째 차단 | 🧪 J8 — 실기기 미확인 |
| X29 | **재설치 후 한도 우회 불가** | 서버 max 병합으로 회수 | 🧪 `FocusAllowanceService.sync` max/OR 확인 — 실기기 미확인 |
| X29b | **날짜 변경 우회 불가** | 기기 날짜를 바꿔도 한도가 안 풀림 | ❌ **뚫린다(발견 10)** |
| X30 | 크레딧 연장 | 5 차감, 음수 없음 | 🧪 L18 |
| X31 | 구매 성공 → 즉시 프리미엄 | 반영 | ⬜ ⚠️ |
| X32 | 구매 복원 | 프리미엄 복구 | ⬜ ⚠️ |
| X33 | 프리미엄 = 광고 없음 | 배너 미표시 | 🧪 `(tabs)/_layout.tsx:107` |
| X34 | 구독 만료/환불 | 무료로 복귀 | ⬜ |
| X35 | **개발 모드 과금 차단** | dev에서 결제 경로 진입 불가 | ❌ **발견 2 — 미구현** |
| X36 | 수면 오탐(K8) | 손으로 넘기며 20분 → 세션 안 끝남 | ✅ 08-12 |
| X37 | 수면 정탐(K9) | 야간 무입력 → 팝업 → 30초 → 종료 | ✅ 08-12 |
| X38 | 🤖 자동넘김 실동작 | 로그 아니라 **화면으로** 영상이 바뀜 | ✅ 08-12 |
| X39 | 🤖 자동넘김이 댓글창을 안 엶 | 안 엶 | ✅ 08-12 |
| X40 | 검색 결과는 자동 진행 안 함 | 고른 것만 재생 | ✅ 08-12 |

---

### X-D. 스토어 정책 — **반려 사유가 되는 것만**

| # | 항목 | PASS 기준 | 현재 |
|---|---|---|---|
| X41 | 🤖 접근성 서비스 사용 고지 | Play 정책 문구 + 인앱 고지 | ⬜ ⚠️ **반려 단골** |
| X42 | 🤖 접근성 용도가 정책에 부합 | 접근성 목적 설명이 실제 기능과 일치 | ⬜ ⚠️ |
| X43 | 개인정보 처리방침 접근 | 앱 안에서 열림 | ⬜ |
| X44 | 데이터 안전성 신고 정확 | 실제 수집 항목과 일치 | ⬜ |
| X45 | 계정 삭제 경로 | 앱 안 + 웹 | ⬜ ⚠️ 양 스토어 필수 |
| X46 | 브랜드 명칭 | `Shorts with PACE`/`Loops with PACE` — 마크 합성 금지 | 🧪 `QA_MATRIX.md` 1-4 확정 |
| X47 | 스크린샷/설명에 YouTube 로고 없음 | 없음 (5.2.2 방어) | ⬜ |
| X48 | 피드가 첫 화면 아님 | 집중 세션이 메인 | ⬜ |
| X49 | 🍎 Sign in with Apple 공식 버튼 | HIG 4.8 | ⬜ ⚠️ `LAUNCH_CHECKLIST` C2 미해결 |
| X50 | 🍎 동작 안 하는 UI 없음 | BT 스텁/Sleep Timer 미구현 노출 금지 | ⬜ ⚠️ C5/C1 |
| X51 | App Review Notes 첨부 | `APP_REVIEW_NOTES.md` 붙여넣기 | ⬜ |
| X52 | 심사자 계정 로그인 | 화이트리스트로 프리미엄 진입 | ⬜ |

---

### X-E. 되돌리기 능력 — **사고가 났을 때 복구할 수 있는가**

> 앞의 어떤 항목보다 이게 중요하다. 위를 다 통과해도 사고는 난다 — 그때 **스토어 심사를 다시
> 타지 않고** 고칠 수 있느냐가 이 앱의 실제 안전망이다.

| # | 항목 | PASS 기준 | 현재 |
|---|---|---|---|
| X53 | OTA가 이 빌드에 도달 | 채널 `production`, runtimeVersion 일치 | ⬜ ⚠️ 🤖 `1.0` 고정 / 🍎 `1.0.3` |
| X54 | OTA 롤백 | `eas update:roll-back`이 실제로 되돌림 | 🧪 R10 — **실행 확인 필요** |
| X55 | 광고 킬 | 사고 시 광고를 끌 수단 | ⬜ **없으면 OTA뿐** |
| X56 | 강제 업데이트 킬스위치 | `app-config.enabled=false` 배포로 즉시 해제 | 🧪 R15 |
| X57 | `minBuildNumber` 운영 규칙 | **심사 중 상향 금지**, 출시 후 하루이틀 뒤 | 🧪 현재 `1/1`(아무도 차단 안 됨) |
| X58 | 백엔드 롤백 | `railway up`으로 이전 버전 복귀 | ⬜ |

---

### PASS 판정 규칙

1. **X-A·X-C·X-E에 ❌가 하나라도 있으면 올리지 않는다.** 각각 "빌드가 틀림 / 돈과 집행이 틀림 /
   틀렸을 때 못 고침"이라 사후 복구가 안 되거나 비싸다.
2. **X-D의 ⚠️는 반려로 직결된다** — 심사에서 걸리면 재제출 주기가 며칠 날아간다.
3. **X-B는 ❌ 없이 ⬜만 남았으면 올려도 된다** — 단 ⬜로 남긴 항목을 릴리즈 노트에 적어 둔다.
4. 🧪만 있고 ✅가 없는 항목은 **"코드는 맞다"까지만** 통과다(위 "🧪는 무엇을 보증하나").
   X-C의 🧪는 전부 실기기로 승격하는 것이 목표다.

> **이 게이트가 구조적으로 보증하지 못하는 것** — 정직하게 적어 둔다.
> ① 심사원의 주관적 판단(특히 X41/X42 접근성 용도), ② 우리 기기 1대(Note20)에 없는 OEM의
> 배터리 정책(아래 Y-7), ③ 사용자 규모에서만 드러나는 서버 쿼터·부하(G12/P13이 그 예다),
> ④ 시간이 지나야 터지는 것(구독 갱신·만료, 틱톡/유튜브 UI 변경). **패스는 "알려진 실패를 다
> 막았다"이지 "실패가 없다"가 아니다.**

## Y. 중단·예외 시나리오 — 전원 · 배터리 · 강제종료 · 네트워크 · OEM (Y1~Y70)

> **이 앱에서 이 카테고리가 특별히 위험한 이유.** 보통 앱은 프로세스가 죽으면 "사용자가 불편"할
> 뿐이다. 이 앱은 프로세스가 죽으면 **집행이 사라진다** — 알약이 없어지고 차감이 멈춘 채 사용자는
> 계속 볼 수 있다. 즉 여기서의 실패는 기능 실패가 아니라 **제품 목적 자체의 실패**다. 반대로
> 과잉 방어(살아나려고 배터리를 태우거나 재부팅마다 튀어나오는 것)는 스토어 반려와 삭제 사유가
> 된다. **두 방향 다 검사한다.**
>
> 업계 표준 인터럽트 테스트 분류(기기 인터럽트 / 네트워크 인터럽트 / OS 인터럽트 + 상태 복구)를
> 뼈대로 쓰고, 이 앱 고유의 "포그라운드 서비스 + 접근성 서비스 생존" 축을 더했다. 출처는 문서 끝.
>
> **재현 명령은 전부 `adb -s R3CN80S5GWW …` 기준이다.**

### Y-1. 전원 · 배터리 (Y1~Y12)

| # | 시나리오 | PASS 기준 | 재현 |
|---|---|---|---|
| Y1 | 세션 중 **전원 버튼 종료** | 재부팅 후 고아 세션이 정리되고 시간이 유실 안 됨 | 실제 종료 |
| Y2 | 세션 중 **배터리 방전 종료** | Y1과 동일 | `cmd battery set level 1` |
| Y3 | 재부팅 후 접근성 서비스 | 자동 유지되는가 / 안 되면 **안내가 뜨는가** | `adb reboot` |
| Y4 | 재부팅 후 세션 상태 | 정의된 동작(복구 or 종료) — **정의부터 필요** | `PaceBootReceiver.kt` |
| Y5 | 절전 모드 진입 | 차감이 멈추거나 튀지 않음 | `settings put global low_power 1` |
| Y6 | **Doze 진입** | 알약·차감이 정지되면 그 사실이 기록됨 | `dumpsys deviceidle force-idle` |
| Y7 | Doze 해제 복귀 | 남은시간이 벽시계로 튀지 않음 | `dumpsys deviceidle unforce` |
| Y8 | 배터리 최적화 대상일 때 | 포그라운드 서비스 유지 안내 배너 | A26/A27 |
| Y9 | 최적화 예외 배너 탭 | 시스템 설정으로 이동 | ⬜ |
| Y10 | 저전력에서 광고 로드 | 실패해도 크레딧 대안 제시 | ⬜ |
| Y11 | **충전 중 = 수면 보조신호** | 오탐 방향으로 기울지 않음 | I16 |
| Y12 | 과열 스로틀링 | 앱이 죽지 않음 | ⬜ |

### Y-2. 프로세스가 갑자기 죽을 때 (Y13~Y24) ⚠️ 최우선

> **핵심**: 세션 종료 DB write가 React 언마운트 cleanup에 묶여 있다(`overlay/index.tsx`).
> 프로세스가 죽으면 **그 write가 아예 안 돈다** — 다음 콜드스타트의 `closeOrphanedSession`이
> 유일한 안전망이다. 그 안전망만 검사해서는 부족하고, **안전망이 못 잡는 창**을 봐야 한다.

| # | 시나리오 | PASS 기준 | 재현 |
|---|---|---|---|
| Y13 | 세션 중 앱 강제 종료 | 고아 세션이 다음 실행에 정확히 닫힘 | ✅ 08-12(D15, 55분 정확) |
| Y14 | 세션 중 프로세스 kill | 알약이 남지 않음 | `am kill com.strides7.pace` |
| Y15 | 세션 중 force-stop | 서비스·알림 전부 정리 | `am force-stop com.strides7.pace` |
| Y16 | 강제종료 직후 **재실행 없이** 대상 앱 사용 | **집행 공백** — 어디까지 허용할지 정의 | ⬜ ⚠️ **정의 없음** |
| Y17 | 고아 세션이 **2개 이상** | 앱별 집계가 하나만 봄 | 🔍 V15/D16 |
| Y18 | 강제종료 후 즉시 재실행 | 0초 세션이 안 쌓임 | ❌ 🤖 가드 없음(발견 1) |
| Y19 | 크래시 후 재실행 | ErrorBoundary → 복구 경로 | ⬜ W1/W2 |
| Y20 | OTA 리로드가 세션을 날림 | 세션 중이면 **리로드 보류** | 🧪 R5 |
| Y21 | OTA 리로드가 재생을 날림 | 🍎 재생 중 보류 | 🧪 R6 |
| Y22 | 저메모리 킬 후 복귀 | 상태 복구 | ⬜ |
| Y23 | 세션 중 앱 업데이트(스토어) | 프로세스 교체 후 정합 | ⬜ |
| Y24 | 세션 중 앱 삭제 | 서버 허용량은 남음 | ⬜ ⚠️ X29와 같은 축 |

### Y-3. 네트워크 (Y25~Y36)

> **이 앱의 원칙은 fail-open이다** — 오프라인이라고 정상 사용자의 연장을 막지 않는다.
> 그래서 검사 방향이 둘이다: **막지 않는가**(사용성) + **우회가 회수되는가**(매출).

| # | 시나리오 | PASS 기준 | 재현 |
|---|---|---|---|
| Y25 | 기내모드로 첫 실행 | 크래시 없음, 오프라인 안내 | `svc wifi disable; svc data disable` |
| Y26 | 오프라인 연장 | **막히지 않음**(fail-open) | K7 |
| Y27 | **비행기모드로 광고 한도 우회** | 다음 온라인에서 max 병합으로 회수 | 🧪 서버 `max` 병합 확인 — 실기기 미확인 |
| Y27b | **오프라인 + 날짜 변경 동시** | 막힘 | ❌ **뚫린다(발견 10)** — 오프라인이면 서버 조회조차 없다 |
| Y28 | 오프라인 세션 기록 | 로컬 저장 후 나중에 업로드 | ⬜ |
| Y29 | 온라인 복귀 백로그 | 일괄 업로드, 중복 없음 | ⬜ |
| Y30 | 세션 도중 끊김 | 로컬 계속, 알약 정상 | ⬜ |
| Y31 | 느린 3G | 부팅이 4초 넘게 안 막힘 | 게이트 타임아웃 🧪 R18 |
| Y32 | 서버 500 / 타임아웃 | 재시도, 앱 멈춤 없음 | ⬜ |
| Y33 | 허용량 조회 401 | 로컬로 동작 | ⬜ |
| Y34 | **허용량 단조 병합** | 0 전송이 3을 지우지 않음 | ⬜ ⚠️ P8 |
| Y35 | OTA 체크 중 끊김 | `check-failed` + 백오프 | 🧪 R3/R4 |
| Y36 | 광고 로드 실패 반복 | 백오프, 무한 재시도 없음 | 🧪 |

### Y-4. OS 인터럽트 (Y37~Y46)

| # | 시나리오 | PASS 기준 |
|---|---|---|
| Y37 | 세션 중 전화 수신 | 복귀 후 정상, 차감 정합 |
| Y38 | **광고 재생 중 전화** | 보상 처리가 명확(지급 or 미지급), 상태 안 깨짐 |
| Y39 | 광고 중 앱 이탈 | 보상 미지급, 상태 정상 |
| Y40 | 결제 중 인터럽트 | 이중 청구 없음 ⚠️ |
| Y41 | 권한 팝업이 제스처 중에 뜸 | 교착 없음 |
| Y42 | 수면 팝업이 대상 앱을 가림 | 30초 타임아웃으로 반드시 빠져나옴 (I12) |
| Y43 | 알림 홍수 | 중복 알림 없음 |
| Y44 | 방해금지 모드 | OS 정책 준수 |
| Y45 | 화면 회전 / 폴더블 접힘 | 알약 위치 보정, 레이아웃 유지 |
| Y46 | 멀티윈도우·PIP | 창 경계 기준 판정 정확 |

### Y-5. 저장소 · 메모리 (Y47~Y52)

| # | 시나리오 | PASS 기준 |
|---|---|---|
| Y47 | 저장소 가득 | DB write 실패가 앱을 죽이지 않음 |
| Y48 | DB 파일 손상 | 복구 or 초기화, 크래시 루프 없음 ⚠️ |
| Y49 | 앱 데이터 삭제 후 실행 | 온보딩부터, **서버 허용량은 유지** |
| Y50 | 캐시 삭제 | 피드/썸네일 재로드 |
| Y51 | 장시간 세션 메모리 | 누수 없음(4시간) |
| Y52 | 오버레이 이벤트 로그 무한 증가 | 상한 or 정리 존재 → ❌ **정리 로직 없음(발견 11)** |

### Y-6. 시간 · 로케일 · 기기 설정 (Y53~Y60)

| # | 시나리오 | PASS 기준 |
|---|---|---|
| Y53 | 기기 시간 수동 변경(과거로) | 음수 시간 없음 🧪 D19 |
| Y54 | 시간 변경으로 **자정 넘김 위조** | 한도가 리셋되어 무한 사용되지 않음 → ❌ **뚫린다(발견 10)** |
| Y54b | 날짜 위조로 **출석 크레딧 반복 수령** | 막힘 → ❌ **뚫린다** — 출석은 서버 연동이 아예 없다 |
| Y54c | 날짜 위조 후 **정상 시각 복귀** | 부당 취득분이 회수됨 → ❌ 회수 경로 없음(서버가 max로만 올린다) |
| Y54d | 자동 시각(NTP) 강제 | 앱이 신뢰 가능한 시각을 쓰는가 | ⬜ **설계 결정 필요** |
| Y54e | 날짜 위조로 **포커스 게이트** 해제 | 막힘 → ✅ **안 뚫린다** — `timedOut`이 날짜 키 없이 저장되고 서버 병합이 OR. **여기는 건드리지 말 것** |
| Y55 | 타임존 변경 | 오늘 집계 정확(로컬 기준 일관) 🧪 `utils/date.ts` |
| Y56 | 자정 경계에 세션 진행 중 | 어제/오늘 분리, 시작 시각 기준 귀속 🧪 D18 |
| Y57 | 24시간제 ↔ 12시간제 | 표시 정확 |
| Y58 | 시스템 언어 변경 | 즉시 반영, 미지원 로케일은 영어 폴백 |
| Y59 | 폰트 크기 최대 / 디스플레이 확대 | 알약·모달 안 깨짐 |
| Y60 | 다크/라이트 전환 | 가독성 유지 |

### Y-7. 🤖 OEM 배터리 공격 (Y61~Y70) ⚠️ 우리 기기 1대로는 못 잡는 영역

> 웹 조사 결과 요지: **OEM은 스톡 안드로이드 문서와 무관하게 자체 정책으로 프로세스를 죽인다.**
> 삼성은 3일간 포그라운드 활동이 없으면 "잠자는 앱"으로 분류하고, **OTA 업데이트가 배터리 최적화
> 예외를 조용히 초기화**한다. 샤오미/오포/비보는 자동시작 권한을 리셋하거나 주기적 청소로
> 포그라운드 서비스까지 죽인다. 그리고 **에뮬레이터와 픽셀에서는 이 중 어느 것도 재현되지 않는다.**
> 우리 검증 기기는 갤럭시 Note20 **한 대**뿐이므로, 이 구획은 구조적으로 미검증 상태다.

| # | 시나리오 | PASS 기준 | 비고 |
|---|---|---|---|
| Y61 | 삼성 "잠자는 앱" 분류 | 분류되지 않거나, 되어도 세션이 정상 | 3일 미사용 후 |
| Y62 | **OTA 후 최적화 예외 초기화** | 앱이 감지해서 재안내 | ⚠️ 조용히 무력화되는 경로 |
| Y63 | OTA 후 접근성 서비스 유지 | 유지 or 재안내 | ⚠️ |
| Y64 | 자동시작 권한 리셋(샤오미 계열) | 재안내 | 기기 없음 |
| Y65 | OEM 청소가 포그라운드 서비스 kill | 다음 실행에 고아 정리 | Y13과 동일 안전망 |
| Y66 | **Android 11+ 미사용 앱 권한 자동 초기화** | 복귀 시 권한 재요청 흐름 | 몇 달 미사용 |
| Y67 | 대상 앱만 쓰고 Pace를 안 열 때 | 집행이 계속 도는가 | ⚠️ **제품 핵심** |
| Y68 | 배터리 사용량 이상 신고 | 과도한 웨이크락 없음 | 삭제 사유 방어 |
| Y69 | 다른 접근성 앱과 공존 | 서로 안 깨짐 | ⬜ |
| Y70 | 기기 2종 이상 교차 검증 | 최소 삼성 + 비삼성 1대 | ❌ **현재 삼성 1대뿐** |

> **Y67이 이 구획의 결론이다.** 사용자는 Pace를 열지 않고 유튜브만 쓴다 — 그게 정상 사용이다.
> 그 상태에서 OEM이 우리 프로세스를 죽이면 **아무도 모르게 집행이 꺼진다**(사용자는 앱이 고장난
> 줄도 모르고, 우리는 지표로도 못 본다). Y61~Y66은 전부 Y67로 수렴한다.

## Z. 앱 교차 사용 — 틱톡 ↔ 유튜브 번갈아보기 (Z1~Z24, 2026-08-13 신설)

> **왜 별도 구획인가.** `QA_MATRIX.md` 1-4가 "한도·타이머는 앱 구분 없이 공용 — 앱을 갈아타며
> 보는 회피를 막아야 한다"고 못 박아 뒀다. 즉 **앱 교차 사용은 이 제품의 핵심 시나리오**인데,
> A~Y는 이걸 D21 한 줄(`⬜`)로만 갖고 있었다. 두 앱이 **동시에** 떠 있는 조합(PIP)에서 이미
> 실기기 버그가 났고(`2a1cefc`), **순차로** 갈아타는 조합은 아직 아무도 안 봤다.
>
> 축이 셋이다: ① 순차 전환(A 보다가 B) ② 동시 존재(A가 PIP로 남고 B를 봄) ③ 되돌아오기(A→B→A).

### Z-1. 순차 전환 — 세션 귀속 (Z1~Z8) 🔴 여기서 결함이 나왔다

| # | 시나리오 | 기대 | 상태 |
|---|---|---|---|
| Z1 | 유튜브 세션 중 **틱톡 카드 탭** | 틱톡 시청이 틱톡으로 기록 | ✅ **08-13 실기기** — 새 세션 `START remaining=45`, 분석탭에 두 앱 다 표시 |
| Z2 | 틱톡 세션 중 유튜브 카드 탭 | 대칭으로 동일 | 🧪 같은 코드 경로 — 반대 방향은 미실측 |
| Z3 | 세션 중 **런처에서** 다른 앱 실행 | 위와 동일하게 귀속 | ❌ 동일(카드 경유 여부와 무관) |
| Z4 | 전환 시 홈 카드 "Active" 배지 | 지금 보는 앱에 표시 | ✅ 08-13 — 세션이 갈리므로 배지도 따라간다 |
| Z5 | 전환 후 분석탭 Platform Breakdown | 두 앱이 각각 뜸 | ✅ **08-13 실기기 — YouTube + TikTok 둘 다 표시** |
| Z6 | 전환 시 이중 세션 행 | 안 생김 | 🧪 의도적으로 방지(`home.tsx:497` 감사 HIGH2) |
| Z7 | 전환 시 남은시간 | 공용이므로 이어서 차감 | 🧪 `useTimerStore` 공용 |
| Z8 | 전환 시 포커스 타이머 | 공용 `endsAt` 하나로 유지 | 🧪 |

> **Z5가 M11의 진짜 원인일 가능성이 높다.** 2026-08-12에 "유튜브 55분 + 틱톡 기록이 둘 다
> 있는데도 섹션 미표시"로 ❌ 처리하고 원인을 "adb로 만든 세션이 안 닫혀서"로 추정했는데,
> **실사용 경로에서도 같은 결과가 나온다** — 갈아타도 행이 하나뿐이라 `platform_app`이 한
> 종류만 남고 `length > 1`이 영원히 거짓이 된다. 재현 조건이 훨씬 흔하다.

### Z-2. 동시 존재 — 두 앱이 같이 떠 있을 때 (Z9~Z16)

| # | 시나리오 | 기대 | 상태 |
|---|---|---|---|
| Z9 | 유튜브 PIP + 틱톡 전체화면 | **전체 창인 쪽**의 진행률을 읽음 | 🧪 `2a1cefc` — `bestTrackedWindow()` 정의 `:1078`, 읽기 경로 `:1098` |
| Z10 | 같은 조합에서 스와이프 | 읽는 창과 쏘는 창이 같음 | 🧪 스와이프 경로 `:1354`가 같은 함수 호출 — **두 호출처 확인** |
| Z11 | One UI PIP 플래그 잔류 | 창 폭 80% 기준으로 거름 | 🧪 |
| Z12 | 둘 다 전체화면 후보일 때 | `currentTrackedPackage` 우선 | 🧪 |

> ⚠️ **Z6·Z9~Z12를 처음에 ✅로 적었다가 🧪로 내렸다(2026-08-13 재확인).** 근거가 **커밋 메시지와
> 코드**였지 실기기가 아니었다. 이 문서에서 ✅는 "실기기 검증 완료(날짜 표기)"이고, 그 구분이
> 무너지면 **"검증됐다"는 숫자 자체가 거짓말이 된다** — 이 문서가 막으려는 것이 정확히 그것이다.
> `2a1cefc`가 고친 증상("유튜브가 작은 창 갔다 큰 창으로 왔다")은 실기기에서만 나는 조합이므로,
> **PIP 상태를 실제로 만들어 재확인**해야 ✅로 올릴 수 있다.
| Z13 | 동시 존재 시 알약 | 하나만, 보는 앱 위에 | ⬜ |
| Z14 | 동시 존재 시 차감 | 이중 차감 없음 | ⬜ ⚠️ |
| Z15 | 스플릿스크린(둘 다 절반) | 정의된 동작 | ⬜ **정의 없음** |
| Z16 | PIP 유튜브가 재생 중일 때 수면감지 | 오탐 없음 | ⬜ ⚠️ |

### Z-3. 자동넘김 상태 이월 (Z17~Z24)

> 두 앱의 진행률 신호가 **다르다**: 유튜브는 `현재/전체 시간 텍스트`, 틱톡은 `SeekBar RangeInfo`.
> 그런데 판정 상태 변수는 서비스 하나에 공유돼 있고, `currentForegroundPackage`가 바뀔 때
> **리셋하는 코드가 없다**(`PaceAccessibilityService.kt:695`는 대입만 한다).

| # | 항목 | 기대 | 상태 |
|---|---|---|---|
| Z17 | 전환 시 `estimatedDurationMs` | 새 앱 첫 영상에 이월 안 됨 | 🔍 새 영상 감지 시엔 리셋되나 **앱 전환 자체로는 안 됨** |
| Z18 | 전환 시 `lastAdvanceTotalSec` | 유튜브 전용 값이 틱톡 판정에 안 섞임 | 🔍 미확인 |
| Z19 | 전환 시 `lastKnownFrac` | 틱톡 값이 유튜브 판정에 안 섞임 | 🔍 미확인 |
| Z20 | 전환 시 `videoStartedAtMs` | 90초 상한이 새 앱 기준으로 다시 시작 | 🔍 미확인 |
| Z21 | 전환 직후 즉시 스와이프 | 이월된 값 때문에 첫 영상이 잘리지 않음 | ⬜ ⚠️ 광고 복귀 때 겪은 것과 **같은 유형** |
| Z22 | 전환 시 `videoAdvanceCount` | 세션 편수가 앱별로 구분되나 | ⬜ 지금은 한 세션에 합산 |
| Z23 | 전환 시 수면감지 증거채널 | 앱마다 다른 채널로 올바르게 전환 | ⬜ ⚠️ **2026-08-12 수면 오탐의 뿌리** |
| Z24 | 전환 시 20초 폴백 시계 | 새 앱 기준으로 리셋 | 🔍 `lastSwipeAtMs`는 공유 |

> **Z21이 왜 위험한지는 이미 증명됐다.** 2026-08-13에 "광고 보고 오면 보던 쇼츠가 잘린다"는
> 지적으로 광고 복귀 시 `lastSwipeAtMs`/`videoStartedAtMs`/`lastFracAtMs`를 리셋하는 코드가
> 들어갔다(`checkPlaybackAndMaybeSwipe` 상단). **앱 전환도 정확히 같은 모양의 중단**인데
> 그 리셋이 없다 — 광고에서만 고치고 앱 전환에는 같은 규칙을 안 적용한 상태다.

---

# 🔴 이 문서를 만들며 발견한 것 (즉시 조치 대상)

| # | 발견 | 근거 | 심각도 |
|---|---|---|---|
| 1 | ~~탭 진입 직후 사용량 0 깜빡임~~ (C3) | 1회 관측 후 **재현 실패** — 이후엔 진입 즉시 정확(56m) | 관찰 |
| 2 | ~~열린 세션 2개 이상 집계 누락~~ (D16/M15) | **철회** — 고아 정리가 콜드스타트에 돌아 실사용 재현 안 됨 | 철회 |
| 2b | 🔴 **앱별 사용시간 섹션이 안 뜬다** (M11) | 08-12: 유튜브 55분 + 틱톡 기록이 **둘 다 있는데도** 미표시 | 중 |
| 3 | **KR 목록에 스페인어 콘텐츠** (G5) | 08-12 HOT: `Mundos Salvajes` | 중 |
| 4 | **"유튜브 앱 켠 시간" 문구가 틱톡 추가 후 부정확** (M18) | 분석 탭 문구 | 하 |
| 5 | **검색/HOT이 쿼터를 태워 동반 정지** (G12/H22/P13) | 08-12 `quotaExceeded` | 상(수정 배포함) |
| 6 | **versionCode 11 = 이미 사용됨** | 업로드 불가 | 출시 차단 |
| 7 | **광고 트랙 설정 미확정** | 프로덕션이면 실광고 필요 | 출시 차단 |

## 2026-08-13 확장·코드검증에서 새로 나온 것

| # | 발견 | 근거 | 심각도 |
|---|---|---|---|
| 1 | ~~3초 미만 세션 무시가 🍎에만 있다~~ | ✅ **해결(2026-08-13)** — `sessionTeardown.ts`에 가드를 넣어 🤖도 3초 미만은 시청으로 기록하지 않는다. 다만 열린 행을 그냥 두면 고아가 되므로 **0초 + `discarded_too_short`로 닫는다**("짧아서 안 세는 것"과 "안 닫은 것"을 통계에서 구분) | ~~중~~ → 해결 |
| 2 | ~~개발 모드 과금 차단이 없다~~ | ✅ **해결(2026-08-13)** — `purchase`/`restore` 둘 다 `if (__DEV__) throw 'DEV_BILLING_BLOCKED'`. restore까지 막은 건 개발 중 계정이 실제 구독 상태로 바뀌면 무료/프리미엄 게이팅을 테스트할 수 없어서다 | ~~중~~ → 해결 |
| 3 | 🟡 **무료 보관 한도(3개)가 없다** | K14가 "초과 시 안내"를 약속하는데 `savedVideosRepository`·오버레이·상수 어디에도 한도가 없다. 검사 항목이 **존재하지 않는 기능**을 기술 중 — 구현하거나 항목을 지워야 함 | 중 |
| 4 | ~~dev 화면 차단 근거가 틀렸다~~ | ✅ **주석 정정(2026-08-13)** — "라우트 등록을 빼서 막는다"는 틀린 설명을 지우고, **진짜 방어는 각 파일의 `if (!__DEV__)` 스텁 하나뿐이며 그걸 지우면 즉시 유출된다**고 명시했다. 코드 동작은 원래 안전했으므로 바꾸지 않았다 — 다음 사람이 틀린 근거를 믿고 스텁을 지우는 걸 막는 게 목적 | ~~하~~ → 해결 |
| 5 | ~~마이그레이션 사다리에 강제 수단이 없다~~ | ✅ **해결(2026-08-13)** — `warnIfSchemaDrifted()` 추가. dev 부팅 때 `SCHEMA_SQL`이 선언한 컬럼과 실제 `PRAGMA table_info`를 대조해, 스키마엔 있는데 테이블엔 없으면(= `CURRENT_DB_VERSION`을 안 올렸다는 뜻) 경고한다. **기존 설치에서만 나는 증상이라 신규 설치로 개발하면 절대 안 보이던 것**이 이 사고의 본질이었다. 릴리즈에선 안 돈다 | ~~하~~ → 해결 |
| 6 | ~~집계 4함수 중 하나만 규칙이 다르다~~ | ✅ **해결(2026-08-13)** — `getPreviousWeekStats`도 `CASE WHEN ended_at IS NOT NULL` 형태로 통일. 결과는 원래 같았지만(지난주엔 열린 행이 없다) **규칙이 갈려 있는 상태 자체**를 없앴다 | ~~하~~ → 해결 |
| 7 | 🔍 **실기기에 틱톡이 설치돼 있지 않다** | `adb`(R3CN80S5GWW) 확인: `com.google.android.youtube`만 있고 틱톡 패키지 2종 다 없음. 그런데 🤖 홈에는 틱톡 카드가 조건 없이 노출된다 — **C8(미설치 상태 탭)이 지금 이 기기에서 상시 재현되는 경로**다. 코드상 `tiktok://` 실패 → `webFallback`으로 떨어지지만(안전망 있음), 세션은 그대로 시작돼 **알약도 안 뜨고 시간도 안 깎이는 빈 세션**이 열린다 | 중 |
| 8 | ~~`eas.json` production이 아무도 안 읽는 광고 스위치를 넘긴다~~ | ✅ **해결(2026-08-13)** — 3개 프로필에서 죽은 `EXPO_PUBLIC_USE_REAL_ADS` 제거, production에 실제 스위치 `EXPO_PUBLIC_ANDROID_REAL_ADS: "false"`를 **명시**. 값이 `false`인 건 `submit.android.track`이 아직 `alpha`(비공개 테스트)이기 때문이고, 없어도 기본이 테스트 광고지만 **"빠뜨린 것"과 "정한 것"을 구분**하려고 적었다. 프로덕션 트랙 승격일에 `"true"`로 바꾼다(X5b) | ~~상~~ → 해결 |
| 9 | ~~`app.json`이 아직 `versionCode: 11`~~ | ✅ **해결(`f7ae3df`)** — `app.json`과 `android/app/build.gradle`(진짜 값) 둘 다 12로. 설치본에서 `versionCode=12` 확인 | ~~상~~ → 해결 |
| 10 | ~~기기 날짜를 바꾸면 "하루 N회" 한도가 리셋된다~~ | ✅ **양쪽 다 해결** — 광고 한도는 `f7ae3df`(서버 `sanitizeDate` ±1일 클램프), **출석 크레딧은 2026-08-13**: `FocusAllowanceResponse`에 **`serverToday`(서버 시각 UTC)** 를 얹고 `checkInIfNeeded`가 그 값으로 판정한다. 새 테이블·엔드포인트 없이 **이미 매 부팅 호출되는 응답**에 신뢰 날짜를 실었다. 오프라인은 로컬 날짜 폴백(fail-open) + 단조 가드(`today <= lastCheckInDate`면 미지급) | ~~상~~ → 해결 |
| 11 | ~~`overlay_events`에 정리 로직이 없다~~ | ✅ **해결(2026-08-13)** — `purgeOldOverlayEvents()`(보관 30일)를 콜드스타트 고아 세션 정리 옆에 붙였다. 서버가 `focus_allowance`에 이미 쓰던 규칙(보관 기간)을 로컬 로그에도 적용 | ~~하~~ → 해결 |
| 12 | 🟡 **앱을 갈아타면 두 번째 앱의 시청시간이 첫 번째 앱으로 기록된다** → **코드 수정 완료, 실기기 미검증** | `home.tsx:490`이 세션이 `running`이면 **플랫폼이 달라도** 새 세션을 안 만들고 앱만 띄운다(`launchPlatformApp`). `viewing_sessions.platform_app`은 INSERT 때 한 번 박히고 **세션 도중 갱신하는 코드가 전무**하다(`endSession`도 안 건드림). → 유튜브로 시작해 틱톡을 30분 보면 **30분이 통째로 유튜브로 기록**된다. 원래 의도는 "같은 카드 재탭 시 이중 세션 방지"(감사 HIGH2)였는데 **같은 앱 재탭과 다른 앱 전환을 구분하지 않는다.** D21이 기대값으로 적어 둔 "이전 세션 종료 후 새 세션"이 구현돼 있지 않다. 부수 피해: 홈 "Active" 배지가 엉뚱한 카드에 남고, **분석탭 Platform Breakdown이 `length > 1`을 못 채워 섹션째 숨는다(M11의 실사용 재현 경로)** | **상** |
| 13 | ~~`build.gradle`의 `versionName`이 `app.json`의 `version`과 어긋난다~~ ✅ **해결(2026-08-13)** — `versionName "1.0.3"`으로 맞추고, 같이 올려야 한다는 경고를 그 줄 위에 남겼다 | `f7ae3df`가 versionCode는 양쪽 다 12로 올렸는데 **버전 문자열은 `app.json`만 1.0.3이 되고 `build.gradle`은 `versionName "1.0.2"`로 남았다.** 그런데 `build.gradle:95` 주석이 스스로 적어 뒀듯 **bare workflow에서 진짜 값은 `build.gradle`** 이다(app.json은 `expo prebuild`가 이 파일을 재생성할 때만 쓰임). → 지금 빌드하면 **스토어에 1.0.2로 올라간다**(이미 나간 이름과 같아 릴리즈 관리가 꼬인다). 실기기 설치본에서 `versionCode=12 / versionName=1.0.2`로 실제 확인. **업로드를 막지는 않는다**(Play는 같은 versionName + 다른 versionCode를 허용) — 그래서 출시 차단이 아니라 중이다. 강제 업데이트 게이트도 **빌드 번호로 판정**하므로 영향 없다(`appVersionGate`) | 중 |

| **14** | ~~아무것도 안 봐도 일일 한도가 깎인다~~ ✅ **해결 + 실기기 양방향 검증 완료(2026-08-13 14:23)** | 아래 상세 — 안 볼 땐 5분간 0분 소진, 유튜브 켜면 3분에 3분 정상 차감 | ~~최상~~ → 해결 |
| **15** | 🔴 **앱을 설치/업데이트하면 접근성 서비스가 꺼진다** | 안드로이드 기본 동작. 재설치 직후 `settings get secure enabled_accessibility_services`가 **빈 값**임을 실측. 사용자가 수동으로 다시 켜기 전까지 **추적·집행·자동넘김·수면감지가 전부 죽는다.** 앱은 배너로 알리지만, 사용자가 Pace를 안 열고 대상 앱만 쓰면(정상 사용) 그 배너를 볼 일이 없다 — **Y67로 적어만 두고 안 돌려봤던 그 경로** | **상** — OS 동작이라 못 막는다. 아래 16~19로 "안 막고 유도" 방향을 보강 |
| **16** | 🔴 **알약 "권한 필요" 배지가 탭이 안 먹었다** | 배지를 띄워만 놓고 클릭 리스너가 없었다(탭해도 logcat 한 줄 안 남음을 실측). **유튜브를 보는 동안 상시 노출되는 유일한 유도 지점**인데 죽어 있었다 — 홈 배너는 Pace를 열어야 보이고 알림은 지나치면 그만이다. → 접근성이 깨진 상태에서만 탭 가능하게 하고 접근성 설정으로 보낸다(정상일 때는 리스너를 떼서 기존 알약 탭/P 메뉴를 방해하지 않는다) | **상** → 해결 |
| **17** | 🔴 **집중 탭이 "실시간 추적 중"이라고 거짓말한다** | 그 배지가 **조건 없이 항상** 떠 있었다. 접근성이 꺼져 추적이 반쪽인데도 초록불이라, **홈은 "추적이 꺼져 있어요", 집중 탭은 "실시간 추적 중"** 이라고 같은 앱의 두 화면이 정면으로 반대되는 말을 하는 게 실기기에서 관측됐다. 사용자는 뭘 믿을지 알 수 없고 우리도 "왜 자동넘김이 안 되지?"의 원인을 못 짚는다. → 홈 배너와 **같은 진실원천**(`overlayService.hasAccessibilityPermission`)을 쓰고, 꺼져 있으면 알약과 같은 앰버 "권한 필요"로 바꾸고 탭하면 설정으로 보낸다 | **상** → 해결 |
| **18** | 🟡 **집중 탭 히어로가 "YouTube" 하드코딩** | 틱톡 세션 중에도 "YouTube"라고 떠서 **사용자가 보는 화면과 앱이 말하는 게 달랐다.** 발견 12와 같은 계열(앱 구분이 UI에서 무너지는 지점). → `useSessionStore.platformApp`을 쓴다 | 중 → 해결 |
| **19** | 🔴 **내가 넣은 발견 14 수정이 알약을 죽였다** | `performTick`에서 `ForegroundAppWatcher.getForegroundPackage()`를 **직접 불렀는데**, 그건 순수 조회가 아니라 `cursor`를 전진시키는 **부작용이 있는 상태 함수**다. 이미 포그라운드 폴링이 매초 부르고 있어서 **두 호출이 서로 UsageEvents를 뺏었다** — 내 60초 틱이 `MOVE_TO_FOREGROUND`를 먼저 소비하면 폴링은 그 전이를 영영 못 본다. 실측: 유튜브가 분명히 전경인데 "대상 앱도 포그라운드가 아님"이 찍히고(15:49:32) **같은 시간대에 알약도 사라졌다.** → 새로 조회하지 않고, 폴링이 매초 갱신하는 캐시(`lastUsageBasedVisible`)를 **읽기만** 한다 | **상** → 해결 |

| **20** | 🔴 **대상 앱 밖인데 자동넘김이 20초마다 계속 발사됐다** | `no-progressbar` 폴백이 `root == null`(감시 대상 앱의 창을 아예 못 찾음 = 그 앱이 화면에 없음)일 때도 스와이프를 쐈다. `frac == null`이 "진행바가 없는 영상"과 "앱이 아예 없음"을 구분하지 못했는데, 폴백을 만든 의도는 전자(짧은 틱톡 영상)뿐이었다. **실측: 유튜브에서 빠져나와 Pace 홈 화면에 있는 동안 `AUTO_NEXT reason=no-progressbar`가 20초 간격으로 4회 연속** 찍혔다(`RANGE_INFO none — visited=0 rootPkg=null`과 같은 시각). 사용자 입장에선 가만히 있는데 **화면이 20초마다 제멋대로 스크롤된다.** → `root != null`일 때만 발사 | **상** → ✅ 해결·검증 |

> ⚠️ **F10("대상 앱 밖에서는 자동넘김 안 함")은 문서에 2026-08-12부터 ✅로 적혀 있었다.** 그런데
> 실제로는 이 경로로 뚫려 있었다 — 그때 ✅를 준 검증이 "대상 앱 밖"의 어느 상태를 봤는지 모르겠으나
> 적어도 **Pace 자기 화면**은 안 본 것이다. ✅가 붙어 있다고 안전한 게 아니라는 사례로 남긴다.
> **재검증(수정 후)**: Pace 홈에서 90초 대기 → `AUTO_NEXT` 발사 **0건**(수정 전 20초마다 발사).

> **발견 19는 내가 만든 결함이다.** 발견 14를 고치면서 "UsageStats로 확인하면 된다"까지는 맞았는데,
> 그 함수가 **상태를 가진 함수**라는 걸 안 보고 직접 불렀다. 한 번 고친 뒤 실기기에서 다시 안 돌려봤으면
> "발견 14 해결"로 남은 채 **알약이 죽은 빌드가 나갈 뻔했다.** 실기기 재확인이 왜 필요한지의 사례로 남긴다.

### 발견 15 대응 — "막지 않고 유도" (사장님 결정, 2026-08-13)

> 세션 시작을 막는 안은 **기각됐다.** 사장님 판단: *"막으면 앱을 이탈할거야."* 맞는 판단이다 —
> 접근성이 없어도 **제품의 본체(시간 추적·한도 집행)는 UsageStats로 돌아간다.** 안 되는 건
> 자동넘김·수면감지·손짓 같은 부가기능이다. 막으면 **잘 돌아가는 기능까지 못 쓰게** 된다.
>
> 대신 **안 막았을 때 생기는 문제**를 전수로 찾아 없앴다(발견 16·17·18). 유도 지점은 셋이고
> 이제 셋 다 살아 있다:
>
> | 지점 | 언제 보이나 | 상태 |
> |---|---|---|
> | 알약 "권한 필요" 배지 | **대상 앱을 보는 내내** — 가장 강력 | ✅ 발견 16으로 탭 가능하게 |
> | 알림 | 세션 시작 1분 뒤 1회 | ✅ 앞선 커밋에서 살림 |
> | 홈 배너 · 집중 탭 배지 | Pace를 열었을 때 | ✅ 발견 17로 두 화면이 같은 말을 하게 |

#### ✅ 실기기 검증 완료 (2026-08-13 16:53~16:55, 접근성 OFF→ON 양방향)

| 확인 | 결과 |
|---|---|
| 집중 탭 배지(접근성 OFF) | ✅ "실시간 추적 중"(보라) → **"권한 필요"(앰버)** — 홈 배너와 같은 말 |
| 알약 배지 탭 | ✅ `I PaceOverlay: 권한 필요 배지 탭 — 접근성 설정으로 이동` → 포그라운드가 `com.samsung.accessibility/.homepage.AccessibilityHomepageActivity`로 전환 |
| 알약 생존(발견 19) | ✅ `4m left / 권한 필요`로 정상 표시 — 커서 뺏김이 사라져 다시 뜬다 |
| 접근성 ON 복귀 | ✅ 배지가 **"실시간 추적 중"으로 자동 복귀**, `PaceAccessibility` 로그 재개 |
| 알약 자기앱 숨김 | ✅ Pace 화면에선 사라짐(`supportedAppWindowVisible=false`) |

⚠️ **빌드 함정 하나 기록** — 이 검증 도중 gradle 빌드를 **동시에 두 개** 돌렸더니 JS 번들이 이전
소스로 구워져, APK 타임스탬프는 최신인데 **focus.tsx 수정이 안 들어간 APK**가 나왔다. 화면으로
확인하지 않았으면 "고쳤다"고 넘어갈 뻔했다. 릴리즈 빌드는 **한 번에 하나만** 돌릴 것.

### 🔴🔴 발견 14 — 아무것도 안 봐도 한도가 깎인다 (2026-08-13 실기기 실측)

> **이게 "매번 출시해도 된다더니 기본 기능이 안 됐어"의 정체다.** 그리고 이 문서가 이걸 못 잡은
> 이유가 더 중요하다 — **Y63·Y67에 항목으로 적어만 두고 ⬜로 남긴 채 실기기로 안 돌렸다.**
> 🧪(코드 검증)를 아무리 쌓아도 이건 안 나온다. 실제로 앱을 깔고 4분 놔둬야만 보인다.

**재현 (Galaxy Note20 / R3CN80S5GWW, 릴리즈 빌드 vc12)**

| 시각 | 화면 | 오늘 사용시간 |
|---|---|---|
| 13:32 | **Pace 홈 화면**(유튜브·틱톡 안 켬) | 7분 |
| 13:36 | 동일 | **10분** |

4분 동안 3분이 깎였다. `adb logcat`에도 그대로 남는다:

```
13:33:51 PaceOverlay: tick remaining=52   ← 포그라운드 = com.strides7.pace/.MainActivity
13:34:51 PaceOverlay: tick remaining=51
13:35:51 PaceOverlay: tick remaining=50
13:36:51 PaceOverlay: tick remaining=49
PaceAccessibility: startPlaybackTracking() called but instance is null
  — accessibility not bound yet, usage-time will fall back to always-decrement
```

**원인** — `PaceOverlayService.performTick()`의 차감 조건이 `isPlaying != false`였다. 즉
`isLikelyPlaying()`이 **null(관측 불가)일 때도 차감**한다. 코드 주석은 이걸 *"신호가 아예 없을 땐
안전하게 항상 차감 쪽으로 폴백"* 이라고 불렀는데 — **그 방향이 사용자에게 불리한 쪽이다.**

**이건 이 프로젝트가 스스로 못 박아 둔 원칙과 정면으로 어긋난다** (`QA_MATRIX` 1-3):
> *"오차 방향은 **자는 사람을 못 잡는 쪽**이어야지 **보는 사람 화면을 끄는 쪽**이면 안 된다."*

수면감지는 `canObserveWatchEvidence()` 게이트로 사용자에게 유리하게 폴백하는데, **시간 차감만
반대 방향으로 폴백**하고 있었다. 같은 파일 안에서 두 기능의 안전 방향이 갈려 있었던 것이다.

**연쇄** — 발견 15(설치하면 접근성이 꺼진다) → `isLikelyPlaying()`이 null → 발견 14(무조건 차감)
→ 사용자는 **앱을 깐 직후부터 아무것도 안 했는데 한도가 소진**된다. 두 개가 겹쳐야 터지므로
어느 하나만 봐서는 안 보인다.

**수정(이번 커밋)** — 관측 불가(null)일 때는 **대상 앱이 실제로 포그라운드일 때만** 차감한다.
`ForegroundAppWatcher.getForegroundPackage()`는 UsageStats 기반이라 **접근성과 무관하게** 동작한다.

```kotlin
val shouldDecrement = when (isPlaying) {
  true  -> true
  false -> false
  null  -> SupportedApps.PACKAGES.contains(ForegroundAppWatcher.getForegroundPackage(this))
}
```

⚠️ **우회 경로를 만들지 않는다** — 접근성이 꺼져 있어도 유튜브/틱톡이 포그라운드면 그대로 깎인다.
안 깎이는 건 **대상 앱이 아닐 때**뿐이고, 그건 애초에 깎으면 안 되는 시간이다.

#### ✅ 실기기 검증 완료 (2026-08-13 14:14~14:23, 접근성 **꺼진 상태**로 양방향 확인)

수정본을 릴리즈 빌드로 다시 깔고(설치가 접근성을 다시 껐다 — 발견 15가 그대로 재현), **그 상태로**
두 방향을 다 확인했다. 한쪽만 보면 "안 깎이게만 만든" 더 나쁜 버그를 못 잡는다.

| 구간 | 포그라운드 | `remaining` | 판정 |
|---|---|---|---|
| 14:16~14:19 (5분) | **Pace 홈** | 12 → 12 → 12 → 12 | ✅ **안 깎인다**(수정 전엔 13:33 52분 → 14:11 14분, 38분 소진) |
| 14:20~14:23 (3분) | **유튜브** | 12 → 11 → 10 → 9 → 8 | ✅ **정상 차감** — 접근성이 꺼져 있어도 우회 안 됨 |

로그도 의도대로 남는다:
```
14:19:30 W PaceOverlay: tick skipped decrement — 접근성 미연결이고 대상 앱도 포그라운드가 아님
14:20:30 D PaceOverlay: tick remaining=11   ← 유튜브 포그라운드, 정상 차감
```

→ **K2("대상 앱 밖으로 나감 → 시간이 안 깎임")가 이제 실제로 성립한다.** 이 항목은 그동안
`QA_MATRIX` 2부에 있었지만 빈칸이었고, 채워봤으면 그때 잡혔을 결함이다.

**남은 것(이번 커밋에 없음)**: 발견 15 — 설치·업데이트 후 접근성이 꺼지는 것 자체는 OS 동작이라
막을 수 없다. **재활성화를 사용자가 반드시 통과하게 만드는 흐름**이 필요하다(지금은 닫을 수 있는
배너 하나뿐이고, 대상 앱만 쓰는 사용자는 그걸 볼 일이 없다). 제품 결정이 걸려 있어 코드로
임의 변경하지 않았다.

> ### 발견 12 수정 내용 (2026-08-13) — B안 채택
>
> **① 종료 로직을 공용 함수로 뽑았다**: `src/services/sessionTeardown.ts`.
> `/overlay` cleanup에만 있던 40여 줄(종료 사유 재조회 · `sleep_detected` 시각 보정 · 실시청 초
> 우선순위 · **네이티브 카운터를 stop 전에 읽는 순서**)을 그대로 옮기고, `/overlay`와 `home.tsx`가
> 같이 쓴다. 복제(A안)를 피한 이유는 이 파일들이 반복해서 경고하는 "같은 계산이 두 곳에 갈라져
> 한쪽만 고쳐진다"가 정확히 이 로직에서 났기 때문이다.
> ⚠️ 스토어/네이티브 정리(`timer.endSession` / `autoNext.stop` / `overlayService.endSession` /
> `useSessionStore.finish`)도 그 함수의 `finally`로 **함께** 옮겼다 — `/overlay`에 남겨두면
> `await`보다 먼저 돌아 네이티브 카운터가 읽히기 전에 리셋된다(그 순서가 계약이다).
>
> **② `home.tsx`가 플랫폼을 비교한다**: `running`이고 `platformApp !== 탭한 플랫폼`이면
> `teardownSession(…, endReason: 'manual_stop')`을 **await한 뒤** `startSession(새 플랫폼)`.
> await가 필수인 이유 — 안 기다리면 두 세션이 겹치고, `/overlay`의 마운트 가드
> (`status === 'running'`)에 새 세션이 막혀 **조용히 아무 일도 안 난다**.
>
> **③ ✅ 실기기 검증 완료(2026-08-13 17:02~17:41, 신규 설치 상태부터 풀 패스)**
>
> | 단계 | 관측 |
> |---|---|
> | 유튜브 세션 시작 | `onStartCommand START remaining=60`, 차감 60→45 정상, `VIDEO_ADVANCE reason=near-end current=15s total=16s` |
> | 홈 복귀 | 오늘 사용 **3m** — 유튜브 시청분이 정확히 기록 |
> | **틱톡 카드 탭(유튜브 PIP 상태)** | 새 세션 `START remaining=45` — **한도를 이어받아** 새로 열림(공용 규칙대로) |
> | 틱톡 3분 시청 | 차감 45→43, `AUTO_NEXT reason=frac-predict frac=0.9979` 정상 |
> | **분석 탭** | 🔴 **"플랫폼 분석"에 YouTube와 TikTok이 둘 다 표시됨** |
>
> **이게 결정적 증거다.** 2026-08-12에 사장님이 요구한 *"틱톡을 봤으면 같이 나와야 하잖아"* 가
> **오늘 처음으로 실제로 동작했다.** 그 전까지는 갈아타도 행이 하나뿐이라 `length > 1`을 영영
> 못 채웠다(M11 ❌ / Z1~Z5 ❌의 실체). → **M11·Z1·Z2·Z4·Z5 전부 ✅로 승격.**
>
> **발견 12는 2026-08-12에 사장님이 직접 요구한 기능을 무력화한다** — *"틱톡을 안 봤을 땐 0이니까
> 안 나와도 되는데 봤으면 같이 나와야 하잖아."* 그런데 **갈아타며 보면 영원히 한 앱만 기록되므로
> 두 앱이 같이 나오는 상태 자체가 만들어지지 않는다.** 그동안 M11을 "adb 스크립트가 세션을 안
> 닫아서"로 추정하고 넘겼는데, 실사용 경로에도 같은 결과를 내는 원인이 따로 있었던 셈이다.
>
> **고치는 방향**: `onSelectPlatform`에서 `running`일 때 **현재 세션의 플랫폼과 탭한 플랫폼을
> 비교**해, 같으면 지금처럼 재개하고 **다르면 이전 세션을 닫고 새 세션을 연다**. 시간·한도는
> 공용(`useTimerStore`/`endsAt`)이라 그대로 이어지므로 사용자 체감 변화 없이 기록만 정확해진다.
> 런처로 직접 전환하는 경로(Z3)는 네이티브 `currentTrackedPackage`가 이미 매초 알고 있으므로,
> 그 값이 바뀔 때 같은 처리를 태우면 된다.

> 발견 1·7은 같은 곳에서 만난다 — 틱톡 미설치 상태로 카드를 탭하면 세션이 열리고, 사용자가 바로
> 돌아오면 발견 1 때문에 **0초 행이 그대로 기록**된다. 둘 다 단독으로는 사소하지만 겹치면
> "안 본 앱이 기록에 남는다"가 된다.
>
> **발견 10은 성격이 다르다.** 2026-08-10에 사장님이 "앱 지웠다 설치하면 계속 광고 보고 쓸 수
> 있는 거 아냐?"라고 지적해 **재설치 우회**는 막았다(기기 id + 서버 max 병합, 훌륭하게 동작한다).
> 그런데 **같은 목적을 훨씬 쉬운 방법으로 달성하는 경로**가 옆에 열려 있다 — 앱을 지울 필요도
> 없이 설정에서 날짜만 바꾸면 된다. 막은 문 옆에 더 큰 문이 열려 있는 셈이라, 재설치 방어를
> 넣은 그 판단 기준대로면 이것도 막아야 한다.
>
### 발견 10 상세 — 무엇이 뚫리고 무엇은 안 뚫리나 (2026-08-13 재확인으로 범위 축소)

> ⚠️ **처음 조사에서 "포커스 게이트까지 뚫린다"는 뉘앙스로 적었는데 그건 틀렸다.** 재확인 결과
> 게이트 자체는 제대로 막혀 있다. 아래가 정확한 경계다 — **잘못 넓게 적으면 멀쩡한 코드를
> 건드리게 되므로** 구분해 둔다.

| 대상 | 날짜 변경에 뚫리나 | 근거 |
|---|---|---|
| **출석 크레딧 +5/일** | 🔴 **완전히 뚫림** | `useAttendanceStore`가 `lastCheckInDate === today`만 본다. **서버 연동이 아예 없다**(`client.ts`에 attendance API 없음) → 날짜를 N번 바꾸면 5N 크레딧. 그 크레딧으로 연장하면 **광고를 아예 안 봐도 된다** |
| **광고 연장 하루 3회 한도** | 🟡 **대부분 막힘(`f7ae3df`)** | 서버 `sanitizeDate()`가 **서버 날짜와 ±1일 밖이면 서버 날짜로 클램프**한다. 서버 날짜 강제가 아니라 클램프인 건 맞는 선택이다 — 타임존 폭이 UTC-12~UTC+14로 26시간이라 강제하면 자정 경계에서 해외 사용자가 손해를 본다. **잔여**: ±1일이 허용되므로 `D-1`/`D`/`D+1` 세 날짜가 살아 있어 **하루 3회가 실질 9회**가 된다. 무제한 → 3배로 줄어든 것이고 0은 아니다(타임존을 지키려면 감수하는 트레이드오프) |
| Flip 크레딧 · 일일 보너스 | 🟡 리셋됨 | `useFlipStore`/`useDailyBonusStore` 둘 다 날짜 키 |
| 한도도달 팝업 횟수 | 🟢 무해 | `useLimitHitStore` — 표시용 카운터 |
| **포커스 세션 게이트(`timedOut`)** | ✅ **안 뚫림** | `useFocusSessionStore`의 저장 형태가 `{ endsAt, timedOut }`으로 **날짜 키가 없다.** `load()`는 날짜를 안 보고 그대로 복원하며, `endsAt`이 지났으면 `timedOut = true`를 세운다. 서버 병합도 `state.timedOut \|\| server.timedOut`(OR)이라 가짜 날짜가 준 `false`가 기존 `true`를 못 지운다. **이 스토어가 `todayKey()`를 쓰는 건 서버 행의 키로만이다** |

> **정리하면**: 게이트는 여전히 "광고를 보거나 크레딧을 써라"를 강제한다(설계대로다). 뚫리는 건
> **그 요구를 하루에 몇 번까지 허용하느냐는 상한**이다. 그런데 출석 크레딧이 서버 없이 날짜만으로
> 무한 적립되므로, 결과적으로 **광고를 한 번도 안 보고 무제한 연장**이 가능하다 — 상한이 없어진
> 것과 게이트가 없어진 것이 실질적으로 같아지는 지점이다.
>
> **재설치 방어와의 대비.** 2026-08-10에 "앱 지웠다 깔면 우회되는 거 아냐?"를 막을 때는
> 기기 id(SSAID/Keychain) + 서버 max 병합까지 제대로 넣었다. 같은 목적의 더 쉬운 경로가
> **설정 → 날짜 변경** 하나로 열려 있다.
>
> **고치는 방향**(참고): ① 서버가 클라 날짜를 검증한다 → ✅ **`f7ae3df`로 완료**(`sanitizeDate` ±1일
> 클램프, 테스트 2건 추가·30개 전부 통과). ② **출석 크레딧을 서버로 옮긴다** → ❌ **미완**.
> ③ 로컬에 "마지막으로 본 시각보다 과거면 무시"하는 단조 시계 → ⬜.
>
> 🔴 **①만으로는 끝나지 않는다.** 처음부터 "①+②가 최소 조합"이라고 적어 둔 이유가 그대로 남아
> 있다 — 크레딧은 연장에 광고 대신 쓸 수 있으므로, 출석 크레딧이 로컬 전용인 한 **날짜를 바꿔
> 크레딧을 모아 광고를 한 번도 안 보고 연장**하는 경로가 열려 있다. `client.ts`에 attendance
> API는 여전히 0건이다(2026-08-13 재확인).

# 검증 진행 현황

| | 2026-08-12 | 2026-08-13(확장 후) |
|---|---|---|
| 총 항목 | 약 300 | **666행** (R~Z 신설 약 320) |
| ✅ 실기기 검증 | 62 | **116** (🍎 Mac 3-3 + 🤖 08-13 자동넘김·versionCode 실측) |
| 🧪 코드 검증 | — | **102** |
| ❌ 실패/버그 | 3 | **결함 13건 중 12건 코드 수정 완료** — 남은 1건은 K14(구현 여부가 제품 결정). **발견 12만 실기기 검증 대기** |
| 🔍 의심/제약 | 6 | 27행 |
| ⬜ 미검증 | 나머지 | **391** |

> ⚠️ **2026-08-13 재확인에서 ✅ 5개를 🧪로 내렸다**(Z6·Z9~Z12). 근거가 커밋 메시지·코드였지
> 실기기가 아니었다. 숫자가 줄어든 게 아니라 **원래 그 숫자가 아니었던 것**이다.

> 숫자는 **행 수**다(한 결함이 여러 행에 걸리기도 한다). 재집계 명령:
> `for m in ✅ 🧪 ❌ 🔍 ⬜; do echo -n "$m "; grep -c "^|.*$m" QA_FULL_TEST.md; done`

**2026-08-13 확장에서 새로 생긴 검사 영역** — R(앱 업데이트/OTA·강제 게이트) · S(보관함) ·
T(손짓/스냅/플립/볼륨키/BT) · U(🍎 Pace Feed) · V(데이터 무결성) · W(장애 복구) ·
**X(릴리즈 게이트 런시트)** · **Y(중단·예외 70종)**. 전부 **코드에는 있는데 A~Q에 한 줄도 없던**
영역이다.

## 🚦 출시 판정 (2026-08-13 재분석)

> **판정: 아직 올리지 않는다.** X구획 PASS 규칙(X-A·X-C·X-E에 ❌가 하나라도 있으면 보류)에
> 걸리는 게 2건 남았다. 나머지는 **하루 사이에 상당히 정리됐다.**

### ✅ 이번에 해소된 것

| 항목 | 근거 |
|---|---|
| versionCode 11 → 12 | `f7ae3df` — `app.json` + `build.gradle` 둘 다 |
| 날짜 변경으로 광고 한도 무제한 | `f7ae3df` — `sanitizeDate()` ±1일 클램프(타임존 보존) |
| **긴 영상이 안 넘어감** — 실기기에서 117초 영상이 세션 내내 유지되던 것 | `MAX_SINGLE_VIDEO_MS = 90_000` over-stay 상한 |
| 광고 보고 오면 보던 영상이 잘림 | 복귀 시 `lastSwipeAtMs`/`videoStartedAtMs`/`lastFracAtMs` 리셋 |
| **유튜브 PIP + 틱톡 동시**에 엉뚱한 창을 읽고 조작 | `2a1cefc` — `bestTrackedWindow()`로 읽는 창·쏘는 창 통일 |

> 🔴🔴 **2026-08-13 오후 정정 — 이 판정은 틀렸다.** 아래 "차단 0건"은 **코드를 읽고** 내린 결론이었고,
> 실기기에 릴리즈 빌드를 깔아 4분 놔두자 **아무것도 안 봤는데 한도가 3분 깎였다**(발견 14·15).
> 제품의 존재 이유인 "시청 시간 집행"이 **설치 직후부터 틀린 값을 쓰고 있었다.**
> 이 문서가 그걸 못 잡은 이유는 명확하다 — Y63/Y67에 **항목으로 적어만 두고 ⬜로 남긴 채
> 실기기로 안 돌렸다.** 🧪를 아무리 쌓아도 이 결함은 안 나온다.
> **교훈: X구획을 "릴리즈 바이너리로 돌린다"고 적어만 두고 실제로 안 돌리면 X구획도 🧪일 뿐이다.**

### 🔴 코드 수정은 완료됐으나 — **실기기 검증 전에는 출시 판정 불가**

| 순서 | 할 일 | 근거 | 상태 |
|---|---|---|---|
| ~~1~~ | ~~eas.json 광고 스위치~~ | 발견 8 · X4/X8 | ✅ 해결 |
| ~~2~~ | ~~`build.gradle` versionName~~ | 발견 13 · X3 | ✅ 해결 |
| ~~3~~ | ~~앱 전환 시 세션 분리~~ | 발견 12 · Z1~Z5 · D21 | 🧪 **B안으로 수정 완료 — 실기기 검증만 남음** |

> **B안을 골랐다.** A안(복제)은 이 파일들이 반복해서 경고하는 사고 유형 그대로였고, C안(네이티브
> `currentTrackedPackage` 변화 시점에 분리)은 런처 직접 전환(Z3)까지 덮지만 범위가 커서 뒤로 미뤘다.
> 세션 종료 로직을 `services/sessionTeardown.ts`로 뽑아 `/overlay`와 `home.tsx`가 같이 쓴다.
>
> ⚠️ **아직 ✅가 아니다.** 실기기에서 유튜브 세션 중 틱톡 카드를 눌러 `viewing_sessions`가
> **두 행으로 갈라지고 각각 platform_app이 맞는지** 확인해야 한다. 이건 이 프로젝트에서 회귀가
> 가장 잦았던 경로라, 타입체크 통과는 근거가 못 된다(BAL 유예 초과·`resumeThirdPartyApp` 건 둘 다
> 코드는 멀쩡했다).
>
> **Z3(런처로 직접 전환)은 여전히 안 덮인다** — 카드를 안 거치고 홈 화면에서 틱톡을 직접 열면
> 세션은 유튜브 그대로다. C안이 필요해지는 지점이고, 별도 항목으로 남긴다.

### 🟡 그다음 (게이트를 막지는 않지만 매출·정확도에 붙음)

| 순서 | 할 일 | 근거 |
|---|---|---|
| ~~3~~ | ~~출석 크레딧 서버 이관~~ | ✅ **해결** — `serverToday` 필드로 신뢰 날짜 판정(발견 10) |
| ~~5~~ | ~~🤖 3초 미만 세션 가드~~ | ✅ **해결** — `sessionTeardown`(발견 1) |
| ~~7~~ | ~~dev 과금 차단 · `overlay_events` 정리~~ | ✅ **해결**(발견 2 · 11) |
| ~~8~~ | ~~`build.gradle` versionName~~ | ✅ **해결**(발견 13) |
| **1** | 앱 전환 시 **자동넘김 시계 리셋** — 광고 복귀에는 이미 넣은 그 리셋을 앱 전환에도 | Z17~Z21 |
| **2** | **Z3** — 카드를 안 거치고 런처로 직접 전환하는 경로의 세션 분리(C안) | Z3 · 발견 12 잔여 |
| **3** | K14(무료 보관 한도) — **구현할지 항목을 지울지는 제품 결정**이라 코드로 안 건드렸다 | 발견 3 · S10 |

> **K14를 왜 안 고쳤나.** 나머지는 "약속한 동작이 코드에 없다"는 결함이지만, 이건 **없는 기능을
> 새로 만드는 일**이고 무료/유료 경계를 바꾸는 매출 결정이다. 문서가 존재하지 않는 기능을 기술 중인
> 상태만 ❌로 정확히 남겨 두고, 만들지 지울지는 사장님 결정 사항으로 둔다.

> 발견 8·13을 같이 처리한 이유 — 둘 다 "빌드 산출물이 의도와 다른데 **파일만 보면 맞아 보이는**"
> 같은 유형이고, 둘 다 한 줄이며, 둘 다 **올린 뒤에는 되돌리기가 비싸다**(스토어에 한 번 나간
> versionCode/versionName은 재사용 불가). 그래서 이 둘은 X-A(빌드 **전** 정적 확인)에 두는 게 맞다.

> ⚠️ 미검증이 많은 것은 정상이다 — 이 문서는 한 번에 다 도는 문서가 아니라
> **영역별로 나눠 반복해서 도는 문서**다. 출시 직전에는 `QA_MATRIX.md`의 sanity만 돌린다.
>
> ⚠️ **🧪를 "검증됐다"고 읽지 말 것.** 위 "🧪는 무엇을 보증하나"대로, 그 코드에 기기에서
> 도달하는지는 아무것도 말해주지 않는다. 특히 **R20(강제 업데이트 차단)** 은 fail-open 경로가
> 전부 🧪라고 해서 안심할 항목이 아니다 — 차단이 실제로 걸리는 단 하나의 경로는 사람이 봐야 한다.

---

# 참고 — Y구획(중단·예외)의 외부 근거

Y구획은 이 앱의 코드에서만 뽑은 게 아니라, 모바일 QA의 인터럽트/예외 테스트 분류와 안드로이드
OEM의 실제 프로세스 종료 행태를 웹에서 조사해 뼈대로 삼았다(2026-08-13 조사).

- 인터럽트 테스트 3분류(기기 인터럽트 / 네트워크 인터럽트 / OS 인터럽트) — [Interrupt Testing for Mobile: Calls, Alerts, Network Drops](https://www.drizz.dev/post/interrupt-testing)
- 저평가되는 카테고리로 "인터럽트 처리 + 상태 복구(앱 kill 후 재실행)"를 지목 — [Mobile App Testing Checklist: 12 Categories with 150+ Test Items](https://www.testscenario.com/mobile-app-testing-checklist/)
- 릴리즈 전 체크리스트 구성 — [BrowserStack — Mobile App Testing Checklist](https://www.browserstack.com/guide/mobile-app-testing-checklist)
- 저배터리/저전력/백그라운드 제한/과열 조건 테스트 — [Mobile App Testing: The Complete 2026 Guide and Checklist](https://pie.inc/blog/mobile-app-testing-guide/)
- **삼성 "잠자는 앱"(3일 무활동 시 종료), OTA가 배터리 최적화 예외를 조용히 초기화** — [What Android OEMs do to background apps, and the 11 layers I built to survive it](https://dev.to/stoyan_minchev/what-android-oems-do-to-background-apps-and-the-11-layers-i-built-to-survive-it-28bb)
- OEM 배터리 공격에 포그라운드 서비스가 종료되는 사례와 대응 — [Keeping Android Services Alive Against OEM Battery Aggression](https://dev.to/haseebthedev0/keeping-android-services-alive-against-oem-battery-aggression-3n4h)
- Doze / App Standby 공식 동작 — [Optimize for Doze and App Standby — Android Developers](https://developer.android.com/training/monitoring-device-state/doze-standby)

> **조사에서 얻은 가장 중요한 한 줄**: *"에뮬레이터와 픽셀은 OEM 배터리 관리에 대해 아무것도
> 알려주지 않는다 — 처음부터 실제 OEM 기기로 테스트하라."* 우리는 삼성 1대뿐이라 **Y70이
> 구조적으로 미충족**이며, 이건 항목을 더 적어서 해결되는 종류가 아니다(기기가 필요하다).

---

# 2026-08-13 프로덕션 출시본(versionCode 13 / 1.0.4) 실기기 검증 기록

비공개 테스트 종료 → 프로덕션 트랙 출시본을 만들면서 돈 검증이다. **실기기(삼성 R3CN80S5GWW,
One UI / Android 16) + 로컬 gradle 릴리즈 빌드**로만 했다(EAS 미사용 — 유료라 쓰지 않는다).

## 이번에 잡은 것

### 1. 🔴 유튜브 영상이 20초에 강제로 넘어가던 회귀 (기본 동작 파손)
8/12에 틱톡용으로 넣은 "진행바 없는 영상 20초 폴백"이 **유튜브에도 걸렸다.**

```
17:07:38 RANGE_INFO none — rootPkg=com.google.android.youtube
17:07:46 AUTO_NEXT reason=no-progressbar elapsed=20073ms
17:07:48 VIDEO_ADVANCE looped-back total=179s   ← 잘린 건 3분짜리 영상
```

이건 8/3에 "유튜브는 재생위치를 못 읽는 구간이 상시라 타이머 폴백이 보던 영상을 끊는다"는
이유로 삭제했던 Tier 2를 더 짧은 주기로 되살린 것이었다(PaceAccessibilityService 1050~1062줄).

폴백을 유튜브에서 **통째로 빼자 반대쪽으로 부러졌다** — `RANGE_INFO none`이 4분간 52회 찍히는
동안 자동넘김이 한 번도 안 걸렸다(= "포커스 온인데 계속 같은 영상"). 그래서 **간격을 앱별로**
나눴다: 유튜브 90초(승인된 한 영상 체류 상한), 틱톡 20초.

| 검증 | 결과 |
|---|---|
| 유튜브 4분 연속 | `near-end` 4연속 — 32/33s · 49/50s · 47/48s · 37/38s (전부 종료 1초 전) |
| 유튜브 폴백 발사 | **0회** |
| 틱톡 동일 세션 | `no-progressbar` 20초 정상 발사(pkg=…trill), `frac-predict` 2회(70s·48s 영상, 종료 2.4초 전) |
| 차감 | tick 정상(60→…→28) |

### 2. 🔴 틱톡에서 검색 칩을 누르면 유튜브 결과가 뜨던 것
사장님 지시("검색은 우리 UI, 결과만 틱톡 화면")가 **반쪽만** 구현돼 있었다 — `submitSearch()`
(직접 입력·검색 버튼)에만 `isTikTokContext()` 분기가 있고 **프리셋 칩은 `runSearch()`를 직접**
불렀다. 칩도 같은 분기를 타게 하고, 넘길 땐 `p.label`을 쓴다(`p.query`엔 유튜브용 로케일
접미사 "쇼츠"가 붙어 있어 틱톡 검색엔 방해).

같이: 패널 제목이 틱톡에서도 "쇼츠 검색"이었다 → "틱톡 검색"/"Search TikTok".

| 검증 | 결과 |
|---|---|
| 틱톡 → P → Search | 제목 "틱톡 검색", 칩 "먹방" → `TikTok search opened via snssdk1233://` → 틱톡 자체 검색 결과(상위/동영상/사용자), 알약 유지 |
| 유튜브 → P → Search | 제목 "쇼츠 검색", 칩 "먹방" → 기존 우리 결과 목록 그대로(회귀 없음) |
| 메뉴 구성 | 유튜브 4개(HOT 포함) / 틱톡 3개(HOT 없음) — 의도대로 |

### 3. ⛔ 출시 사고 직전에 잡은 함정 — `.env`를 고쳐도 번들에 안 들어간다
Gradle의 `createBundleReleaseJsAndAssets`는 입력으로 **JS 소스만** 본다. `.env`는 번들 시점에
@expo/env가 읽어 인라인하는데, 그 파일이 바뀌어도 태스크는 UP-TO-DATE로 건너뛴다.

```
.env 수정   17:51   (EXPO_PUBLIC_ANDROID_REAL_ADS=true)
AAB 빌드    17:54   → 안에 든 번들은 16:55 것
실기기 화면        "테스트 광고" 배지 그대로
```

그대로 올렸으면 **프로덕션 수익 0**이었다. 반대 방향(테스트 트랙에 실광고)으로 틀리면 AdMob
계정 정지다. 절차를 build.gradle에 박아뒀다:

```
rm -rf android/app/build/generated/assets/react
./gradlew bundleRelease assembleRelease
# 그리고 릴리즈 APK를 실기기에 깔아 배너에 "테스트 광고" 배지가 없는지 눈으로 확인
```

## 같이 확인한 것(회귀 없음)

| 항목 | 근거 |
|---|---|
| 실광고 전환 | "테스트 광고" 배지 사라지고 AdChoices ⓘ 붙은 한국 광고주 광고 표시(**클릭 금지** — 검증은 노출만) |
| 버전 | `versionCode=13 versionName=1.0.4` (dumpsys package) |
| 서명 | AAB 서명 SHA256 = 업로드 키 지문 일치(`7B:77:73:68:…:43`) → Play 거부 없음 |
| AAB 내용물 | 번들이 방금 생성분과 바이트 동일(cmp), 실광고 단위 포함, "틱톡 검색" 문자열 dex에 포함 |
| 앱별 사용시간 | 분석 탭 "YouTube 15m·88% / TikTok 2m·12%" |
| 접근성 배너 | 실제로 언바인드된 동안만 뜨고, 다시 켜면 사라짐(양방향) |
| 알약 | 유튜브/틱톡 양쪽에서 "N m left / FOCUS N m / P" 표시 유지 |
| Shorts HOT | 패널·탭(All/Music/Gaming/Comedy/Entertainment)·목록 정상 |

## 남은 것(출시를 막지는 않음)

- **KR HOT 목록에 스페인어 항목 유입** — 6개 중 1개(`Rescaté a Este Pequeño Oso Polar…`).
  기능 결함은 아니고 추천 품질 문제.
- **수면감지 오탐 없음(K8)** — 10분 무입력이 필요해 이번 라운드에서 못 돌렸다.
- **Y70(OEM 배터리 공격)** — 삼성 1대뿐이라 구조적으로 미충족(위 참고 섹션).

---

# 🍎 맥 세션에 넘김 — iOS 피드: 검색 결과에서 영상 고르면 로딩 5초+

**사장님 실기기 재현(2026-08-13 밤, 아이폰)**: 포커스 온 상태에서 "양다일" 검색 → 결과
리스트에서 곡 선택 → **로딩 5초 이상**.

## 원인(코드로 확정, 윈도우 세션에서 정적 분석)

`src/app/feed/index.tsx:819`
```tsx
<YouTubeShortsPlayer key={forcedVideoId ?? current!.videoId} … />
```

검색 결과 탭 → `playInFeed()` → `jumpToVideo(id)` → `setForcedVideoId(id)` → **key 변경 →
WKWebView 통째로 파괴 후 재생성**. 그리고 이 플레이어는 우리가 만든 재생기가 아니라
**실제 `https://www.youtube.com/shorts/{id}` 페이지를 띄우는 WebView**다
(`YouTubeShortsPlayer.ios.tsx:638`). 즉 한 번 고를 때마다:

1. WKWebView + 웹 콘텐츠 프로세스 새로 생성
2. youtube.com 모바일웹 전체 로드(무거움)
3. 우리 주입 스크립트 재실행 → `<video>` attach 대기 → onReady

여기서 5초가 나온다. 이미 197줄 주석이 "리스트 재생은 항목마다 리로드=리마운트"라고 인정하고
있고, 8/9에 사장님이 지적한 "로딩이 왜이리 오래 걸려"에 대해 그때는 **스피너만 추가**했다
(170~174줄) — 체감만 손대고 실제 시간은 그대로 뒀다.

## 왜 윈도우 세션이 안 고쳤나

- `/feed`는 **iOS 전용**이다(`home.tsx:459` — 안드로이드는 실제 유튜브 앱을 띄운다).
  → 오늘 밤 **안드로이드 프로덕션 출시에는 영향이 없다.**
- 이 컴포넌트는 손댈 때마다 미묘하게 깨진 이력이 길다(무음 스위치, 첫 영상만 소리남,
  옛 프레임 잔상, forcedVideoId 렌더 게이트…). 윈도우에서는 iOS 빌드·검증이 **불가능**하므로
  검증 없이 출시 당일 건드리지 않는다.

## 제안하는 수정 방향(맥이 구현·검증)

1. **WebView 재생성을 없앤다.** `key` 교체 대신 마운트를 유지하고, imperative handle에
   `loadVideo(id)`를 추가해 **페이지 안에서** 이동시킨다
   (`useImperativeHandle`에 `advance/previous/setMuted`가 이미 있는 자리).
   - 절약되는 것: WKWebView + 웹 콘텐츠 프로세스 생성, 주입 스크립트 재파싱, HTTP 캐시 유실.
   - ⚠️ `navVideoId`가 swipe 모드에서 `firstVideoIdRef.current`로 고정돼 있어(621줄) 현재는
     리마운트가 영상 교체의 **유일한 수단**이다. 이 전제를 같이 풀어야 한다.
   - ⚠️ `forcedVideoId`는 렌더 게이트(817줄)와 복귀 경로(855줄)에도 쓰인다 — 같이 봐야 한다.
2. 남는 비용은 youtube.com 페이지 로드 자체다. 1번만으로 "즉시"는 안 된다 —
   **예상 개선은 5초 → 2~3초 수준**. 그 이상을 원하면 프리로드(이미 `preload` prop 존재)를
   검색 결과 상위 1~2개에 미리 걸어두는 방법이 있다.
3. 어느 쪽이든 **실기기에서 초 단위로 재보고**(선택 탭 → onReady 로그) 숫자를 남길 것.
