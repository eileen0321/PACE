# PACE — 회귀 테스트 체크리스트 (2026-08-10 하루치 수정 전수)

> **왜 이 문서를 만들었나.** 2026-08-10 하루에만 버그가 10건 가까이 나왔고, 그중 상당수가 **예전에
> 고쳤던 것의 재발**이었다. "제한을 판정하는 상태가 화면/컴포넌트 생명주기에 묶여 우회 가능해지는"
> 패턴만 해도 Android(`b64b6d8`) → iOS(맥, `sessionTimedOutModule`) → 다시 iOS/Android(`982bbf1`)로
> **세 번** 따로 발견됐다. 같은 날 "광고 연장이 5분이 아니라 10분"도 맥과 Windows가 **각자 따로**
> 고쳐서 병합 충돌이 났다(`48b6d3f`).
>
> 재발을 잡을 장치가 지금까지 하나도 없었다. 그래서 두 가지를 같이 만든다:
>   1. **자동화** — 순수 로직은 `./mvnw -o test`가 매번 잡는다(아래 §0).
>   2. **이 문서** — 네이티브/기기/OS에 붙어 있어 자동화가 원리적으로 불가능한 것들의 수동 절차.
>
> ⚠️ **이 문서는 Windows 세션과 맥 세션이 같이 쓴다.** iOS 항목은 맥이 실행한다. 항목마다
> 플랫폼을 명시했으니 **자기 플랫폼 것만** 돌리고, 결과는 `PACE_PROJECT_MANAGEMENT.md`에 날짜와
> 함께 남겨라(이 문서의 `상태` 칸은 비워둔 채 커밋한다 — 아직 아무도 안 돌렸기 때문이다).

**플랫폼 표기**: `🤖 Android 전용` / `🍎 iOS 전용(맥 세션)` / `🌐 공통(양쪽 각각 확인)` / `☁️ 서버`

**상태 표기**: `PASS` 실기기 확인 / `FAIL` 재현됨 / `BLOCKED` 환경이 안 돼서 못 함 / (빈칸) 미실행

---

## 0. 먼저 — 자동화된 것은 사람이 다시 하지 마라

```bash
cd backend && ./mvnw -o test
```

**2026-08-10 실행 결과: 28건 전부 통과** (`FocusAllowanceServiceTest` 13 + `RevenueCatServiceTest` 8 +
`ShortsHotServiceTest` 7).

| 테스트 | 무엇을 지키나 | 원 이슈 |
|---|---|---|
| `FocusAllowanceServiceTest` | 서버 허용량 **병합 규칙**(카운트 max / timedOut OR / 지난 마감시각은 timedOut 확정 / 연장이면 해제 / 마감시각을 앞당기지 못함) + 7일 보관 cutoff | `40ec367`, `f38b136` |
| `ShortsHotServiceTest` | 카테고리가 비면 `all`로 대체 응답(빈 탭 방지), 국가 정규화(KR/JP/US 외 → US) | `2803cbf`, `a55b1d6` |
| `RevenueCatServiceTest` | (기존) 구독 grant/revoke 상태 머신 | - |

> `FocusAllowanceServiceTest`는 **재설치 차단의 마지막 보증**이다. 병합을 덮어쓰기로 되돌리는
> 회귀를 실제로 잡는지 확인했다 — `Math.max`/`||`를 단순 대입으로 바꿔보니 정확히 그 3개 테스트가
> 실패하고, 되돌리니 다시 통과했다(코드는 원상복구됨).
>
> ⚠️ **JS/RN 쪽은 테스트가 0개다**(jest 미설정). `useFocusSessionStore`의 `extend()`/`load()`처럼
> 자동화 가치가 높은 순수 로직이 아직 사람 손에 달려 있다 — §6 참고.

---

## 1. 🤖 Android 전용 — 보상형 광고와 이어서재생(HOT chain)

이 구역 전체의 로그 준비:

```bash
adb logcat -c
adb logcat -s PaceRewardedAd:I PaceAccessibility:I PaceOverlay:I PaceOverlayService:I
```

⚠️ 태그가 **네 개**다. `CHAIN *`은 `PaceAccessibility`, `HOT chain advance`는 `PaceOverlayService`,
`pill SHOW/HIDE`는 `PaceOverlay`에서 나온다 — 하나라도 빼면 인과가 끊겨서 "발동 안 했다"고 오판하게 된다.

### A1. 보상형 광고가 1~2초 만에 사라지지 않는다

- **플랫폼**: 🤖 Android 전용
- **원 이슈**: `b8d9793` (사장님: "광고보기 누르면 광고가 뜨고 지나가버린다" — 시도 6회 전부 재현, 보상 0회)
- **왜 재발하기 쉬운가**: 원인이 광고 SDK가 아니라 **우리 앱의 HOT 이어서재생 감시**였다. 감시가
  광고 화면을 "제목이 바뀐 유튜브"로 오인해 다음 쇼츠를 딥링크로 열었고, 광고 태스크가 뒤로 밀려
  `noHistory` 껍데기가 finish됐다. 광고 쪽 코드를 아무리 봐도 안 보이는 종류의 결함이다.

**재현 절차**
1. 무료 계정(프리미엄 아님)으로 시작. `adb logcat -c`로 로그를 비운다.
2. Pace 실행 → P메뉴 → **HOT** → 아무 카테고리에서 아무 영상이나 탭한다.
   (⚠️ 이 단계를 **반드시** 해야 한다. 이어서재생 감시가 켜져 있는 상태가 이 버그의 전제다.
   로그에 `CHAIN start baseline=...`이 찍히는 것으로 확인.)
3. 유튜브에서 **쇼츠를 2~3개 스와이프**해 감시가 실제로 돌고 있게 둔다(`CHAIN fire callback` /
   `HOT chain advance ... remaining=N`이 찍힌다).
4. 그 상태로 Focus Session이 타임아웃될 때까지 둔다 → 오버레이 배지 탭 → 재개 시도 →
   **"광고 보고 5분 더"** 버튼을 누른다.
5. 광고를 **끝까지** 시청한다.

**기대 결과 (합격 기준)**
- 광고가 **끝까지 재생되고 보상이 지급된다**. 1~2초 만에 사라지지 않는다.
- 광고 시청 중 화면이 유튜브/쇼츠로 **전환되지 않는다**.
- 광고가 닫힌 뒤 Focus Session이 **정확히 5분** 늘어난다(A5/C1과 겹침).

**확인 방법 (logcat)**
- `PaceRewardedAd: using preloaded ad`(또는 로드 완료) **시각부터 광고 닫힘까지 구간에
  `CHAIN fire callback`이 0건**이어야 한다. 1건이라도 있으면 FAIL.
- 같은 구간에 `HOT chain advance next=... remaining=` 도 **0건**.
- 광고가 닫힌 뒤에는 `CHAIN resync new baseline='...'`가 **한 번** 찍히고(기준 다시 잡기),
  그 리싱크 폴에서는 `CHAIN fire`가 없어야 한다 — 돌아오자마자 한 칸 건너뛰면 FAIL.
- `pill SHOW fg=com.google.android.youtube`가 광고 도중에 찍히면 FAIL(유튜브가 광고 위로 올라온 것).

**⚠️ 함정**: `adShowing` 플래그는 껍데기 `onCreate`에서 켜고 **`onDestroy`에서만** 꺼야 한다.
보상/닫힘 콜백은 광고가 아직 떠 있을 때 오므로 거기서 풀면 이 증상이 그대로 재현된다.

---

### A2. 이어서재생이 감시 대상 앱 밖(홈/런처)에서 발동하지 않는다

- **플랫폼**: 🤖 Android 전용
- **원 이슈**: `87899b2` (커밋 후반부. 실기기: `22:41:46 CHAIN fire` → `22:41:48 usage=com.sec.android.app.launcher`)
- **왜 재발하기 쉬운가**: `trackedAppRootNode()`의 `rootInActiveWindow` 폴백이 "감시 대상 앱을 못
  찾으면 지금 활성 창이라도 쓴다"였다. 그래서 **런처 위젯 글자**("날씨. 이동하려면 두 번 누른 후
  움직이세요.")를 새 영상 제목으로 읽고 홈에 있던 사용자를 유튜브로 끌고 들어갔다. 폴백을 "관대하게"
  고치려는 다음 사람이 똑같이 되돌리기 쉬운 한 줄이다.

**재현 절차**
1. P메뉴 → HOT → 영상 탭(이어서재생 큐가 2개 이상 남게 — `remaining=` 값으로 확인).
2. 유튜브에서 1~2개 스와이프해 감시가 도는 것을 확인.
3. **홈 버튼**으로 런처로 나간다. 그대로 **60초 이상** 아무것도 하지 않는다.
   (감시 폴링은 1.5초 간격이라 60초면 약 40회 판정 기회가 있다.)
4. 이어서 **다른 앱**(설정 등)으로도 들어가 30초 둔다.

**기대 결과**
- 홈/다른 앱에 있는 동안 **유튜브가 저절로 앞으로 나오지 않는다**. 화면이 안 바뀐다.

**확인 방법 (logcat)**
- 3~4단계 구간에 `CHAIN fire callback`이 **0건**. 1건이라도 있으면 FAIL.
- 대신 `CHAIN resync new baseline='null'`이 반복해서 찍히는 것이 정상이다(감시 대상 앱이 없어
  판정을 쉬고 리싱크로 넘긴 것 — 2026-08-10 실기기 검증에서 `×4` 확인된 그 모습).
- `pill HIDE ... usage=com.sec.android.app.launcher` 뒤에 `CHAIN fire`가 오면 정확히 그 버그다.

---

### A3. 이어서재생이 Pace 자기 화면을 유튜브로 오인하지 않는다

- **플랫폼**: 🤖 Android 전용
- **원 이슈**: `b8d9793` (A1의 원인 중 절반 — 폴백이 **우리 앱 창까지** 감시 대상인 양 돌려줬다)

**재현 절차**
1. P메뉴 → HOT → 영상 탭(큐 2개 이상 남김).
2. 오버레이 P메뉴 → **"앱으로"**(Pace 본체 화면으로 복귀) → 60초 이상 머문다.
3. Pace 안에서 탭을 몇 번 옮겨 화면 글자가 실제로 바뀌게 한다(Home ↔ Stats ↔ Settings).

**기대 결과**
- Pace 화면 안에서 아무리 화면을 바꿔도 **유튜브가 저절로 열리지 않는다**.

**확인 방법 (logcat)**
- 해당 구간 `CHAIN fire callback` **0건**. `pill ... self=true` 상태에서 fire가 나면 FAIL.

---

### A4. 이어서재생 감시가 큐를 다 쓰면 멈춘다 (오래 살아남지 않는다)

- **플랫폼**: 🤖 Android 전용
- **원 이슈**: `b8d9793`에서 드러난 사실 — 20:46에 시작된 감시가 **46분 뒤 21:32까지** 그대로 돌고
  있었다(`remaining=17`). 사용자는 HOT을 건드린 기억도 없이 "광고보기가 그냥 안 되는" 것으로 겪는다.
  A1이 표면 증상이라면 이건 **폭발 반경**이다 — 감시가 오래 살수록 A1/A2/A3의 재현 확률이 올라간다.
- **관련 회귀 이력**: 2026-08-07 — 마지막 항목 콜백이 `stopFavoriteChainWatch()`를 부른 직후
  러너블이 **자기 자신을 다시 예약**해 정지가 무력화됐다. 같은 자리에서 두 번째다.

**재현 절차**
1. HOT에서 영상 탭 → 로그의 `remaining=N` 값을 적어둔다.
2. 유튜브에서 **N번 이상** 스와이프해 큐를 끝까지 소진시킨다.
3. 소진 후 5분간 방치한다.

**기대 결과**
- 큐가 비는 순간 감시가 멈춘다. 이후 접근성 트리를 계속 읽지 않는다.

**확인 방법 (logcat)**
- 큐 소진 시점에 `CHAIN stop`이 **찍힌다**.
- 그 뒤 5분간 `CHAIN resync` / `CHAIN fire`가 **0건**(계속 찍히면 폴링이 되살아난 것 = FAIL).

---

### A5. 🤖 timedOut 플래그가 서비스 프로세스 재시작을 견딘다

- **플랫폼**: 🤖 Android 전용
- **원 이슈**: `982bbf1` (안드 `focusSessionTimedOutPending`이 메모리에만 있어, 서비스 프로세스가
  죽었다 살아나면 배지 한 번에 **광고 없이 10분**이 나갔다 — START_STICKY 복구/스토어 업데이트/
  OEM 권한 회수로 실제로 일어나는 경로다)

**재현 절차**
1. 무료 계정으로 Focus Session을 켜고 **타임아웃될 때까지 둔다**(자동종료 토스트 확인).
2. 오버레이 서비스를 강제로 죽인다:
   ```bash
   adb shell am force-stop com.strides7.pace
   ```
3. Pace를 다시 실행하고 유튜브로 이동해 오버레이 배지를 띄운다.
4. **배지를 탭**해 Focus Session을 다시 켜려고 시도한다.

**기대 결과**
- **광고 시청 모달이 뜬다**(게이트가 살아있다). 광고 없이 그냥 10분이 켜지면 FAIL.

**확인 방법 (prefs 직접 확인 — 가장 확실하다)**
```bash
adb shell "run-as com.strides7.pace cat /data/data/com.strides7.pace/shared_prefs/pace_overlay.xml" \
  | grep -E "focus_session_timed_out_pending|focus_session_deadline_at_ms"
```
- 2단계 직후에도 `focus_session_timed_out_pending`이 **`true`**로 남아 있어야 한다.
- ⚠️ `run-as`는 debuggable 빌드에서만 된다. Release 빌드로 검증 중이면 이 명령은 건너뛰고 위 UI
  기준(광고 모달이 뜨는가)으로만 판정한다.

---

## 2. 🍎 iOS 전용 (맥 세션이 실행)

> **맥 세션 안내** — iOS는 Release 빌드 실기기 검증이 관행이라 RN `console.log`를 못 본다.
> 그래서 iOS 항목은 **화면에 보이는 숫자/문구**와 **서버 응답**으로 판정하게 썼다.
> 로그가 꼭 필요하면 Xcode → Window → Devices and Simulators → 기기 선택 → *Open Console*
> (프로세스 필터 `Pace`). 서버 확인이 필요한 항목은 §4의 `curl` 절차를 쓴다.

### B1. 화면을 나갔다 와도 광고 게이트가 풀리지 않는다

- **플랫폼**: 🍎 iOS 전용
- **원 이슈**: `982bbf1` (그 전 맥 수정 `sessionTimedOutModule`은 `48b6d3f`에서 폐기됨 — 지금 진실은
  `src/store/useFocusSessionStore.ts`)
- **왜 재발하기 쉬운가**: 원래 `sessionTimedOutRef`가 컴포넌트 `useRef`라 P메뉴 "앱으로"가
  `router.back()`으로 피드를 언마운트하면 새 인스턴스에서 `false`로 리셋됐다. **화면을 나갔다 오는
  것만으로 무료 10분**이 나갔다. 이 상태를 컴포넌트로 되돌리는 순간 그대로 재발한다.

**재현 절차**
1. 무료 계정. Focus Session을 켜고 **타임아웃될 때까지 둔다**(자동종료 토스트가 뜬다).
2. P메뉴 → **"앱으로"** → 피드 화면을 벗어난다.
3. 다시 피드로 들어간다.
4. Focus를 **ON**으로 토글한다.

**기대 결과**
- **광고 시청 유도 모달이 뜬다.** 모달 없이 그냥 켜지면 FAIL(이게 사장님이 재현한 그 우회로다).

---

### B2. 앱을 완전히 죽였다 켜도 게이트와 남은 시간이 살아있다

- **플랫폼**: 🍎 iOS 전용 (같은 항목의 Android판은 A5)
- **원 이슈**: `48b6d3f` (사장님: **"애플 앱 죽였다가 다시 들어와서 포커스 온하면 광고 3회 봤는데도
  10분 준다"** — 이 재현이 `pendingExtendMinutesRef`(메모리) 대신 영속 스토어를 채택한 결정적 근거였다)

**재현 절차 (2가지를 다 본다)**
- **(a) 타임아웃 상태의 생존**
  1. Focus Session을 타임아웃까지 둔다.
  2. 앱 스위처에서 Pace를 **위로 밀어 완전히 종료**한다.
  3. 다시 실행 → 피드 → Focus **ON**.
  - **기대**: 광고 모달이 뜬다. 그냥 10분이 켜지면 FAIL.
- **(b) 남은 시간의 생존**
  1. Focus Session을 켜고 **3분쯤 쓴다**(타이머가 예: 10:00 → 07:00).
  2. 앱을 완전히 종료 → 재실행 → 피드.
  - **기대**: 타이머가 **07:00 근처에서 이어진다**. 10:00으로 리셋되면 FAIL.

---

### B3. 재설치해도 같은 게스트 id로 돌아온다 (Keychain)

- **플랫폼**: 🍎 iOS 전용
- **원 이슈**: `d4e004d` (IDFV는 같은 개발사 앱을 전부 지우면 초기화돼 못 쓴다 → Keychain)
- **⚠️ 중요한 전제**: 기존 사용자는 이번 업데이트만으로는 **아무것도 안 바뀐다**(AsyncStorage 값을
  그대로 쓰고 Keychain에 백필). 효과는 **그 다음 재설치부터** 생긴다. 그래서 아래 절차는 반드시
  **삭제 → 설치 → (여기서 id가 Keychain에 심어짐) → 다시 삭제 → 설치** 순서여야 한다.

**재현 절차**
1. 기기에서 Pace를 **삭제**한다.
2. Release 빌드를 설치하고 **한 번 실행**한다(여기서 게스트 id가 Keychain에 저장된다).
3. 무료 Focus를 **광고로 1회 이상 연장**한다(서버에 기록이 남게).
4. Pace를 **다시 삭제**한다.
5. 같은 빌드를 **다시 설치**하고 실행한다.

**기대 결과**
- 5단계 직후 서버가 3단계의 기록(`adExtendCount ≥ 1`)을 **그대로 알고 있다**.
- 즉 재설치로 무료 허용량이 리셋되지 않는다.

**확인 방법**: §4의 `curl GET /focus-allowance` 절차 — 4단계 전과 5단계 후의 응답 `adExtendCount`가
**같아야** 한다. 5단계 후 0으로 돌아가면 FAIL(= 새 게스트 계정이 생긴 것).

**⚠️ 알려진 한계 (버그 아님)**: 기기 전체 초기화, 그리고 **암호화되지 않은 백업 복원**에서는
Keychain도 같이 날아간다. 이건 Apple 설계상 그렇고 우리가 못 막는다 — FAIL로 적지 마라.

---

### B4. iOS 연장이 정확히 5분이다 (10분이 아니다)

- **플랫폼**: 🍎 iOS 전용 (Android는 원래 5분이었다 — 이건 iOS만의 결함이었다)
- **원 이슈**: `c542d25` → `982bbf1` → `48b6d3f`. 같은 버그를 맥과 Windows가 각각 고쳐 충돌했고
  영속 스토어 쪽(`useFocusSessionStore.extend`)이 채택됐다.
- **왜 재발하기 쉬운가**: `FocusSessionExtendModal`이 `onExtend(minutes)`로 정확히 5를 넘기는데
  받는 쪽이 그 인자를 **버리고** `setIsAutoMode(true)`만 부르면, 세션-시작 effect가 설정값(기본
  10분)으로 통째로 새 세션을 켠다. 우연히 원래 세션 길이와 비슷해 보여서 오래 안 걸렸다.
  **크레딧 5개를 쓴 사용자에게 10분을 준** 상태였다.

**재현 절차**
1. 설정에서 Focus Session 길이가 **10분**(기본값)인지 확인한다.
2. Focus Session을 켜고 **타임아웃까지** 둔다.
3. 모달에서 **"광고 보고 5분 더"** → 광고를 끝까지 본다.
4. 광고가 닫힌 직후 타이머 숫자를 읽는다.
5. 같은 절차를 **크레딧 사용** 버튼으로도 한 번 더 한다.

**기대 결과**
- 4단계에서 타이머가 **05:00**에서 시작한다. **10:00이면 FAIL.**
- 토스트 문구("+5m")와 실제 타이머가 **일치**한다.
- 5단계(크레딧)도 동일하게 5분.

**추가 (연장은 "다시 시작"이 아니라 "더하기"다)**
6. 세션이 **남아 있는 상태**(예: 04:00 남음)에서 크레딧으로 연장한다.
- **기대**: 타이머가 **09:00 근처**(4 + 5)가 된다. **05:00으로 리셋되면 FAIL** — `extend()`가
  남은 시간에 더하지 않고 다시 시작한 것이다.

---

## 3. 🌐 공통 — 양 플랫폼에서 각각 확인

### C1. 하루 3회 광고 한도가 실제로 막힌다

- **플랫폼**: 🌐 공통 (Android/iOS 각각)
- **원 이슈**: `40ec367` (한도 자체는 그 전부터 있었으나, 서버 병합이 붙으면서 판정 경로가 바뀌었다)

**재현 절차**
1. 무료 계정. 하루 안에 **광고로 3회 연장**한다(타임아웃 → 광고 → 5분 → 반복).
2. **4번째** 타임아웃 후 연장을 시도한다.

**기대 결과**
- 4번째에는 광고 버튼이 **비활성/미노출**이고, 광고를 볼 수 없다. 4번째도 광고가 나가면 FAIL.
- (크레딧 연장은 별개 한도다 — 크레딧이 있으면 그건 계속 되는 게 정상이다.)

---

### C2. 재설치해도 서버 허용량이 살아있다

- **플랫폼**: 🌐 공통 — 🤖는 SSAID(`6d932ec`), 🍎는 Keychain(`d4e004d`)이 앵커라 **각각** 확인해야 한다
- **원 이슈**: `40ec367` + `6d932ec` + `d4e004d`
- **⚠️ iOS는 B3의 "두 번 설치" 전제가 그대로 적용된다.** Android는 SSAID가 OS 값이라 첫 재설치부터 바로 효과가 있다.

**재현 절차**
1. 무료 계정으로 광고 연장을 **2회** 한 뒤, 세션을 **타임아웃까지** 둔다.
2. §4의 `curl`로 서버 값을 읽어 적어둔다(`adExtendCount=2`, `timedOut=true`).
3. 앱을 **삭제**한다.
4. 같은 빌드를 **다시 설치**하고 실행한다(로그인하지 말 것 — **게스트 상태에서 막히는지**가 핵심이다).
5. Focus를 **ON** 해본다.

**기대 결과**
- 5단계에서 **광고 모달이 뜬다**(무료 10분이 그냥 나가면 FAIL — 이게 사장님이 지적한 무한 반복 경로다).
- 남은 광고 횟수가 **1회**다(3 - 2). 3회로 돌아가 있으면 FAIL.
- §4 `curl`로 재설치 후에도 `adExtendCount`가 **2 이상**임을 확인한다(0이면 게스트 id가 새로 생긴 것 =
  기기 앵커 실패).

---

### C3. 오프라인에서도 정상 사용자의 연장이 막히지 않는다 (fail-open)

- **플랫폼**: 🌐 공통
- **원 이슈**: `40ec367` — 서버 호출은 **전부 실패를 삼킨다**. 이건 의도된 설계다.
- **왜 확인하나**: 나중에 "서버 판정을 강하게 하자"며 `await`에 예외를 던지게 바꾸면, **비행기 안의
  정상 사용자가 Focus를 못 쓰게 된다**. 그 회귀를 잡는 항목이다.

**재현 절차**
1. **비행기모드 ON**.
2. Focus Session을 켠다 → 타임아웃까지 둔다 → 크레딧으로 연장한다.
3. 비행기모드 OFF → 앱을 완전히 종료했다 재실행한다.

**기대 결과**
- 2단계가 **정상 동작한다**(오프라인이라고 세션이 멈추거나 앱이 에러를 내면 FAIL).
- 3단계 이후 서버 값이 로컬을 따라잡는다(§4 `curl`로 `timedOut`/`adExtendCount` 확인) — 비행기모드
  우회는 다음 온라인 sync의 max 병합에서 회수되는 것이 설계다.

---

### C4. 기존 사용자는 업데이트해도 id가 바뀌지 않는다

- **플랫폼**: 🌐 공통
- **원 이슈**: `6d932ec` / `d4e004d`의 ⚠️ 조항 — 이미 저장된 id가 있으면 **그대로 쓴다.** 안 그러면
  업데이트 순간 전원이 새 게스트 계정으로 갈아타 **기존 설정/통계/구독 연결이 끊긴다.**
  C2를 고치려다 이쪽을 깨는 것이 정확히 가장 위험한 회귀다.

**재현 절차**
1. **이전 버전**이 설치돼 있고 사용 이력이 있는 기기를 준비한다(삭제하지 말 것 — 덮어쓰기 설치).
2. 통계 탭의 누적 기록/설정값/크레딧 잔량을 적어둔다.
3. 새 빌드를 **덮어쓰기 설치**한다(Android: `adb install -r`, iOS: 같은 서명으로 재설치).

**기대 결과**
- 통계/설정/크레딧이 **그대로 남아 있다**. 초기화됐으면 FAIL.
- 프리미엄 사용자라면 **구독 상태가 유지**된다.

---

### C5. 쇼츠 검색(P메뉴 Search)이 양쪽에서 같게 동작한다

- **플랫폼**: 🌐 공통 (Android `dd4dd06`, iOS 이식 `cf4d395`)
- **왜 재발하기 쉬운가**: 검색은 Railway가 아니라 **Vercel 프록시**를 친다. 두 호스트가 달라
  `cacheProxyBaseUrl`을 따로 밀어주는데, 이 배선이 끊기면 **한 플랫폼에서만** 조용히 죽는다.

**재현 절차**
1. P메뉴 → **Search**.
2. 국가별 프리셋 칩이 뜨는지 본다(한국 기기면 축구/야구/먹방/예능/음악/댄스…).
3. **"축구"** 칩을 탭한다.
4. 결과에서 아무 영상이나 탭한다.

**기대 결과**
- 2단계: 프리셋이 **현지 언어로** 뜬다(빈 목록이면 FAIL — 프록시 URL 배선 문제).
- 3단계: 결과가 **전부 진짜 축구 쇼츠**다.
- 4단계: HOT과 **같은 규칙으로 이어서재생**된다(즐겨찾기만 단일 재생이다 — 헷갈리지 말 것).
- ⚠️ 자유 검색 입력창은 **아직 없다**(프리셋만). 없다고 FAIL 적지 마라 — 미구현이다.

---

## 4. ☁️ 서버 — 배포 후 확인

### 공통 준비: 서버 허용량 직접 읽기

여러 항목이 이걸 쓴다. `focus-allowance`는 인증이 필요하므로 앱의 JWT가 있어야 한다.

```bash
BASE=https://pace-backend-production-2e52.up.railway.app
TOKEN=<앱이 들고 있는 accessToken — AsyncStorage 키 `pace_auth_token`>
# 꺼내는 법(둘 중 되는 쪽):
#   🤖 debuggable 빌드: adb shell "run-as com.strides7.pace cat \
#        /data/data/com.strides7.pace/databases/RKStorage" | strings | grep -A2 pace_auth_token
#   🌐 Release 빌드라 못 꺼내면: 검증용으로 잠깐 dev 빌드를 띄워 같은 계정으로 로그인해 얻는다.
#      게스트 계정은 기기마다 달라서 다른 기기의 토큰을 쓰면 **엉뚱한 사용자**를 보게 된다 —
#      반드시 검증 중인 그 기기의 토큰이어야 한다.
DATE=$(date +%F)     # 클라이언트 로컬 날짜를 쓴다 — 기기와 같은 타임존에서 실행할 것

curl -s -H "Authorization: Bearer $TOKEN" "$BASE/focus-allowance?date=$DATE"
# → {"date":"2026-08-10","adExtendCount":2,"timedOut":true,"sessionEndsAt":null}
```

⚠️ **날짜가 핵심이다.** 이 API는 **클라이언트 로컬 날짜**로 키를 잡는다(서버 타임존으로 계산하면
자정 경계에서 사용자가 손해를 본다). 맥/Windows의 타임존이 기기와 다르면 **빈 값이 나오고
"기록이 없다"고 오판하게 된다** — `date` 파라미터를 반드시 명시할 것.

---

### D1. HOT의 music 탭이 all과 다른 목록이다 (채널 발견이 살아있다)

- **플랫폼**: ☁️ 서버 (앱 배포 불필요 — 확인은 앱에서 눈으로)
- **원 이슈**: `87899b2` (`publishedAfter`가 **전 카테고리**의 채널 발견을 0건으로 만들고 있었다) +
  `ee96674` (채널 명단을 `categoryId`로 자동 적재)
- **⚠️ 미검증 상태**: `87899b2` 커밋 본문에 "이 백엔드 수정은 배포·실측 안 됐다"고 명시돼 있다.
  **이 항목은 아직 아무도 확인하지 않았다.**
- **왜 눈으로만 보면 속는다**: gaming 탭은 예전에 적재된 채널이 남아 있어 **멀쩡해 보였다**. 실제로
  죽어 있던 건 "명단을 새로 채우는 경로"였고, 명단이 빈 music만 영영 못 채워져 `all` 폴백이 매번
  발동했다. 즉 **"탭이 비어 있지 않다"는 합격 기준이 아니다.**

**재현 절차**
1. 백엔드 배포 후, 갱신이 한 번 돌 때까지 기다린다(2시간 주기) 또는 수동 갱신:
   ```bash
   curl -s -X POST -H "Authorization: Bearer $TOKEN" "$BASE/shorts-hot/refresh"
   # "throttled"가 오면 최소 간격(10분) 안이다 — 기다렸다 다시.
   ```
2. 앱에서 P메뉴 → HOT → **All** 탭의 상위 5개 제목을 적는다.
3. **Music** 탭의 상위 5개 제목을 적는다.
4. **Gaming** 탭도 같이 본다.

**기대 결과**
- 3단계 Music 목록이 2단계 All과 **다르다**. **완전히 동일하면 FAIL**(= `all` 폴백을 타고 있다,
  즉 music 명단이 여전히 비어 있다는 뜻).
- Music 탭 내용이 실제로 **음악**이다.
- Gaming 탭이 실제로 게이밍 콘텐츠다(LCK/배그 등 — 이건 수정 전에도 통과하던 항목이라 단독으로는
  근거가 못 된다).

**확인 방법 (서버 로그)**
- Railway 로그에 `[ShortsHot] 검색 0건(publishedAfter=...) — 기간 제한 없이 재시도` 가 보이면
  **넓힘 경로가 실제로 동작한 것**이다(정상). 이 줄이 있는데도 최종 결과가 0건이면 다른 원인이다.
- `[ShortsHot] KR/music 비어 있어 all로 대체해 응답` 이 **계속** 찍히면 FAIL(폴백 상시 발동).

---

### D2. 쿼터를 태우지 않는다

- **플랫폼**: ☁️ 서버
- **원 이슈**: 2026-08-10 사고 — 배포 확인한다고 `refresh`를 8번 호출해 **쿼터를 태우고 HOT이 통째로
  비었다**(사장님: "다 출시했는데 어쩔거야"). D1을 검증하는 사람이 그대로 반복하기 가장 쉬운 실수다.

**절차 (검증 중 지켜야 할 규칙)**
- `POST /shorts-hot/refresh`를 **연속 호출하지 마라.** 최소 간격 10분 스로틀이 있고, 막히면 실패가
  아니라 `"throttled"`를 돌려준다 — 그걸 "실패했으니 다시"로 오해하지 말 것.
- 1회 호출 = 실제 쿼터 **약 160 units**다. 멱등이어도 공짜가 아니다.
- 채널 발견은 (국가,카테고리)당 **하루 1회** 제한이라, 같은 날 여러 번 refresh해도 D1의 발견 경로는
  다시 안 돈다 — **하루에 한 번만 의미 있는 검증**이라는 뜻이다.

**기대 결과**
- 검증을 마친 뒤에도 HOT 전 카테고리가 **비어 있지 않다**.

---

### D3. focus_allowance 행이 무한히 쌓이지 않는다

- **플랫폼**: ☁️ 서버
- **원 이슈**: `f38b136` (7일 보관)
- **자동화 상태**: cutoff 계산은 `FocusAllowanceServiceTest.보관기간_정리는_7일_이전_행만_지운다`가 잡는다.
  **실제 스케줄러(새벽 4시)가 프로덕션에서 도는지는 자동화 못 한다** — 그것만 수동이다.

**확인 방법**
- 배포 8일 후, Railway 로그에서 `[FocusAllowance] 보관기간(7일) 경과 N건 삭제 (cutoff=...)` 를 찾는다.
- 없으면 스케줄러가 안 도는 것(`@EnableScheduling` 누락, 인스턴스 슬립 등) — 용량이 무한 증가한다.

---

## 5. 🤖 빌드/배포 시 — 광고 종류 확인

### E1. 트랙별 광고 종류가 맞다

- **플랫폼**: 🤖 Android (iOS도 개념은 같다)
- **원 이슈**: `6d85114` — 비공개 테스트에 **실광고**를 올렸다가 되돌렸다. Play는 다운그레이드를
  거부해서 versionCode를 **7→8→9로 앞으로만** 갈 수밖에 없었다(되돌릴 수 없는 실수였다).

| 트랙 | 광고 | 이유 |
|---|---|---|
| 내부/비공개 테스트 | **테스트 광고** | 테스터 클릭 = 무효 트래픽 = **계정 정지 위험** |
| 프로덕션 | 실광고 | 실사용자에게 테스트 광고는 정책 위반 |

**확인 방법 (업로드 **전에** — 올린 뒤에는 못 되돌린다)**
- `.env`의 `EXPO_PUBLIC_USE_REAL_ADS` 값을 확인한다. ⚠️ `eas.json`의 값은 **EAS 빌드 프로필에만**
  적용된다 — 로컬 gradle 빌드는 `.env`를 읽는다. 이 차이가 실제로 사고를 냈다.
- 환경변수를 gradlew 앞에 붙이는 방식은 **안 먹는다**(Expo가 `.env`를 읽어 인라인한다).
- 번들 태스크가 캐시되므로 값을 바꾼 뒤엔:
  ```bash
  rm -rf android/app/build/generated/assets/react android/app/build/intermediates/assets/release
  ```
- AAB를 **까서** 실 단위 ID(`ca-app-pub-3201481146134957/...`)가 들어갔는지 직접 확인한다.

---

## 6. 자동화하지 못한 것과 그 이유

| 항목 | 왜 자동화 못 했나 |
|---|---|
| A1~A4 (광고 오버레이 / 이어서재생) | **AccessibilityService + 실제 유튜브 앱 + AdMob SDK**의 3자 상호작용이다. 접근성 트리를 읽어 "보이는 제목이 바뀌었나"를 판정하는 구조라, 실제 화면 위에 실제 광고가 떠야만 재현된다. 계측 테스트(Espresso/UIAutomator)로도 유튜브 앱과 광고 SDK를 제어할 수 없다. |
| A5 / B2 (프로세스 재시작 생존) | OS가 프로세스를 죽였다 살리는 것 자체가 검증 대상이다. `START_STICKY` 복구, 앱 스위처 종료, OEM 권한 회수는 JVM 안에서 재현 불가. |
| B1 (화면 언마운트 생존) | RN 네비게이션 생명주기 + AsyncStorage. **jest가 아예 설정돼 있지 않다** — 여기가 가장 큰 자동화 공백이다(아래 참고). |
| B4 / C1 (연장 5분 / 하루 3회) | 판정 로직은 `useFocusSessionStore`/`useFocusExtendAdStore`(TypeScript)에 있는데 JS 테스트 인프라가 0이다. 서버 쪽 병합만 Java로 잡혀 있다. |
| B3 / C2 / C4 (재설치 내성 기기 id) | Android SSAID(`Settings.Secure.ANDROID_ID`)와 iOS Keychain은 **OS가 앱 밖에서** 들고 있는 값이다. 앱 삭제/재설치라는 행위 자체가 검증 대상이라 어떤 테스트 러너로도 못 만든다. |
| C3 (오프라인 fail-open) | 실제 네트워크 단절이 필요하다. 모킹하면 "예외를 삼키는가"만 보게 되는데, 정작 회귀는 UI가 멈추는 형태로 나타난다. |
| C5 (쇼츠 검색) | 오버레이 UI + Vercel 프록시 실호출. 프리셋은 CDN 지오IP(`Vary: x-vercel-ip-country`)에 의존해 기기 위치가 결과를 바꾼다. |
| D1 (HOT 채널 발견) | **YouTube Data API 실호출**이 본질이다. `publishedAfter`가 0건을 만든다는 사실 자체가 실제 API 응답에서만 드러난다(파라미터만 바꿔 직접 호출해 확정한 값이다). 모킹하면 우리가 믿는 가정을 우리가 다시 확인하는 것뿐이라 이 버그를 못 잡는다. |
| D3 (보관 스케줄러 실행) | cutoff 계산은 자동화했다. `@Scheduled`가 프로덕션에서 실제로 깨어나는지는 배포 환경의 문제라 단위 테스트 밖이다. |
| E1 (트랙별 광고) | 빌드 산출물(AAB)과 스토어 트랙 설정의 문제다. 코드가 아니라 **번들에 무엇이 인라인됐는가**가 대상이다. |

### 🔴 가장 큰 공백 — JS/RN 테스트가 0개다

`useFocusSessionStore`의 `extend()`(남은 시간에 **더하기**, 다시 시작이 아님), `load()`(마감시각이
지났으면 `timedOut`을 세움), `mergeServer()`(마감시각은 **더 이른 쪽**을 채택)는 **전부 순수 로직**이라
원래 자동화 가치가 가장 높은 축에 속한다. 오늘 재발한 버그(B1/B2/B4)가 정확히 이 함수들에 있었다.

지금은 jest가 설정돼 있지 않아(`package.json`에 test 스크립트 없음) 손을 못 댔다. 다음 작업으로
`jest-expo` + `@testing-library/react-native`를 붙이면 위 셋은 **바로 자동화된다** — 그때 이 문서에서
B1/B2/B4의 일부를 덜어낼 수 있다. **AsyncStorage 모킹만 있으면 되고 네이티브는 필요 없다.**

---

## 7. 이 문서를 쓰는 방법

1. 자기 플랫폼 항목만 순서대로 돌린다(🤖는 Windows 세션, 🍎는 맥 세션, 🌐는 양쪽 각각).
2. **결과를 `PACE_PROJECT_MANAGEMENT.md`에 날짜 + 세션(Mac/Windows)과 함께 남긴다.**
   로그를 봤으면 로그 줄을 그대로 붙여라 — 이 프로젝트에서 "확인했다"는 말만 남은 기록은 나중에
   전부 다시 검증해야 했다.
3. FAIL이 나오면 **원 이슈 커밋을 먼저 읽어라.** 커밋 본문에 증상·원인·실기기 로그가 다 있다.
4. 새 버그를 고쳤으면 **여기에 항목을 추가**한다. 자동화 가능한 것(순수 로직)이면 문서가 아니라
   `backend/src/test/...` 또는 (jest가 붙은 뒤엔) JS 테스트로 넣어라 — 문서는 사람이 안 돌리면
   아무것도 못 막는다.
