# PACE — 기능 × 플랫폼 매트릭스 + 통합 테스트 (상시 유지 파일)

> **이 파일 하나만 본다.** 공통 기능과 OS 차이를 갈라 두면 한쪽만 고쳐지고 다른 쪽이 조용히
> 썩는다(실제로 그렇게 됐다 — 아래 "왜 이 파일이 필요했나" 참고). 기능을 추가하거나 고칠 때
> **여기 표를 먼저 갱신하고** 테스트를 붙인다.
>
> - 날짜별 회귀 실행 기록은 `QA_REGRESSION_YYYY-MM-DD.md`에 남긴다(이 파일은 상시본).
> - 🤖 = Android 세션(Windows), 🍎 = iOS 세션(Mac). 각자 자기 표시 항목을 검증한다.

---

## 왜 이 파일이 필요했나

2026-08-12에 실기기에서 드러난 것:

1. **수면 오탐** — 무입력 시계를 리셋하는 실사용 경로가 "재생위치를 읽을 수 있는 앱"에만
   있었다. 틱톡을 추가하자 2026-08-02에 고쳤던 오탐이 **그대로 되살아났다.**
   기능(수면감지)은 공통인데 그 전제(재생위치 관측)는 앱마다 달랐고, 아무도 그걸 표로
   갖고 있지 않았다.
2. **자동넘김** — 안드는 되고 iOS는 원리적으로 불가인데, 그 차이가 코드 주석에만 흩어져
   있어 매번 다시 조사했다.

→ **전제(무엇이 있어야 이 기능이 성립하는가)를 표로 고정한다.**

---

# 1부. 기능 × 플랫폼 지원 매트릭스

## 1-1. 근본적인 구조 차이 (이걸 모르면 아래 표가 안 읽힌다)

| | 🤖 Android | 🍎 iOS |
|---|---|---|
| 무엇을 조작하나 | **설치된 진짜 앱**(YouTube/TikTok) | **WebView 안의 웹사이트** |
| 조작 수단 | `AccessibilityService` + `dispatchGesture` | 페이지 JS |
| 입력 신뢰도 | OS 레벨 주입 → 앱이 진짜 입력으로 받음 | 합성 이벤트 → `event.isTrusted=false` |
| 화면 관측 | 접근성 트리(노드·RangeInfo) | DOM |

**이 한 줄이 대부분의 차이를 설명한다.** iOS는 틱톡 WebView 안에서 다음 영상으로 못 넘어간다.

⚠️ **2026-08-12 정정 — `isTrusted` 가설은 실기기 테스트로 반증됐다.** 처음엔 페이지 JS가
만든 합성 입력이 `event.isTrusted=false`라 막히는 거라고 추정했다(합성터치·wheel·키보드·
scrollTop·Swiper `slideNext`·`scrollIntoView`·PointerEvent·실제 media `ended` 이벤트 —
**8개 기법 전부 실패**, `27daef1`/`8083b0b`). 그런데 사장님 실기기에서 **진짜 손가락 스와이프**로
직접 테스트한 결과 — **똑같이 딱 1번만 넘어가고 그 다음부턴 영구히 멈췄다.** 진짜 신뢰된 OS
레벨 입력도 실패했으니 트러스트 문제가 아니다. 남은 유력한 설명: **로그인 안 한 익명 WebView
세션에 틱톡이 영상을 딱 2개까지만 주고 그 이상은 로그인/앱 설치를 유도하려고 의도적으로
막는 구조**(가설, 완전히 확정된 건 아님 — 로그인 세션으로 테스트하면 검증 가능하나 아직 안 함).
→ 결론: 지금 아키텍처(WebView로 페이지 통째로 띄우기)로는 iOS 틱톡 자동넘김이 **원리적으로
안 된다.** 되게 하려면 안드처럼 완전히 다른 방식(Apple Screen Time API로 진짜 틱톡 앱을 열고
시스템 레벨에서 제한 — 별도 프로젝트급)이 필요하다.

## 1-2. 앱별 지원 현황

| 기능 | 🤖 YouTube | 🤖 TikTok | 🍎 YouTube | 🍎 TikTok |
|---|---|---|---|---|
| 앱 실행 / 콘텐츠 재생 | ✅ | ✅ | ✅ | ✅ (로그인벽·캡차 없음, `27daef1`) |
| 알약(남은시간) 표시 | ✅ | ✅ | ✅ 인앱 | ✅ 인앱 |
| 사용시간 차감 | ✅ | ✅ | ✅ | ✅ |
| **자동넘김(영상 끝 자동 전환)** | ✅ 재생시간 텍스트 | ✅ **SeekBar RangeInfo** | ❌ | ❌ `isTrusted` |
| 손짓 / BT 리모컨 넘김 | ✅ | ✅ (트리 불필요) | ❌ | ❌ |
| 수면감지 | ✅ | ✅ (관측 가능 확인 후) | ✅ 인앱 블랙아웃 | ✅ 인앱 블랙아웃 |
| HOT 목록 | ✅ | ❌ 스크래핑 차단 | ✅ | ❌ |
| 검색 | ✅ | ❌ | ✅ | ❌ |
| 홈 카드 노출 | ✅ | ✅ | ✅ | ❌ 미노출 |

### 각 ❌의 이유 (추측 금지 — 확인된 근거만)

| 항목 | 이유 | 근거 |
|---|---|---|
| 🍎 자동넘김 전무 | iOS는 다른 앱의 UI/제스처를 조작할 수단이 없다 | `autoNextService.ios.ts` — `supportsAutoNext: false` |
| 🍎 TikTok 자동넘김 | 익명 WebView 세션이 영상 2개 이상 안 줌(가설) — 트러스트 문제 아님 | 맥 세션 8개 기법 + **실기기 진짜 손가락**까지 전부 동일하게 1회만 이동 후 정지 (`27daef1`/`8083b0b`) |
| TikTok HOT/검색 | 서명(X-Bogus/msToken) 없이는 API 직접 호출 불가 | 스크래핑 차단 확인 |
| 🍎 TikTok 카드 미노출 | 목록을 못 얻어 빈 피드가 됨 | `home.tsx` — `Platform.OS === 'android'` 가드 |

## 1-3. 기능이 성립하기 위한 **전제** ⚠️ 가장 중요

여기를 안 채우면 위 오탐이 반복된다. **새 앱을 추가할 때 이 표부터 채운다.**

| 기능 | 전제 | YouTube | TikTok | 전제가 깨지면 |
|---|---|---|---|---|
| 사용시간 차감 | 포그라운드 판정 | 접근성 창 + UsageStats | 동일 | 시간이 안 깎이거나 안 볼 때 깎임 |
| 자동넘김 | **재생 진행률 관측** | `현재/전체` 시간 텍스트 | **SeekBar `RangeInfo`** | 영영 안 넘어감 |
| 수면감지 | **"깨어있음" 증거 채널** | `totalSec` 변화 | 진행률 급락 / 영상 지문 | **보고 있는데 화면이 꺼진다** |
| 알약 표시 | 대상 앱 창 가시성 | `getWindows()` | + UsageStats 폴백 | 알약이 안 뜨거나 홈에서 뜸 |

> **깨어있음 증거 채널이 없는 앱은 수면감지를 확정하면 안 된다.**
> `PaceOverlayService`의 SUSPECT→PROMPTED 승격에 `canObserveWatchEvidence()` 게이트가 있는
> 이유다. 오차 방향은 **"자는 사람을 못 잡는 쪽"**이어야지 "보는 사람 화면을 끄는 쪽"이면 안 된다.

## 1-4. 시간 규칙 — 앱별인가 공용인가 (2026-08-12 사장님 확정)

> **한도·타이머는 앱 구분 없이 공용이다.** 유튜브를 30분 보면 틱톡에 남는 건 30분이다.
> 근거: 앱의 목적이 "쇼트폼 **총** 시청시간 줄이기"이므로, 앱을 갈아타며 보는 회피를 막아야 한다.

| 항목 | 규칙 | 구현 위치 |
|---|---|---|
| 일일 한도(기본 60분) | **공용** | `dailyLimitMinutes + bonus − todayUsageMinutes` |
| 포커스 세션 타이머 | **공용** (`endsAt` 하나) | `useFocusSessionStore` |
| 광고 연장 하루 3회 | **공용** | `focus_allowance` |
| 홈·분석 사용시간 표시 | **합계 하나** (앱별 분리 표시 없음) | `getTodayUsageMinutes` — `platform_app` 조건 없음 |
| 세션 기록의 앱 구분 | **앱별로 정확히 남는다** | 아래 참고 |

### 앱별 사용시간 표시 (2026-08-12 추가, `7fb7b20`)

분석 탭의 **Platform Breakdown**을 되살렸다(2026-07-26 `c0cb9b6`에서 "유튜브 하나뿐이라
항상 100%"라며 삭제했던 것). 사장님 지시: *"틱톡을 안 봤을 땐 0이니까 안 나와도 되는데
봤으면 같이 나와야 하잖아."* 표시는 `12m · 60%`처럼 **분을 앞에** 둔다.

**🍎 iOS에도 그대로 적용된다 — 코드가 전부 공용이다.** 플랫폼 분기 파일이 하나도 없다:

| 파일 | `.ios`/`.android` 변형 |
|---|---|
| `src/app/(tabs)/stats.tsx` | 없음 (공용) |
| `src/store/useStatsStore.ts` | 없음 (공용) |
| `src/database/repositories/statsRepository.ts` | 없음 (공용) |

- iOS도 홈 카드 → `/overlay` 라우트를 타므로(`home.tsx`의 `Platform.OS === 'android'`
  분기는 **연결 애니메이션 유무**만 가르고, `router.push('/overlay', {platform})`은 양쪽 다 실행)
  `startSession(user.id, platform)`으로 **실제 앱 이름이 기록된다.**
- 진행 중 세션 합산에 쓰는 `overlayService.getWatchedSeconds()`도 iOS 구현이 있다
  (2026-08-06에 "iOS 통계가 반쪽"이라 고친 그것) — 벽시계 폴백으로 새지 않는다.
- **다만 iOS에는 틱톡 카드가 없다**(`home.tsx`의 `Platform.OS === 'android'` 가드).
  따라서 iOS 사용자는 앱이 유튜브 하나뿐 → `length > 1` 조건에 걸려 **섹션이 자연히 숨는다.**
  iOS에 틱톡이 들어오는 순간 코드 수정 없이 자동으로 나타난다.

**기록은 앱별로 정상이다** — 경로가 두 개이고 둘 다 맞다:
- `src/app/overlay/index.tsx:197` — 안드로이드 앱실드 경로. 라우트 파라미터의 실제 값
  (`youtube`/`tiktok`)을 그대로 기록한다.
- `src/app/feed/index.tsx:271` — iOS 인앱 피드. `'youtube'` 고정인데 **그 화면 자체가
  유튜브 전용**이라 맞는 값이다.

즉 `platform_app` 데이터는 이미 정확하게 쌓이고 있고, **표시만 합산**하고 있다.
앱별로 나눠 보여주고 싶어지면 쿼리에 `platform_app` 조건만 더하면 된다(데이터 마이그레이션 불필요).

### 홈 플랫폼 카드 — 이름과 커버 (2026-08-12 확정)

| 카드 | 제목 | 배지 | 커버 |
|---|---|---|---|
| YouTube | **`Shorts with PACE`** | `GUARDED` | `assets/covers/youtube.jpg` |
| TikTok | **`Loops with PACE`** | `LOOPS` | `assets/covers/tiktok.jpg` |

> ⚠️ **`YouTube with PACE` / `TikTok with PACE`는 쓰지 않는다** (사장님 확인 후 결정).
> YouTube·TikTok 브랜드 가이드라인은 자사 마크를 **다른 이름과 합쳐 새 명칭을 만드는 것**을
> 금지하고, Apple 5.2.1 / Play 지식재산 정책도 **제휴 오인**을 반려 사유로 본다.
> → 플랫폼 마크가 아니라 **제품명(Shorts/Loops)에만** PACE를 붙인다.

**🍎 맥 세션에 필요한 것 — 커버 이미지는 이미 git에 있다.**
`assets/covers/tiktok.jpg`는 `47cc6ff`부터 추적되어 있고 `origin/master`에 그대로 있다
(원본 프로토타입 `healthy-shorts-assistant(5)/src/assets/images/tiktok_cover_*.jpg`와
**바이트 단위 동일**, git hash `df0bbf2…`). 따로 올릴 게 없다.

대신 **오버레이 그라데이션 수정**(`3b5e4a1`)을 반드시 받아야 한다 — 이게 없으면
iOS에서도 틱톡 커버가 안 보인다:

| | 원본 프로토타입 | 잘못돼 있던 값 | 수정 후 |
|---|---|---|---|
| 방향 | 아래→위 **세로** | 왼→오른 가로 | 아래→위 세로 |
| 농도 | accent 40% → 검정 **80%** | accent 35% → 검정 **90%** | 검정 80% → accent |

유튜브 커버는 밝은 빨강이라 90% 검정 밑에서도 보였지만 **틱톡 커버는 어두운 야경이라
통째로 묻혔다** — 같은 컴포넌트·같은 프롭인데 한쪽만 안 보이던 이유다.

## 1-5. 알아두면 헛수고를 막는 확인된 사실

| 조사한 것 | 결과 | 다시 하지 말 것 |
|---|---|---|
| `MediaSession` / `PlaybackState` | `have 0 sessions` — 유튜브·틱톡 **둘 다 세션 안 올림** | 영상 길이 못 얻음 |
| `AudioPlaybackConfiguration` piid | 루프 시 같은 트랙 재사용(30초 불변) | 영상 종료 감지 불가 |
| `media.metrics` | audioserver 트랙 통계뿐 | 영상 길이 없음 |
| 틱톡 자체 Auto Scroll | 롱프레스 메뉴에 **없음**(기기 확인) | 앱에 맡길 수 없음 |
| `uiautomator dump` | **`RangeInfo`가 XML에 안 찍힌다** | 덤프만 보고 "진행바 없다" 판단 금지 |
| 틱톡 접근성 트리 | **노출됨(181노드)** — 설명·작성자·사운드 읽힘 | "틱톡은 트리가 없다"는 틀린 말 |
| 틱톡 다음영상 선로딩 | 오디오 트랙 2개 관측(재생 중 + `paused`) | 종료 직전 넘기면 로딩 없음 |

---

# 2부. 공통 테스트 (양 OS 동일하게 통과해야 함)

각 항목은 **양쪽 세션이 각자 자기 OS에서** 돌리고 결과를 아래 표에 적는다.

| # | 케이스 | 통과 기준 | 🤖 | 🍎 |
|---|---|---|---|---|
| K1 | 포커스 세션 시작 → 대상 앱 진입 | 알약이 뜨고 남은시간이 1분마다 줄어듦 | | |
| K2 | 대상 앱 밖(홈/런처)으로 나감 | 알약이 사라지고 시간이 **안** 깎임 | | |
| K3 | 광고 보고 5분 연장 | 광고가 중간에 끊기지 않고, 연장이 정확히 **5분** | | |
| K4 | 하루 3회 광고 한도 | 4번째는 막힘 | | |
| K5 | 앱 완전 종료 후 재실행 | 남은시간·게이트 상태가 살아있음 | | |
| K6 | 재설치 | 서버 허용량이 살아있음(같은 기기 id) | | |
| K7 | 오프라인 | 정상 사용자의 연장이 막히지 않음(fail-open) | | |
| K8 | **수면감지 오탐** ⚠️ | 대상 앱을 **손으로 넘기며** 20분 시청 → 세션이 안 끝남 | | |
| K9 | 수면감지 정탐 | 22시~9시 사이 무입력 15분+ → 팝업 → 30초 무응답 → 종료 | | |
| K10 | 검색 | 검색어 입력 → 결과 중 **고른 것만** 재생(자동 진행 없음) | | |

> **K8이 이번에 터진 그 케이스다.** 반드시 "폰을 책상에 두고 손가락으로만 넘기는" 방식으로
> 한다 — 폰을 들거나 흔들면 가속도계가 깨어있음으로 처리해 오탐이 가려진다.

---

# 3부. OS 차이 테스트 (한쪽만 존재하는 기능)

## 3-1. 🤖 Android 전용

| # | 케이스 | 통과 기준 |
|---|---|---|
| A1 | 자동넘김 — YouTube | 영상이 끝나면 다음으로 넘어감. 중간에 안 끊김 |
| A2 | **자동넘김 — TikTok** | `AUTO_NEXT reason=frac-predict` 후 **실제로 다른 영상**이 뜸 |
| A3 | 자동넘김 전환 품질 | 전환 시 로딩/버퍼링 없음(앱 자체 스와이프와 동일) |
| A4 | 손짓 / BT 리모컨 | 양쪽 앱에서 다음 영상으로 넘어감 |
| A5 | 이어서재생(HOT chain) | 대상 앱 **밖**에서는 발동 안 함 |
| A6 | 보상형 광고 중 | 광고가 떠 있는 동안 chain이 유튜브를 덮지 않음 |
| A7 | 접근성 꺼짐 | 기능이 죽더라도 앱이 죽지 않음. 수면감지는 **확정 보류** |

### A2/A3 확인 방법 (로그)
```bash
adb logcat -s PaceAccessibility:V | grep -E "RANGE_INFO|AUTO_NEXT|VIDEO_ADVANCE|gesture"
```
- `RANGE_INFO n=1 SeekBar(max=10000.0,frac=…)` → 진행률 관측 정상
- `AUTO_NEXT reason=frac-predict … remain=…ms` → 예측 발사
- `dispatchGesture accepted=true` + `gesture onCompleted` → 제스처 접수·완료
- **화면 캡처로 영상이 실제 바뀌었는지 확인할 것** — 로그만으로는 스와이프가 댓글창을
  연 건지 피드를 넘긴 건지 구분되지 않는다(실제로 그렇게 착각했다)

## 3-2. 🍎 iOS 전용

| # | 케이스 | 통과 기준 |
|---|---|---|
| B1 | 화면을 나갔다 와도 광고 게이트가 안 풀림 | |
| B2 | 앱을 죽였다 켜도 게이트·남은시간 유지 | |
| B3 | 재설치해도 같은 게스트 id(Keychain) | |
| B4 | 연장이 정확히 5분(10분 아님) | |
| B5 | 자동넘김 토글이 **보이지 않음** | `supportsAutoNext:false`가 UI에 반영 |
| B6 | 인앱 블랙아웃(수면) | 보고 있을 때 안 덮임 / 실제 수면 시 덮임 |

---

# 4부. 🍎 맥 세션에 요청 (Windows 세션 → Mac 세션)

1. **이 파일을 리뷰하고 iOS 쪽 내용을 직접 채워 주세요.** 특히
   - 1-2 표의 🍎 열에서 사실과 다른 칸
   - 3-2에 빠진 iOS 전용 기능(Live Activity, Flip 모드, 볼륨키 등 — 안드에 없는 것들)
   - 1-3 "전제" 표의 iOS판(WebView DOM에서 무엇을 관측할 수 있는지)
2. **2부 공통 테스트를 iOS에서 돌리고 🍎 열에 결과를 적어 주세요.** 특히 **K8(수면 오탐)** —
   안드에서 실제로 터진 케이스이고, iOS도 같은 상태기계(`useSleepGuard` + `sleepBlackout`)를
   씁니다. iOS에서 "깨어있음 증거"가 무엇인지 확인이 필요합니다.
3. **앞으로 기능 추가·수정 시 이 파일 한 곳만 갱신합니다.** 파일을 OS별로 쪼개지 마세요 —
   쪼갠 탓에 한쪽만 고쳐지고 다른 쪽이 썩는 게 이번 사고의 원인이었습니다.
4. 🔴 **앱별 사용시간(Platform Breakdown) iOS 검증** — `7fb7b20`. 코드가 전부 공용이라
   iOS에도 그대로 나갔는데 **저는 iOS에서 못 돌려봤습니다.** 확인해 주실 것:
   - ① 분석 탭이 예전처럼 정상 렌더되는가(회귀 없음). iOS는 앱이 유튜브 하나뿐이라
     **섹션이 안 보이는 게 정상**입니다 — 안 보이면 통과.
   - ② `getTodayUsageByApp`이 진행 중 세션을 `overlayService.getWatchedSeconds()`로
     합산합니다. iOS 구현이 null을 주는 경로가 남아 있으면 벽시계로 폴백해
     **같은 화면 안에서 "이번 주 시청"과 앱별 합계가 어긋납니다**(2026-08-04에 겪은 그 사고).
     세션이 도는 동안 두 숫자가 일치하는지 봐 주세요.
   - ③ iOS는 `feed/index.tsx:271`이 `'youtube'` 하드코딩입니다. 그 화면이 유튜브 전용인 한
     맞는 값이지만, iOS에 틱톡 피드가 생기면 **여기부터 고쳐야** 합니다(안 고치면 틱톡
     시청이 유튜브로 기록됩니다).

   ⚠️ 제 안드로이드 검증도 **미완**입니다 — adb 스크립트로 만든 세션이 제대로 닫히지 않아
   열린 행이 둘이 됐고, 집계가 열린 세션을 하나만 보므로 앱이 하나로 잡혀 섹션이 숨었습니다.
   정상 사용 흐름으로 재검증이 필요하고, 거기서도 안 나오면 "열린 세션 하나만 집계" 규칙을
   앱별 집계에서는 다르게 가져가야 합니다.

### 🍎 Mac 세션 응답(2026-08-12) — 위 4번 확인 결과

- **①**: 시뮬레이터에서 분석 탭 정상 렌더 확인(크래시/에러 없음). 오늘 사용량이 0이라
  Platform Breakdown 섹션 자체가 안 보이는 상태 — 예상대로 정상(통과). 실사용 세션을 걸어
  섹션이 실제로 "나타나는" 것까지는 시뮬레이터 탭 조작 수단이 없어 못 봄(스크린샷은 확보).
- **②**: 실기기가 지금 오프라인이라 진행 중 세션을 직접 만들어 두 숫자를 비교하는 라이브
  테스트는 못 했음 — 대신 코드로 확정: `getTodayUsageMinutes`와 `getTodayUsageByApp`의 열린
  세션 처리 로직이 **완전히 동일한 공식**(`statsRepository.ts:95-108` vs `116-135` —
  `wallClock = min(14400, now-started_at)`, `watched = overlayService.getWatchedSeconds()`,
  `elapsed = watched != null ? min(wallClock, watched) : wallClock`)이라 **구조적으로 어긋날
  수 없음**(같은 입력에 같은 함수를 두 곳에서 그대로 재사용). `overlayService.ios.ts:69`의
  `getWatchedSeconds()`도 스텁이 아니라 실제 구현(2026-08-06 `3ac55aa`) 확인 — null 폴백
  경로가 상시로 타는 상태 아님. 실기기 라이브 확인은 여전히 권장(엣지 케이스 대비)이지만
  코드상 회귀 가능성은 낮다고 판단.
- **③**: 확인함, 인지하고 있음 — 지금 진행 중인 iOS 틱톡 WebView 조사(아래 참고)가 실제로
  결실을 맺으면 `feed/index.tsx:271`의 `'youtube'` 하드코딩을 그 작업과 함께 고칠 것.

---

# 5부. 자동화 현황과 공백

| 층 | 상태 |
|---|---|
| 백엔드 단위 테스트 | ✅ 28개 (`FocusAllowanceServiceTest`, `ShortsHotServiceTest`, `RevenueCatServiceTest`) |
| JS/RN 테스트 | ❌ **0개** — `package.json`에 `test` 스크립트조차 없음 |
| 기기 스모크 자동화 | ❌ 없음 (adb 스크립트화 필요) |
| 빌드/업로드 게이트 | ❌ 없음 — `upload.mjs`가 검사 없이 AAB를 올림 |

> 🔴 **게이트가 없어서 vc10/vc11이 검사 없이 나갔다**(검색 자동넘김 버그, 미검증 틱톡 카드).
> 최소한 업로드 전에 2부 공통 테스트는 사람이 돌려야 한다.
