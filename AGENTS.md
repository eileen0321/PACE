# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Project management

Read `PACE_PROJECT_MANAGEMENT.md` first, every session (Windows=Android/common, Mac=iOS).
It has the current issue list, per-platform todo queue, and "next instructions" for each
session. Update it (issues + todo + progress log) before ending a session or after finishing
a significant task — it's the handoff channel between the Windows and Mac sessions.
