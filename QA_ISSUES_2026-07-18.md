# PACE — 기능별 QA 검증 리포트 (2026-07-18)

> 목적: iOS 시뮬레이터 실행 + 전체 소스 정적 분석으로 발견한 결함을 **다른 AI가 그대로 수정**할 수 있게 정리.
> 각 항목은 `파일:줄`, 증상, 재현/발현 조건, 수정 방향, **검증 상태**를 포함한다.
>
> **검증 상태 범례**
> - `RUNTIME` — 시뮬레이터에서 실제로 눈으로 확인함(스크린샷 있음)
> - `CODE` — 코드로 직접 확인함(로직상 확실)
> - `STATIC` — 정적 분석 결과(런타임 미확인, 수정 전 재현 권장)

---

## 검증 환경 & 방법

| 항목 | 내용 |
|---|---|
| 앱 | Pace (Expo Router / React Native 0.86 / Expo SDK 57), `com.pace.app` |
| 기기 | iOS 시뮬레이터 **iPhone 17 Pro (iOS 26.5)** — 신규 erase 후 부팅 |
| 빌드 | `npx expo run:ios` (Debug), Metro 연결 |
| 빌드 블로커(해결됨) | Xcode 26.6은 **iOS 26.5 시뮬레이터 런타임**을 요구하는데 최초엔 26.4만 설치돼 있어 `xcodebuild`가 시뮬레이터 destination을 못 찾음. `xcodebuild -downloadPlatform iOS`로 26.5 런타임 설치 후 빌드 성공. (개발 머신 재현 시 동일 조치 필요) |
| 검증 방식 | 화면별 렌더/i18n/에러를 스크린샷으로 확인. **이 환경에서는 시뮬레이터에 합성 탭·키 입력이 라우팅되지 않아**(접근성 미허용) 버튼 탭 기반 인터랙션 검증은 불가 → 화면 진입은 `src/app/index.tsx`의 초기 redirect를 화면별로 바꿔 재실행하는 방식으로 수행. 인터랙션 로직은 코드로 확인. |

### 참고(오탐 아님 / 수정 불필요)
- **"'Pace'에서 열겠습니까?" 다이얼로그**가 매 콜드 실행 시 뜨는 것은 **RN/Expo 디버그 빌드 아티팩트**다. 앱 시작 코드에 자기 스킴을 여는 `Linking.openURL` 호출이 없음(전수 grep 확인). 릴리스 빌드/실기기에선 발생하지 않으므로 **앱 버그 아님**.

---

## 요약 (심각도순)

| # | 심각도 | 제목 | 위치 | 상태 |
|---|---|---|---|---|
| 1 | 🔴 Critical | 세션 시청 시간이 항상 0으로 기록 → 모든 사용량 통계 0 고정 | overlay/index.tsx:84 | CODE+RUNTIME |
| 2 | 🔴 Critical | 세션 카운트다운이 전혀 안 줄어듦(`tickMinute` 미호출) | store/useTimerStore.ts:33 | CODE+RUNTIME |
| 3 | 🟠 High | Focus "세션 종료 → 예, 종료"가 아무 동작 안 함 | (tabs)/focus.tsx:62 | CODE |
| 4 | 🟠 High | "시간 연장(+10/20/30m)"이 영속 일일 한도를 영구 변경 | (tabs)/focus.tsx:61 | CODE+RUNTIME |
| 5 | 🟠 High | "설정 초기화"가 로그아웃만 하고 아무것도 초기화 안 함 | (tabs)/settings.tsx:53 | CODE |
| 6 | 🟠 High | Paywall이 "상품 정보를 불러오는 중"에서 무한 정지 — 가격·구매 CTA 없음 | paywall/index.tsx | RUNTIME |
| 7 | 🟡 Medium | Focus "PAUSE/계속" 버튼이 실제론 Auto Next를 토글(라벨 불일치) | (tabs)/focus.tsx:154 | CODE+RUNTIME |
| 8 | 🟡 Medium | 오버레이가 항상 "1개 시청"으로 기록(stale closure) | overlay/index.tsx:84 | CODE |
| 9 | 🟡 Medium | 설정 알림 토글이 로컬 state뿐 — 저장·연동 안 됨 | (tabs)/settings.tsx:132 | CODE |
| 10 | 🟡 Medium | 설정 Support 행(도움말/피드백/평가)이 탭해도 무반응 | (tabs)/settings.tsx:159 | CODE |
| 11 | 🟡 Medium | 매 실행 시 `[expo-notifications] Error reading persisted…` 런타임 에러 배너 | 앱 시작/알림 초기화 | RUNTIME |
| 12 | 🟡 Medium | Pace Feed 샘플 영상 로드 실패(-1102) + 흑백 재생 | feed/index.tsx | RUNTIME |
| 13 | 🟡 Medium | 휴식 알림 주기 불일치: Focus는 "15분마다"(하드코딩) vs 설정/홈은 20m | (tabs)/focus.tsx:188,192 | CODE+RUNTIME |
| 14 | 🟡 Medium | Sign in with Apple가 공식 버튼이 아닌 커스텀 텍스트 버튼 → 심사(4.8/HIG) 리스크 | auth/index.tsx:52 | CODE+RUNTIME |
| 15 | 🟡 Medium | Focus Android 권한 상태가 mount 시 1회만 조회(포커스 복귀 시 미갱신) | (tabs)/focus.tsx:42 | CODE |
| 16 | 🟢 Low | i18n 혼용: 홈/헤더 전체 영어, Focus의 "PAUSE"·"Break Reminder"도 영어인데 나머지는 한국어 | (tabs)/home.tsx, focus.tsx | RUNTIME |
| 17 | 🟢 Low | WeeklyGraphCard의 "목표 제한선" 값이 실제 dailyLimit과 무관하게 고정 가능성 | components/cards/WeeklyGraphCard.tsx | RUNTIME(부분보정) |
| 18 | 🟢 Low | Pace Feed 화면 하드코딩 KR/EN 문자열(i18n 미배선) | feed/index.tsx:99+ | STATIC |
| 19 | 🟢 Low | Pace Feed 진입 버튼이 iOS 전용 capability인데 Android에도 노출 | (tabs)/focus.tsx:243 | STATIC |
| 20 | 🟢 Low | 죽은 스토어 `useSessionStore` / `useCapabilityStore` 미사용 | store/useSessionStore.ts, useCapabilityStore.ts | STATIC |
| 21 | 🟢 Low | `Text.defaultProps` 전역 폰트 트릭이 React 19에서 무력화 가능성 | app/_layout.tsx:28 | STATIC(런타임상 폰트는 정상 표시됨 — 재확인 권장) |

---

## 상세

### 🔴 1. 세션 시청 시간이 항상 0으로 기록 — 모든 사용량 통계가 0 고정
- **위치**: `src/app/overlay/index.tsx:84` (쓰기) → `src/database/repositories/statsRepository.ts` (읽기: `SUM(duration_seconds)`)
- **증상**: 세션 종료 정리에서 `endSessionRow(sessionIdRef.current, 0, videoIndex + 1, …)`로 `durationSeconds`에 **하드코딩 `0`**을 넘긴다. 사용량의 유일한 소스가 `viewing_sessions.duration_seconds`라 홈 히어로(Today Session), Focus(시청/남음), Stats(이번 주·평균·플랫폼별), 주간 그래프가 전부 0에 고정.
- **런타임 근거**: Stats 화면 전 항목이 `0m / 0s / video / 0m`, 홈 `0 / 60 min (0% Complete)`. (스샷 `11_stats_7s.png`, `08_clean_launch.png`)
- **수정 방향**: 세션 시작~종료 실경과 초(또는 타이머 누적값)를 계산해 `0` 대신 전달.

### 🔴 2. 세션 카운트다운이 전혀 줄어들지 않음 — `tickMinute`가 죽은 코드
- **위치**: `src/store/useTimerStore.ts:33` (정의됐으나 호출부 없음 — `src/` 전체에 `setInterval`/호출 0건)
- **증상**: `remainingMinutes`는 `startSession`에서 1회 세팅 후 감소 안 함. 오버레이 카운트다운 정지, "5분/1분 남음" 넛지 미발생, `daily_limit_reached`·`sleep_timer_expired` 자동 종료(overlay/index.tsx:96-97) 미작동.
- **런타임 근거**: 오버레이 세션 화면이 "**60m Left**"로 고정(스샷 `16b_overlay_later.png`).
- **수정 방향**: 세션 마운트 시 60초 인터벌로 `timer.tickMinute()` 호출(+언마운트 정리).

### 🟠 3. Focus "세션 종료 → 예, 종료"가 아무 동작도 안 함
- **위치**: `src/app/(tabs)/focus.tsx:62` — `confirmFinish = () => { setShowFinishConfirm(false); }`
- **증상**: 확인 모달은 "오늘 세션 카운터를 종료·리셋"한다고 안내하지만 실제로는 모달만 닫는다. 세션 종료/카운터 리셋/타이머 정지/네비게이션 없음.
- **수정 방향**: `confirmFinish`에서 실제 세션 종료(타이머 endSession, 세션 row 기록, 통계 refresh) 수행.

### 🟠 4. "시간 연장(+10/+20/+30m)"이 영속 일일 한도를 영구 변경
- **위치**: `src/app/(tabs)/focus.tsx:61` — `update({ dailyLimitMinutes: settings.dailyLimitMinutes + amount })`
- **증상**: 세션 1회 연장이 아니라 AsyncStorage+SQLite에 write-through되는 **기본 일일 한도** 자체를 올린다. 이후 설정·홈의 기본 한도가 계속 부풀려짐.
- **런타임 근거**: Focus에 "시간 연장 +10m/+20m/+30m" 칩 렌더 확인(스샷 `13_focus.png`).
- **수정 방향**: 영속 `dailyLimitMinutes` 대신 세션 스코프 타이머 값을 연장.

### 🟠 5. "설정 초기화"가 로그아웃만 하고 실제로 아무것도 초기화 안 함
- **위치**: `src/app/(tabs)/settings.tsx:53` — `confirmReset = () => { setShowResetConfirm(false); logout(); }`
- **증상**: 제목/설명은 "모든 제한·기록 삭제"인데 `logout()`은 `USER_SCOPED_KEYS`만 지운다(설정 키 `pace_user_settings`·SQLite 로그 미포함). 백엔드가 없어 로그아웃 즉시 동일 `local-<deviceId>` 게스트로 재생성→동일 데이터 복귀. 설정·기록 그대로 남음.
- **수정 방향**: 설정 키 + SQLite 세션/통계 테이블을 실제로 비우고 기본값 재적재.

### 🟠 6. Paywall이 "상품 정보를 불러오는 중"에서 무한 정지 — 가격·구매 CTA 부재
- **위치**: `src/app/paywall/index.tsx`
- **증상**: RevenueCat 오퍼링이 로드되지 않아 상품/가격/갱신주기/구매 버튼이 전혀 렌더되지 않고 "상품 정보를 불러오는 중…" + "구매 복원"만 표시. (시뮬레이터에 RC 키/StoreKit 설정이 없어 발생 — 다만 로딩 실패/무상품 상태의 **폴백 UI가 없다**는 것이 문제.)
- **런타임 근거**: 스샷 `14_paywall.png`.
- **App Store 리스크**: 가이드라인 3.1.1/3.1.2 — 구매 전 가격·갱신주기·해지방법 고지 필요. 로딩 실패 시 재시도/오류 안내 UI 필요.
- **수정 방향**: 오퍼링 로드 실패/무상품 상태에 대한 에러·재시도 UI 추가. 실기기+RC 키로 상품 표시 재검증(jlpt `IOS_PRELAUNCH_TESTCASES.md` A. IAP 절 참고).

### 🟡 7. Focus "PAUSE/계속" 버튼이 실제론 Auto Next 토글 (라벨 불일치)
- **위치**: `src/app/(tabs)/focus.tsx:153-161` — `onPress`가 `update({ autoNext: !settings.autoNext })`
- **증상**: play/pause 아이콘 + "PAUSE"/"계속" 라벨이지만 세션 일시정지가 아니라 Auto Next 설정을 뒤집는다. 상단 상태 pill(ON/OFF)도 같은 값을 봐서 라벨과 의도가 어긋남.
- **런타임 근거**: Focus에 "PAUSE" 버튼 렌더(스샷 `13_focus.png`).
- **수정 방향**: 실제 세션 일시정지/재개 상태에 배선하거나 라벨을 "Auto Next"로 정정.

### 🟡 8. 오버레이가 항상 "1개 시청"으로 기록 (stale closure)
- **위치**: `src/app/overlay/index.tsx:82-92` (cleanup, deps `[user?.id]`)
- **증상**: cleanup 클로저가 첫 렌더의 `videoIndex(0)`를 캡처 → 매 세션 `videos_watched = 1` 기록. Auto Next로 여러 개 넘겨도 항상 1.
- **수정 방향**: 최신 인덱스를 ref로 읽어 기록.

### 🟡 9. 설정 알림 토글이 로컬 state뿐 — 저장·연동 안 됨
- **위치**: `src/app/(tabs)/settings.tsx:45-47, 132-134` — `notif5m/notifLimit/notifBreak`가 `useState`만
- **증상**: 저장(AsyncStorage/SQLite)도, `expo-notifications` 스케줄 연동도 없음. 재진입 시 리셋되고 실제 알림 동작을 제어하지 않음.
- **수정 방향**: 설정 스토어에 영속 + 알림 스케줄링 연결(또는 스위치 제거).

### 🟡 10. 설정 Support 행(도움말/피드백/평가)이 탭해도 무반응
- **위치**: `src/app/(tabs)/settings.tsx:159-161`, `ChevronRow` 243-249
- **증상**: 셰브론(이동 어포던스)이 있는데 `ChevronRow`가 `onPress` 없는 순수 `View`.
- **수정 방향**: Pressable로 바꿔 도움말 URL/메일/스토어 리뷰 연결하거나 셰브론 제거.

### 🟡 11. 매 실행 시 `[expo-notifications] Error reading persisted …` 런타임 에러 배너
- **위치**: 앱 시작/알림 초기화 경로(정확한 콜사이트는 디버거로 확인 필요)
- **증상**: 콜드 실행 직후부터 **모든 화면 하단에 빨간 에러 배너**가 상시 노출(LogBox `console.error`). 배너 텍스트: `[expo-notifications] Error reading persisted…`(뒤 잘림). 앱은 알림 권한 요청(overlay 진입 시 "Pace에서 알림을 보내고자 합니다")까지는 정상 동작하나, persisted 읽기에서 에러.
- **런타임 근거**: 스샷 `08_clean_launch.png` 외 전 화면.
- **주의**: iOS 시뮬레이터에서 expo-notifications가 부분 미지원(푸시 토큰 등)이라 **시뮬레이터 한정 경고일 수 있음** → 실기기에서 재현 여부 우선 확인. 재현되면 알림 초기화 로직을 try/catch + 조건부(권한/플랫폼) 처리.

### 🟡 12. Pace Feed 샘플 영상 로드 실패(-1102) + 흑백 재생
- **위치**: `src/app/feed/index.tsx` (+ 샘플 소스 상수)
- **증상**: Feed가 데모 영상 `https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4`를 사용하는데 Metro 로그에 `Failed to load available audio tracks … Error … Code=-1102 "요청한 리소스에 접근할 수 있는 권한이 없습니다."`(자막 트랙도 동일). 화면상 영상이 **흑백**으로 재생됨.
- **런타임 근거**: 스샷 `01_home.png`(Feed 재생 화면), Metro 로그 `build3.log`.
- **수정 방향**: (a) 실제 Pace Feed 콘텐츠 소스(Pexels/Pixabay 라이선스, `PACE_ARCHITECTURE.md`의 iOS 전략 참고)로 교체, (b) 로드 실패 시 폴백/에러 UI, (c) 흑백이 의도인지 확인(의도면 무시, 아니면 필터/틴트 제거).

### 🟡 13. 휴식 알림 주기 불일치 — Focus "15분마다"(하드코딩) vs 설정/홈 20m
- **위치**: `src/app/(tabs)/focus.tsx:188`(`t('focus.every15m')` 하드코딩 라벨), `:192`(토글 ON 시 `breakIntervalMinutes = 15`)
- **증상**: Focus는 "15분마다 작동"으로 표시/설정하는데 홈 QuickControls·설정은 "20m"로 표시. Focus에서 토글하면 20→15로 덮어써 값이 어긋남.
- **런타임 근거**: Focus "15분마다 작동"(스샷 `13_focus.png`) vs 홈 "BREAK REMI… 20m"(`08`) / 설정 "기본 휴식 알림 20m"(`12`).
- **수정 방향**: 실제 `breakIntervalMinutes` 설정값을 표시/사용하도록 통일.

### 🟡 14. Sign in with Apple가 공식 버튼이 아닌 커스텀 텍스트 버튼
- **위치**: `src/app/auth/index.tsx:51-55`
- **증상**: SIWA/Google 로그인이 카드 배경 + 텍스트만 있는 커스텀 `Pressable`. Apple `AppleAuthentication.AppleAuthenticationButton`(공식 로고 버튼) 미사용 → 가이드라인 4.8/HIG 위반 소지(심사 리스크). SIWA 버튼 자체는 iOS에서 렌더됨(존재 요건은 충족).
- **런타임 근거**: Auth 화면 렌더(스샷 `15_auth.png`, 소셜 버튼은 다이얼로그에 가려짐 — 코드로 확인).
- **수정 방향**: `expo-apple-authentication`의 공식 버튼 컴포넌트로 교체.

### 🟡 15. Focus Android 권한 상태가 mount 시 1회만 조회
- **위치**: `src/app/(tabs)/focus.tsx:42-56` — `useEffect(..., [])`
- **증상**: 주석은 "화면 포커스마다 재조회"라지만 실제론 mount 1회. Android에서 시스템 설정으로 권한 부여 후 복귀해도 "연결 안 됨/권한 필요" 유지(리마운트 전까지).
- **수정 방향**: expo-router `useFocusEffect`로 재조회.

### 🟢 16. i18n 혼용 — 홈/헤더 전체 영어, Focus 일부 라벨 영어
- **위치**: `src/app/(tabs)/home.tsx`(섹션/상태 전부 영어, 의도적이라 주석 있음), `components/ui/AppHeader.tsx`, `(tabs)/focus.tsx`("PAUSE", "Break Reminder")
- **증상**: 하단 탭바·Stats·Settings·Paywall은 한국어인데 홈/헤더는 전부 영어("CHOOSE PLATFORM", "TODAY SESSION", "GOOD AFTERNOON" 등), Focus도 "PAUSE"·"Break Reminder"만 영어. 언어 일관성 저해.
- **런타임 근거**: 스샷 `08`(홈 영어 + 탭바 한국어), `13`(Focus 혼용).
- **주의**: 홈 영어화는 "한국어가 길어 고정폭 카드가 넘친다"는 이유로 **의도적**(코드 주석). 수정 시 오버플로우 재검토 필요 → 알려진 UX 갭으로 취급.

### 🟢 17. WeeklyGraphCard "목표 제한선" 고정값 가능성
- **위치**: `src/components/cards/WeeklyGraphCard.tsx`
- **증상(보정)**: 정적 분석은 "하드코딩 영어 `Goal: Under 60 min` + 항상 Healthy"로 봤으나, **런타임에선 한국어 "목표 제한선 (60분)"으로 표시**됨(스샷 `11_stats_7s.png`) — 라벨은 번역돼 있음. 다만 현재 일일 한도가 60이라 "(60분)"이 실제값과 우연히 일치. **한도를 60이 아닌 값으로 바꿨을 때도 이 값이 따라오는지 확인 필요**. "Healthy" 뱃지의 조건부 여부도 재확인.
- **수정 방향**: 표시 값이 실제 `dailyLimitMinutes`를 참조하는지, 뱃지가 평균 vs 목표로 조건부인지 확인 후 배선.

### 🟢 18. Pace Feed 화면 하드코딩 KR/EN 문자열(i18n 미배선)
- **위치**: `src/app/feed/index.tsx:99,111,142,149,152` (+ Focus 진입부 242-249)
- **증상**: "Pace Feed 불러오는 중…", "표시할 영상이 없습니다.", "다시 시도", "DEV 샘플 피드 · …", "차단 대신 볼 건강한 대체 피드 · Auto-Next" 등 `t()` 미사용 리터럴. 영어 로케일 사용자에게 한국어 노출.
- **수정 방향**: `feed.*` 키 추가 후 `t()` 배선.

### 🟢 19. Pace Feed 진입 버튼이 iOS 전용인데 Android에도 노출
- **위치**: `src/app/(tabs)/focus.tsx:241-252` vs `src/services/platform/capabilities.ts`(`supportsPaceFeed: Platform.OS === 'ios'`)
- **증상**: 라벨 접미사 "(iOS)"만 iOS 조건, 버튼 자체는 무조건 렌더. Android에서 iOS 전용 기능 진입 가능.
- **수정 방향**: `capabilities.supportsPaceFeed`로 진입 자체를 게이팅.

### 🟢 20. 죽은 스토어 미사용
- **위치**: `src/store/useSessionStore.ts`, `src/store/useCapabilityStore.ts` (전 파일, `src/` 어디서도 import 0건)
- **증상**: 특히 `useSessionStore`는 세션 일시정지/재개/종료의 "단일 소스"로 설계됐지만 미사용(오버레이는 `useTimerStore` 사용). 방치된 리팩터.
- **수정 방향**: 실제 세션 흐름(#3/#7)을 이 스토어로 통합하거나 삭제.

### 🟢 21. `Text.defaultProps` 전역 폰트 트릭이 React 19에서 무력화 가능성
- **위치**: `src/app/_layout.tsx:28-30`
- **증상**: React 19에서 함수형 컴포넌트 `defaultProps` 지원 제거. 무력화되면 명시 스타일 없는 텍스트가 시스템 폰트로 폴백.
- **런타임 참고**: 이번 실행에서 한국어/영어 텍스트가 커스텀 폰트로 정상 표시돼 **당장 깨지진 않아 보임** → RN 0.86의 `Text` 내부 구현에 의존. 향후 업그레이드 리스크로만 기록.
- **수정 방향**: 커스텀 `Text` 래퍼 또는 테마 프로바이더로 대체.

---

## 정적 분석에서 "정상"으로 확인된 항목(수정 불필요)
- `t('section.key')` 참조 키 128개 전수 → `translations.en`에 모두 존재(누락 키로 원시 문자열 렌더되는 곳 없음).
- `router.push/replace` 대상(`/overlay`, `/feed`, `/dev/shorts-poc`, `/paywall`, `/auth`, `/(tabs)/home`) 모두 실제 파일 존재 + `_layout` 등록됨.
- SQLite `getDb()` in-flight 프라미스 캐시가 동시 init 레이스를 정상 방지.
- Auth 화면에 **iOS Sign in with Apple 버튼 존재**(요건 충족, 단 #14의 버튼 스타일 이슈만 별건).

## 후속(이 환경에서 미검증 — 실기기/탭 가능 환경에서 확인 권장)
- 버튼 탭 기반 인터랙션 전반(#3/#5/#7/#9/#10 실제 탭 결과), 세션 시작→통계 반영 end-to-end.
- RevenueCat 실 상품 표시·구매·복원(#6) — 실기기 + StoreKit 샌드박스.
- expo-notifications 에러(#11) 실기기 재현 여부.
- jlpt-master `IOS_PRELAUNCH_TESTCASES.md`의 IAP/AUTH/AUDIO 절을 실기기 리뷰어 런으로 1회.
